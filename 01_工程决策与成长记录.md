# EdgeGate 工程决策与成长记录

> 用途：以第一人称记录我在 AI 辅助开发中如何提出问题、识别约束、比较方案、作出决定并通过证据验收。
>
> 建立日期：2026-08-06
>
> 真实性原则：不把计划写成成果，不把阅读写成掌握，不把 AI 生成的代码写成我独立完成。

## 1. 为什么建立这份记录

这个项目会使用 AI 辅助需求梳理、讲解、代码审阅、故障分析和部分实现。如果最终只展示代码，很难说明我在项目中真正承担了什么。

这份记录不保存聊天流水账，而是保留能够体现我的工程参与度的内容：

- 我发现了什么问题。
- 我为什么认为它值得追问。
- 我识别了哪些事实、约束和风险。
- 我比较了哪些方案。
- 我作出了什么决定。
- AI 在其中提供了什么帮助。
- 我通过什么证据接受或否决结果。
- 我的理解和工程判断发生了什么变化。

它同时服务于：

- 项目过程复盘。
- README 和架构文档的事实来源。
- 简历项目描述。
- HR 面试和技术面试中的问题案例。

## 2. AI 与我的贡献边界

### 2.1 可以表述为我的工作

- 我提出需求、质疑、约束和验收标准。
- 我选择或否决技术方案，并能解释取舍。
- 我亲自完成或复核代码、命令和运行结果。
- 我根据日志、测试和数据判断是否通过。
- 我能复述关键机制，并说明能力边界。
- 我对最终公开内容和工程决策负责。

### 2.2 必须明确为 AI 辅助

- AI 生成的初始计划、讲义、代码或脚本。
- AI 对日志、代码和错误信息的分析建议。
- AI 提供的候选架构、伪代码和排障路径。
- 在我明确授权后由 AI 接管完成的困难实现。

### 2.3 不能写成个人成果

- 只读过但没有实现或验证的技术。
- 计划中的接口、性能和可靠性指标。
- 只由 AI 声称通过、但没有实际命令输出的测试。
- 尚未运行的 Ubuntu 构建、Sanitizer、压测和长稳。
- 无法解释关键设计或失败路径的代码。

## 3. 记录分类与状态

### 3.1 工程决策

编号使用 `D-xxx`，记录会影响产品范围、架构、接口、安全、开发流程或验收的选择。

### 3.2 问题与认知闭环

编号使用 `L-xxx`，记录我提出的关键问题，以及从误解、证据到当前理解的过程。

### 3.3 状态

每条记录必须标明：

- `已决定，未实现`。
- `已实现，未验证`。
- `已验证`。
- `已否决`。
- `待继续`。

同一条记录可以随着项目推进更新状态，但不能删除原来的判断依据。

## 4. 工程决策记录

### D-001：选择完整 Linux 反向代理，而不是再做一个功能 Demo

**日期**：2026-08-01，2026-08-11 完成首次验证

**状态**：核心网络部分已由 AI 实现并验证；`timerfd`、Unix Socket 和管理面仍未实现。

**我提出的问题**

上一个视觉跟踪云台项目虽然结合了软硬件，但 Linux 更多是运行环境，没有充分体现 Linux 应用层开发。我希望第二个项目把 Linux C/C++ 网络和产品工程放在核心位置，并形成一个完整产品，而不是只做几个 Socket 示例。

**事实与约束**

- 我已经完成阻塞 TCP、UDP、Unix Socket 和基本并发练习。
- 我还没有实际实现非阻塞 Socket、`epoll` Reactor、HTTP 状态机和产品化服务。
- 项目需要能放入简历，但不能靠堆砌不理解的技术名词。
- 我希望有可安装、可配置、可监控和可复现的最终成果。

**候选方向**

- 继续做硬件或视觉项目：与现有成果重复，Linux 应用层仍可能只是辅助。
- 做简单网络聊天室或 Echo 服务：适合练习，但难以体现完整产品。
- 做 HTTP/1.1 反向代理：能够连续使用协议解析、非阻塞 I/O、路由、可靠性、管理和可观测性。

**我的决定**

选择 EdgeGate：一个受控范围的 Linux C++ HTTP/1.1 反向代理。项目不绑定硬件、视觉、ROS 2 或具身智能。

**AI 的作用**

AI 检查了现有 C++、Linux 学习材料，提供了候选项目和能力差距分析，并协助形成产品计划。

**我的判断与收获**

我没有因为反向代理听起来“高级”就直接接受，而是要求先对照已有学习记录，确认它能从已学 TCP 自然过渡到非阻塞网络服务。项目价值不只来自核心转发，而来自可解释的协议、故障处理、配置、部署和验证闭环。

**证据**

- [项目计划.md](项目计划.md)
- [知识衔接.md](知识衔接.md)

**简历/面试使用**

当前只能描述为项目方向和规划，不能写成已完成产品。完成并验证后，可用于解释为什么选择反向代理作为 Linux 应用层综合项目。

### D-002：控制 HTTP/1.1 v1 范围，而不是追求协议全覆盖

**日期**：2026-08-01

**状态**：已决定，未实现。

**我提出的问题**

如果同时支持 TLS、HTTP/2、WebSocket、CONNECT、缓存和通用 TCP 转发，项目范围会迅速失控，我也无法对每种边界做可靠测试。

**约束识别**

- 核心训练目标是 TCP 字节流、HTTP 定界、非阻塞 I/O 和代理状态机。
- 第一版需要能在 10～12 周内形成可验证闭环。
- “支持很多协议名”不等于能够正确处理异常输入和失败路径。

**我的决定**

v1 支持：

- 普通 HTTP/1.1 请求。
- `Content-Length` 请求体。
- 顺序 Keep-Alive。
- 无正文、`Content-Length`、chunked、关闭连接四类响应。

明确不支持：

- 请求流水线和 chunked 请求体。
- `Expect: 100-continue`。
- CONNECT、WebSocket、HTTP/2、TLS。
- 缓存和通用 TCP 转发。

**取舍**

减少协议广度，换取对受控子集的增量解析、错误分类、故障处理和测试深度。

**AI 的作用**

AI 提供了常见 HTTP 代理能力列表及风险边界。

**我的判断与收获**

完整产品不等于功能无限。主动写出不支持项，可以防止代码把未知数据误解析为合法请求，也让测试和简历描述更诚实。

**简历/面试使用**

实现后可表述为：“通过明确协议子集和失败语义控制首版范围，优先保证 HTTP 消息定界、Keep-Alive 和故障路径的可测试性。”

### D-003：核心事件循环直接使用 Linux API

**日期**：2026-08-01

**状态**：已决定，未实现。

**我提出的问题**

项目目标是学习 Linux C/C++ 应用层。如果直接使用 Boost.Asio、muduo 或 libevent 完成网络层，虽然能更快做出功能，但非阻塞连接、事件分派、缓冲区和生命周期会被框架隐藏。

**我的决定**

- 直接使用非阻塞 Socket、LT `epoll`、`timerfd`、Unix Socket 和 RAII。
- 自行实现受控 HTTP 增量解析器。
- 允许使用 `yaml-cpp`、`spdlog`、`nlohmann/json` 和 GoogleTest 处理非核心辅助工作。
- Nginx、muduo 和 libevent 只在自己的最小实现完成后局部对照。

**取舍**

- 得到更直接的 Linux 网络编程训练。
- 承担更多状态机、资源释放和错误处理工作。
- 不追求第一版达到成熟框架的性能和兼容性。

**AI 的作用**

AI 提供了依赖边界和参考项目阅读顺序。

**我的判断与收获**

我区分了“能使用一个网络框架”和“理解框架替我解决了什么问题”。核心能力自行实现，配置、日志和测试使用成熟库，是当前学习目标下更合适的边界。

**简历/面试使用**

当前可如实表述为：“在 AI 辅助实现的项目中，采用非阻塞 Socket 和 LT `epoll` 组织 Reactor，并通过并发、慢连接、半关闭、背压和 fd 回收测试验证核心网络模型。”不能写成用户独立手写全部 Reactor；`timerfd` 与管理 Unix Socket 仍是计划。

### D-004：管理面和 Dashboard 首版默认只读

**日期**：2026-08-01

**状态**：已决定，未实现。

**问题**

我希望项目具备前端展示，但网页修改配置、重载和停止服务会引入认证、权限、请求伪造和误操作风险。

**我的决定**

- `edgegatectl` 通过本机 Unix Socket 提供状态、路由、上游、JSON 指标、重载、排空和停止。
- Dashboard 使用原生 HTML/CSS/JavaScript。
- Dashboard v1 只读，默认只监听本机管理地址。
- 网页控制和配置编辑不属于当前范围，若以后增加必须重新对齐安全设计。

**AI 的作用**

AI 解释了只读展示与写操作在安全边界上的区别，并提供了候选接口。

**我的判断与收获**

“有前端”不应自动扩展为“浏览器可以控制服务”。我选择先建立可观测性，再根据真实需求决定是否承担认证和写操作风险。

### D-005：Windows 保存权威源码，Ubuntu 负责 Linux 验证

**日期**：2026-08-01

**状态**：已验证。

**环境约束**

- 正式文件保存在 Windows `D:\桌面\EI\反向代理项目`。
- Linux 编译、调试和运行位于 Ubuntu `/home/zy/projects/edgegate`。

**我的决定**

- Windows 是唯一权威源码和 Git 历史。
- Ubuntu 是执行副本。
- 两端不同时编辑同一文件。
- 构建目录、日志、Socket 文件和敏感信息不参与源码同步。
- 结项并清理后再建立 GitHub 远端。

**风险认识**

本地 Git 能恢复历史修改，但不等同于异机备份。同步工具使用前必须实测排除项和覆盖方向。

**AI 的作用**

AI 帮助把已有工作习惯整理成明确规则，并执行首次 SSH/SCP 迁移、哈希比较和 Ubuntu 构建验证。

**验证证据**

- Windows 仍保留唯一 `.git`；Ubuntu 执行副本没有复制 Git 仓库。
- 9 个正式文件在 Windows 与 Ubuntu 的 SHA-256 全部一致。
- Ubuntu 目标路径为 `/home/zy/projects/edgegate`，构建产物只生成在其 `build/` 中。
- G++ 11.4.0、CMake 3.22.1、Git 2.34.1、Make 4.3 和 GDB 12.1 已确认。

**我的判断与收获**

双环境最危险的不是编译命令，而是产生两个都被当作“最新”的源码副本。先确定唯一权威来源，可以避免调试修改在同步时丢失。

### D-006：文档通过前不创建代码骨架

**日期**：2026-08-01

**状态**：已验证。

**我提出的要求**

先建立知识衔接和完整项目计划，再开始编码；每个阶段只在开始时给当前任务，不提前提供完整答案。

**我的决定**

- `知识衔接.md` 和 `项目计划.md` 是代码前置门禁。
- 我先实现，AI 先给解释、提示和伪代码。
- 只有我明确同意后，AI 才接管困难实现。
- 新对话通过 `00_项目上下文.md` 恢复状态。

**当前证据**

- 已完成并阅读全部知识讲义。
- 两份长期维护文档已经建立。
- 2026-08-06 我明确同意开始完整阶段 1。
- 目前只创建了最小构建入口和冒烟测试，没有代理业务代码。
- 2026-08-11 阶段 1 的同步、严格编译、CTest 和本地 Git 提交完成。

**我的判断与收获**

AI 可以快速生成大量代码，但如果我没有先理解问题和验收标准，最终只能得到一个无法解释的仓库。把文档设为门禁，是为了让实现服务于已确认目标，而不是让生成速度替代工程判断。

### D-007：讲义不降低难度，但新术语必须先解释

**日期**：2026-08-04 至 2026-08-06

**状态**：已执行于知识讲义，后续持续适用。

**我发现的问题**

第一版 `知识衔接.md` 更像知识地图，直接出现 HTTP、`EINPROGRESS`、`getsockopt()`、`epoll` 和 Reactor 等术语。我无法从中判断哪些是旧知识、哪些是新知识，也无法直接学习。

**我的要求**

- 不是降低技术难度。
- 文件夹中没有学习证据的知识一律按没学过处理。
- 新术语出现前先判断我是否知道。
- 必须先解释“是什么、为什么需要”，再给具体运行例子和项目用途。
- 新旧知识必须明确对照。

**执行结果**

- `知识衔接.md` 从知识地图重写为完整讲义。
- 第三章加入阻塞和非阻塞 `accept/recv/send/connect` 对照。
- 非阻塞 `connect()` 加入 `EINPROGRESS` 到 `SO_ERROR` 的完整过程。
- Reactor 加入 fd、事件和连接对象之间的实际分派过程。

**AI 的作用**

AI 根据反馈重写讲义并补充技术解释。

**我的判断与收获**

我没有把“看不懂”简单归因于自己基础不足，而是指出讲义缺失了前置概念和状态变化。技术深度和表达清晰不是对立关系；无法解释输入、状态和结果的术语堆砌也无法证明真正理解。

### D-008：陌生语法先完整示范，再由我亲手输入

**日期**：2026-08-06

**状态**：已应用于最小 CMake 基线，后续持续适用。

**我发现的问题**

在最小 CMake 任务中，最初只给了填空和提示。但我的困难不是不愿意动手，而是已经不记得 CMake 语法。如果没有一份完整示范，我无法从提示恢复具体命令和结构。

**我的决定**

- 对 CMake、Linux 系统 API 等尚不熟悉的语法，AI 先给出一份完整、最小、可运行的示范并逐段解释。
- 我亲手把示范输入正式源码。
- AI 再按真实文件逐行检查。
- 业务设计、状态转换和方案选择仍需要我理解和判断，不能把“先示范语法”扩展成未经同意的一次性代写全部业务。

**AI 的作用**

AI 提供最小 C++/CMake 示例和逐段说明，但没有直接写入五个基线文件。

**我的判断与收获**

“先让我写”不等于必须让我凭记忆猜语法。完整示范解决的是工具语法门槛，亲手输入、修正和实际验证负责把示范转化为我的经验。

### D-009：学习步骤细讲，机械执行合并汇报

**日期**：2026-08-12

**状态**：已决定，后续持续适用。

**我发现的问题**

阶段 2 中，编码任务按小步骤说明有助于我理解每个 Socket、缓冲区和返回值的作用；但 AI 负责的同步、构建、启动测试程序、读取日志和清理进程如果逐条展开，会让项目对话过于碎片化。

**我的决定**

- 需要我亲手实现的新知识和代码继续细分，先解释目的、完整示范和验收，再由我输入并接受审核。
- AI 负责的机械性同步、构建、测试和文档更新按一个小阶段合并执行与汇报。
- 每个编码小阶段保留一次审核，每个完整项目阶段再做一次总验收。
- 出现失败、证据冲突或需要我作出选择时，AI 再展开具体步骤和原因。

**影响**

这种粒度既保留学习过程和我的判断，也让工程执行信息围绕结果和异常组织，减少与理解无关的过程噪声。

### D-010：改为 AI 主导实现，并保留可追溯的代码来源

**日期**：2026-08-13

**状态**：已决定，后续持续适用。

**我发现的问题**

逐段照着示范输入代码有助于熟悉语法，但项目推进速度较慢。当前更需要先形成完整、可验证的工程成果，再通过阶段讲解回看架构、状态机、测试证据和关键取舍。

**我的决定**

- 后续主要由 AI 直接实现代码、测试、构建配置和脚本。
- 我在各阶段审阅通俗讲解、关键抉择、实现思路、能力边界和实测结果。
- AI 直接实现的连续代码块使用 `AI-CODE-BEGIN/END` 特殊注释，并登记到 `AI实现索引.md`。
- 既有由我根据示范亲手输入的代码不追溯标记；若 AI 后续实质重构整段函数，则重构后的范围按 AI 代码登记。
- 简历和面试中如实说明 AI 的实现作用，把我的价值放在范围控制、问题发现、方案判断、验证和复盘上。

**影响**

协作重点从“逐行输入训练”转向“工程闭环和审阅理解”。推进速度提高，但必须通过来源标记、测试证据和决策记录避免混淆贡献边界。

### D-011：阶段 6 路由匹配语义先固化为可测试规则

**日期**：2026-08-13

**状态**：AI 在用户授权连续推进期间作出的暂定实现决定；代码与测试已完成，等待用户阶段审阅，可在 YAML 接入前调整。

**为什么必须先决定**

“Host 通配 + 最长路径前缀”如果不写清优先级，会出现精确 Host 与通配 Host 同时命中、Host 携带端口、`/api` 是否匹配 `/apix` 等歧义。配置文件只是数据入口，不能替代业务语义。

**当前规则**

- Host 大小写不敏感，匹配前去掉端口和一个末尾点。
- 精确 Host 优先于任何通配 Host。
- 通配只允许 `*.example.com` 形式，匹配至少一个子域标签，不匹配裸 `example.com`。
- 多个通配同时命中时，后缀更长者优先。
- Host 层级确定后选择最长路径前缀；查询串不参与路径匹配。
- 当前采用类似 Nginx location 的字面前缀，因此 `/api` 也匹配 `/apix`；如产品希望目录边界语义，应在 YAML 接入前修改测试。
- 同一路由只在健康上游中轮询；无路由与无健康上游分别保留为 `404` 和 `503`。

**AI 的作用与贡献边界**

规则选择、实现和测试均由 AI 在本轮直接完成并标记。用户的贡献是此前确定了“Host 精确或通配 + 最长路径前缀、健康上游轮询”的产品边界；上述细化语义必须经用户复核后才可作为用户自己的设计判断表述。

**验证**

普通与 ASan/UBSan 路由测试均 `11/11`，覆盖大小写、Host/端口非法语法、精确/通配优先级、最长前缀、查询串、轮询、健康摘除、无路由、无健康节点和冲突配置。

## 5. 问题与认知闭环

### L-001：HTTP 消息不是一次 recv() 得到的字符串

**日期**：2026-08-04

**状态**：形成当前理解；阶段 3 独立解析器已用自动化测试验证，用户已完成源码阅读和笔记复核。

**我的问题和补充**

我在阅读 HTTP 章节时，主动补充了 Header、Body、`Host`、`Content-Type`、`Content-Length`、`User-Agent`、`Cookie`，并把请求解析过程写成“请求行 → Header → Body”。

**形成的理解**

- TCP 没有 HTTP 消息边界。
- `\r\n\r\n` 只表示 Header 结束，不一定表示整个请求结束。
- 存在 `Content-Length` 时还要继续读取对应字节数。
- 响应可能按无正文、`Content-Length`、chunked 或关闭连接定界。
- 解析器需要区分“完成、需要更多数据、协议错误”。

**证据与修正**

讲义示例 JSON 正文经过 UTF-8 字节计数为 32 字节，因此把原 `Content-Length: 33` 修正为 32。这个修正提醒我，协议长度必须按实际字节计算，不能凭字符外观估计。

**AI 的作用**

AI 补充了请求行、状态行、Header 大小写、chunk 格式和协议边界。

阶段 3 后半段按新的协作方式由 AI 直接实现，源码已使用 `AI-CODE-BEGIN/END` 标记；这部分不记为我的独立手写代码。

**我的收获**

我开始把 HTTP 看成“TCP 字节流上的状态解析规则”，而不是一次 `recv()` 得到的文本。

**后续验证证据**

- 请求解析测试 28 项、响应解析测试 16 项，覆盖一次输入、逐字节、全部二分点和固定种子随机分片。
- 响应的无正文、`Content-Length`、chunked、关闭连接四种结束方式均有独立测试。
- Ubuntu Debug 与 ASan/UBSan 构建均通过 CTest `45/45`。
- 2026-08-14 用户完成源码阅读和笔记复核后再次回归：当前请求/响应测试为 `29 + 17 = 46` 个；项目全量 Debug 与 ASan/UBSan 均为 `84/84`。前一行的 `28 + 16` 和 `45/45` 保留为阶段 3 初次完成时的历史数据。

### L-002：我以前学的是阻塞 Socket，不是非阻塞 Socket

**日期**：2026-08-05

**状态**：事实已通过既有代码确认；非阻塞实现待完成。

**我的问题**

我追问第三章以前究竟学的是阻塞还是非阻塞 Socket。

**证据**

- 既有代码使用 `socket(AF_INET, SOCK_STREAM, 0)`。
- 没有 `SOCK_NONBLOCK` 或 `fcntl(..., O_NONBLOCK)`。
- `accept()`、`recv()`、`send()` 按默认阻塞行为运行。
- fork 服务端只是让多个子进程分别阻塞。
- `send_all()` 处理短写，但不等于非阻塞。
- `select` 判断 fd 是否可能就绪，也不自动把 Socket 变成非阻塞。

**我的判断**

准确基础应表述为“已实现并验证阻塞 Socket，接触过 select 思想；非阻塞 Socket、`EAGAIN` 和 Reactor 是新知识”。

**我的收获**

我学会根据代码中的标志、返回值和错误处理判断程序模型，而不是因为出现了“并发”“短写”或 `select` 就误认为已经掌握非阻塞。

### L-003：send() 观察的是本地发送缓冲区，不是直接读取对方状态

**日期**：2026-08-05

**状态**：非阻塞处理代码已实现；应用高水位暂停读已验证，`send()` 确切返回 `EAGAIN` 的确定性实验仍待补充。

**我的问题**

我追问发送方怎样知道“对方内核发送缓冲区满”。

**关键澄清**

准确的数据路径是：

~~~text
我方应用
-> 我方内核发送缓冲区
-> 网络
-> 对方内核接收缓冲区
-> 对方应用 recv()
~~~

我方不能直接通过 `send()` 查询对方接收缓冲区。对方不读取时，TCP 接收窗口缩小，我方数据逐渐无法排出，我方内核发送缓冲区最终积满。

非阻塞 `send()` 返回短写或 `EAGAIN`，直接说明的是我方发送缓冲区当前无法继续接收更多应用数据。它可能由对端太慢、网络拥塞等多种原因造成。

**我的收获**

`send()` 成功只表示本地内核接受了数据，不表示对方应用已经接收或处理。应用层只需保存未写完数据，等待可写后继续，而不是猜测远端内部状态。

### L-004：epoll 的 fd 就绪不是线程调度状态

**日期**：2026-08-05

**状态**：阶段 4 代码、测试和用户源码复核均已完成。

**我的原理解**

我尝试把 `epoll` 理解成线程层次的调度：线程从阻塞态变成就绪态后，`epoll` 发出指令，再由程序推进线程运行。

**澄清后的区别**

| 对象 | “就绪”的含义 | 负责者 |
|---|---|---|
| 线程 | 可以被 CPU 调度运行 | Linux 调度器 |
| fd | 当前读写或错误处理可能取得进展 | Linux I/O 系统与 `epoll` |

真实过程：

~~~text
Socket fd 变为 I/O 就绪
-> 内核唤醒阻塞在 epoll_wait() 的线程
-> 线程进入可运行状态
-> 调度器分配 CPU
-> epoll_wait() 返回 fd 事件
-> 用户代码推进对应连接状态
~~~

**我的收获**

Linux 调度器决定哪个线程运行；`epoll` 告诉事件循环哪个 fd 值得处理；我的代码推进的是连接状态，不是线程状态。

**阶段 4 实现证据**

- 单个事件循环在线程中调用 `epoll_wait()`，返回后按 fd 找到 `EventHandler` 对象并调用 `on_event()`。
- 24 个并发客户端均完成回显，一个保持连接的慢客户端没有阻塞快速客户端。
- 这部分由 AI 直接实现并做了源码标记，不计为我的独立手写；2026-08-15 我已结合 `EventLoop`、`ListenerHandler` 和 `EchoConnection` 源码复核，能够解释 fd 就绪与线程调度的区别。

### L-005：Reactor 是代码组织思想，不是另一个 Linux API

**日期**：2026-08-05

**状态**：阶段 4 EventLoop/Handler 结构已验证，用户已结合实现完成复核。

**我的问题**

我追问 Reactor 是否是实现代码的一种思想，因为原讲义只列出 EventLoop、Listener、ClientConnection、UpstreamConnection 和 Timer，无法看出它们怎样工作。

**形成的理解**

~~~text
epoll
-> 返回哪些 fd 有事

Reactor
-> 根据 fd 找到对应对象
-> 调用这个对象应该执行的处理代码
~~~

例如：

~~~text
fd 3 可读 -> Listener 调用 accept()
fd 5 可读 -> ClientConnection 调用 recv()
fd 8 可写 -> UpstreamConnection 继续 send()
fd 4 可读 -> Timer 检查超时
~~~

Reactor 是“等待事件、维护 fd 与对象的对应关系、分派事件”的整体程序结构。代码中不一定必须存在名为 `Reactor` 的类。

**我的收获**

我区分了底层工具和上层设计：`epoll` 解决“谁有事”，Reactor 解决“知道以后代码怎样组织”。

**阶段 4 实现证据**

- `EventLoop` 封装 epoll 注册、修改、删除和等待。
- `ListenerHandler` 处理监听 fd，`EchoConnection` 处理连接 fd；它们实现共同的 `EventHandler` 接口。
- 回调内删除连接采用“先从 epoll 移除、本轮分派结束后再析构”，避免对象执行成员函数期间删除自身。
- 阶段初次完成时普通与 ASan/UBSan 全量 CTest 均 `61/61`，50 次连接后 fd 数恢复；用户复核完成后于 2026-08-15 再次全量回归，两种构建均为 `84/84`。

### L-006：CMake 能配置不代表严格编译真的启用

**日期**：2026-08-11

**状态**：已验证。

**问题过程**

我根据完整示范亲手输入最小 CMake 文件，第一次静态检查发现 4 处问题：

- `cmake_minimun_required_` 拼写错误。
- `CMAKE_CXX_COMPLIER_ID` 把 `COMPILER` 写错。
- `add_complie_options` 把 `compile` 写错。
- `app/edgegatectl_main.cpp` 与真实 `apps/` 目录不一致。

**为什么第二处特别值得注意**

错误的 `CMAKE_CXX_COMPLIER_ID` 可能让 `if()` 条件直接不成立。项目后续仍可能配置和编译成功，但 `-Wall -Wextra -Wpedantic` 没有真正启用。这类错误比直接报错更隐蔽。

**修正和验证**

- 我在 Windows 修正 4 行，AI 只读复核。
- AI 将正式源码复制到 Ubuntu，并验证 9 个文件哈希一致。
- Ubuntu CMake 配置和三个目标构建成功，无警告。
- CTest `1/1` 通过。
- 读取三个目标的 `flags.make`，都实际包含 `-g -Wall -Wextra -Wpedantic -std=c++17`。
- `edgegate` 输出 `EdgeGate baseline`。
- `edgegatectl` 输出 `EdgeGate control baseline`。

**AI 的作用**

AI 提供完整示范、指出准确错误行、执行首次迁移和 Ubuntu 验收。

**我亲自完成的部分**

我创建目录和文件、亲手输入五份内容，并根据错误说明修正 CMake。

**我的收获**

构建成功只能证明当前代码能生成目标，不能证明期望的质量开关生效。对重要编译选项要检查实际生成的编译命令或标志，而不是只相信 CMake 文件看起来正确。

**简历/面试候选表达**

这条目前适合作为学习和排障案例；等项目形成更多构建与测试能力后，再合并为工程化构建描述。

### L-007：错误输出方式应由错误来源决定

**日期**：2026-08-12

**状态**：已理解并在阶段 2 最小监听代码中采用。

**我提出的问题**

我发现代码有时使用 `std::perror()`，有时使用 `std::cerr`，于是追问这种差异是否有明确规则，而不是照抄不同写法。

**判断依据**

- `socket()`、`bind()`、`listen()`、`accept()`、`recv()` 等 Linux API 返回 `-1` 时，内核会把具体失败原因写入 `errno`；此时使用 `std::perror("操作名")`，可以同时输出操作名和 `errno` 对应的文字原因。
- Header 超过项目上限、客户端在 Header 完整前关闭等情况，是 EdgeGate 根据自身规则得出的结论，没有新的 Linux API 错误可读取；此时使用 `std::cerr` 输出程序自己的说明。
- 如果在程序自行判断失败时错误调用 `std::perror()`，它可能输出旧的 `errno`，造成误导。

**验证证据**

- Ubuntu 严格编译通过且无警告。
- `curl` 连接 EdgeGate 后，服务端按实际长度输出 79 字节 Header。
- 当前主动关闭但不返回 HTTP 响应时，`curl` 报告 `Empty reply from server`；这是协议层尚未实现，不是 `recv()` 系统调用失败。

**我的收获**

选择错误输出方式前，先判断错误来自“Linux 系统调用失败”还是“程序业务规则不满足”。日志必须描述真正的错误来源，否则文字看起来详细也可能是错误证据。

### L-008：固定缓冲区可以复用，但必须先完成当前批次处理

**日期**：2026-08-12

**状态**：已验证。

**我提出的问题**

在阅读上游响应转发循环时，我发现每次 `recv()` 都从 `response_buffer.data()` 指向的第一个位置写入，于是质疑第二次读取是否会覆盖第一次数据并造成丢失。

**判断过程**

- `response_buffer` 只是一次读取的临时中转区，不保存完整响应。
- 每轮先通过阻塞式 `send_all()` 把本批 `received` 个字节全部交给客户端 Socket 的内核发送缓冲区，然后才进行下一次 `recv()`。
- 下一轮覆盖用户态 `response_buffer` 时，上一批数据已不再依赖该数组。
- 最后一批即使小于数组容量，也只发送 `recv()` 返回的真实长度，不会发送数组尾部残留内容。
- 请求侧需要搜索 Header 边界并在之后整体转发，因此使用 `request.append()` 累计；响应侧当前不解析，可以采用“收到一批、转发一批”的流式中转。

**验证证据**

- Ubuntu 严格编译零警告。
- EdgeGate 收到并转发 79 字节请求，随后向客户端转发 207 字节响应。
- curl 经 EdgeGate 得到 21 字节正文 `edgegate-upstream-ok\n`，末字节为十六进制 `0a`。
- Python 上游记录 `GET / HTTP/1.1 200`，没有再次出现 `BrokenPipeError`。

**能力边界**

这个结论依赖当前阻塞式 `send_all()` 在返回前已处理完本批数据。后续改成非阻塞 Socket 后，若 `send()` 返回 `EAGAIN`，未发送字节必须保留在连接输出缓冲区中，不能直接覆盖。

**我的收获**

判断缓冲区能否复用，不能只看下一次写入是否从位置 0 开始，而要检查旧数据的最后一个使用者是否已经完成。缓冲区生命周期由数据处理时序决定。

### L-009：找到 Header 结尾只是简易边界判断，不是完整 HTTP 解析器

**日期**：2026-08-13

**状态**：问题已澄清；阶段 3 独立实现与自动化验证完成，代码由 AI 主导实现。

**我提出的问题**

进入阶段 3 前，我追问阶段 2 的
`while (request.find("\\r\\n\\r\\n") == std::string::npos)`
是不是简易版解析器。

**关键区别**

- 这段循环只回答“当前累计字节里是否出现 Header 结束标志”。
- 它不检查请求行是否合法，不拆分 Header，也不知道 `Content-Length` 后面还要读多少正文。
- 它不能区分畸形消息、容量超限和暂时缺少数据。
- 对响应来说，只找 `\r\n\r\n` 更不够，因为正文还可能按无正文、固定长度、chunked 或连接关闭结束。

正式增量解析器把结果明确分为“需要更多数据、消息完成、协议错误”，并把错误进一步分类。无论 TCP 将同一消息切成一个大块、逐字节还是随机小块，最终字段和完成位置都应一致。

**实现与验证证据**

- `RequestParser` 解析请求行、Header、唯一 `Host` 和 `Content-Length` 正文，并拒绝当前范围外能力。
- `ResponseParser` 解析状态行和四种响应定界，chunked 正文可跨任意分片。
- CTest 共 `45/45` 通过；ASan/UBSan 同样 `45/45` 通过。
- 首轮测试实际抓到“无 Header 响应”位置偏移错误，说明测试不仅是在证明正常样例，也能否定错误实现。

**我的决策与贡献边界**

我决定从逐段亲手输入切换为 AI 主导推进，并要求 AI 代码使用特殊标记，后续通过阶段讲解复核其思路。因此这里可体现我识别了“边界搜索不等于协议解析”、限定了受控协议范围并要求可验证实现，但不能写成我独立手写了全部解析器。

**简历/面试候选表达**

可如实表述为：“在 AI 辅助实现的 Linux C++ 反向代理项目中，识别出仅搜索 Header 分隔符无法处理 TCP 任意分片和 HTTP 正文定界，推动采用三态增量解析接口，并用逐字节、全二分点、随机分片和异常输入建立验证矩阵。”

### L-010：内核 Socket 缓冲区不能代替应用层 ByteBuffer

**日期**：2026-08-15

**状态**：已结合阶段 4 实现复核，普通与 Sanitizer 回归通过。

**我提出的问题**

阅读 `ByteBuffer` 时，我追问为什么还要自己构建缓冲区，能否直接使用系统自带的 Socket 缓冲区。

**形成的判断**

两层缓冲区保存的是不同状态：

~~~text
接收：网卡 -> 内核接收缓冲区 -> recv() -> input_ / HTTP 解析器
发送：output_ -> send() -> 内核发送缓冲区 -> 网络
~~~

- `recv()` 返回后，字节已经从内核取到应用；内核不会记录它属于哪条半请求、解析到哪个阶段或是否超过业务上限。
- `send()` 短写或返回 `EAGAIN` 时，未被接受的尾部字节从未进入内核，必须继续保存在 `output_`。
- 因此应用缓冲区不是重复造轮子，而是在保存协议状态、未处理输入和未完成输出。

**实现与验证证据**

- `ByteBuffer` 提供硬容量上限、可读偏移、消费和空间回收。
- `EchoConnection` 用 `input_` 保存已接收待处理字节，用 `output_` 保存待发送字节。
- 512 KiB 背压测试证明多轮部分读写后字节仍无丢失、无乱序。

**我的收获**

以后判断“能否复用系统缓冲区”时，我会先区分内核负责的传输状态和应用负责的协议/业务状态，而不是只看它们都叫缓冲区。

### L-011：监听 fd、已连接 fd 和服务器对象的生命周期必须分开

**日期**：2026-08-15

**状态**：已结合 `ReactorEchoServer` 和 `ListenerHandler` 源码复核。

**我提出的问题**

我追问服务器和 EventLoop 谁先创建，以及所谓“新客户端连接”与普通客户端连接有什么区别。

**形成的判断**

- 构造 `ReactorEchoServer` 时，先按成员声明顺序构造 `loop_`、`stats_`、`port_`，再进入构造函数体，因此注册监听 Handler 时 EventLoop 已经存在；构造函数结束后服务器对象才完整建立。
- TCP 握手由内核完成。监听 fd 的 `EPOLLIN` 表示已完成连接队列可取；`accept4()` 另外返回一条已连接 fd，监听 fd 本身继续接待后续连接。
- “新连接”只是刚完成、尚未被 `accept4()` 取出的 TCP 连接，不是新类型的客户端；同一个客户端程序也可以建立多条连接。

**我的收获**

我不再把“服务器”“监听 Socket”“客户端程序”和“一条 TCP 连接”混成同一个对象，能够按 fd 角色和 C++ 对象生命周期解释接入过程。

**简历/面试候选表达**

可如实表述为：“结合源码复核了非阻塞监听接入链路，能够区分监听 fd 与 `accept4()` 返回的连接 fd，并说明 EventLoop 成员构造、Handler 注册和 RAII 释放的先后关系。”

### D-012：阶段 6 只配置现有能力，并把 EdgeGate 设为转发信息信任边界

**日期**：2026-08-16

**项目阶段**：阶段 6，YAML 路由与上游池

**状态**：已决定，已实现，已验证。

**我发现的问题或提出的质疑**

在实现前，我指出只列出“调整哪些 Header”并不能支持我做决定，因为我还不知道调整动作解决什么问题，也不知道其他方案的结果。我要求先用一条具体 HTTP 请求说明输入和输出，再比较候选做法。

我同时决定：YAML 当前只配置已经实现的能力，以后实现超时、健康检查或日志时再新增配置项；上游当前只支持数字 IPv4，不支持域名，并继续追问“域名是否等于访问公网”。

**为什么值得追问**

- Header 策略决定上游看到的客户端身份是否可信，也影响两条 TCP 连接是否错误共享控制语义。
- 提前写入不生效的配置会制造“看起来可配、实际无效”的产品缺陷。
- 直接把域名加入单线程 Reactor 可能引入阻塞 DNS，影响所有连接。

**候选方案和取舍**

| 方案 | 优点 | 风险/成本 | 是否采用 |
|---|---|---|---|
| 原样转发所有 Header | 代码最少 | 客户端可伪造来源，Connection 跨连接含义错误 | 否 |
| 保留业务 Header，重建转发信息并隔离连接 Header | 身份边界明确，符合当前一次请求一条上游连接 | 需要重新生成请求/响应 | 是 |
| 把 Host 改为上游 IP | 某些固定后端方便 | 可能破坏依靠 Host 的虚拟主机 | 否 |
| 当前就支持域名 | 配置更方便 | 需要异步 DNS、缓存和失效策略，否则阻塞 Reactor | 否，后续按真实需求评估 |

**我的决定**

1. EdgeGate 不信任客户端自带的 `X-Forwarded-*`，删除后根据真实连接重建。
2. 客户端与 EdgeGate、EdgeGate 与上游是两条独立 TCP 连接，Hop-by-hop Header 不跨连接照抄。
3. 配置文件只出现运行时已经执行的字段；新增能力时同步扩展 YAML 和校验。
4. v1 当前上游只接受数字 IPv4。域名不必然访问公网：它也可以解析到内网地址；是否出公网由解析结果和网络路由决定。异步 DNS 不作为阶段 6 条件。

**AI 的作用**

AI 用具体请求前后对比解释 Header 作用，给出候选策略，并完成阶段 6 代码、测试、Ubuntu 同步和故障分析。

**我亲自完成的部分**

我识别出初始方案说明不足以支持决策，要求补充可观察例子；最终确定配置演进方式、IPv4-only 边界和转发 Header 的信任策略。阶段 6 源码由 AI 主导，不能写成我独立编码。

**验证**

- Debug、ASan/UBSan、Release 均 `94/94`。
- 三个真实后端连续六次结果为 `a/b/c/a/b/c`。
- 客户端伪造的 `X-Forwarded-For` 在上游请求中消失，真实地址 `127.0.0.1` 被重建。
- 无路由返回 `404`，无健康节点返回 `503`。

**我的收获与反思**

技术决策不能只看一组 API 或字段名称。先说明数据从哪里来、经过什么变化、谁信任谁，再讨论方案，才能判断安全边界和后续扩展成本。域名也是名称解析方式，不等同于公网访问能力。

**简历/面试候选表达**

可如实表述为：“在 AI 辅助实现的反向代理项目中，参与确定转发 Header 信任边界：区分两段 TCP 连接的 Hop-by-hop 语义，拒绝直接信任客户端 `X-Forwarded-*`，并将同步 DNS 排除在单 Reactor 当前范围外；通过三构建 94 项测试和三后端轮询联调验证。”

### D-013：阶段 7 选择真正的流式转发，而不是继续扩大整包缓冲

**日期**：2026-08-16

**项目阶段**：阶段 7 可靠性机制

**状态**：已实现，已验证，用户尚未逐段复核源码。

**我作出的决定**

在得知阶段 4 Echo 背压只约束“客户端到 Echo 再返回客户端”，不能约束真实代理的“上游到慢客户端”之后，我选择升级为跨两条连接的流式转发：请求体和响应体不等待完整消息进入内存，而是通过有界缓冲边读、边写；达到高水位暂停来源 fd，降到低水位再恢复。

同时接受以下安全边界：

- 每方向容量/高水位/低水位默认 `256/192/64 KiB`。
- 关闭定界响应强制关闭客户端，不额外重编码成 chunked。
- 只有无请求体的 GET/HEAD 且客户端尚未收到任何响应字节时，最多换一个不同健康节点重试一次。
- 已发送部分响应后发生故障只能关闭连接，不能在半条 200 后拼接 502/504。
- 主动 `/health` 连续 3 次失败摘除、连续 2 次成功恢复；真实请求故障也参与健康计数。

**候选方案和取舍**

| 方案 | 优点 | 风险/成本 | 是否采用 |
|---|---|---|---|
| 继续完整缓存响应 | 复用阶段 5/6，代码简单 | 大响应占用大，无法对慢客户端形成跨端背压 | 否 |
| 关闭定界响应转 chunked | 能保留客户端 Keep-Alive | 需要代理重新编码正文，状态和故障边界更复杂 | v1 否 |
| 真流式转发并保留原 chunk framing | 内存有界，可观察暂停/恢复，符合 Linux 网络训练目标 | 状态机、超时和失败处理复杂 | 是 |

**AI 的作用**

AI 提供候选边界并直接实现代码、测试、Linux 构建、`strace` 故障定位和文档。用户选择真流式路线并接受默认阈值、超时、健康检查和重试范围。

**验证与故障闭环**

- 4 MiB 响应配 32 KiB 测试缓冲与慢客户端，暂停/恢复计数均大于 0，内容完整。
- 测试发现 chunk 长度行无扩展时错误使用 `npos` 作为长度；逐字节用例定位并修复。
- `strace -f -e trace=network` 发现一轮 chunked 完成后仍在旧上游 fd 多读一次 EOF，误生成下一轮 502；在移除上游 fd 后立即退出读取循环，Keep-Alive 回归通过。
- Debug、ASan/UBSan、Release 全量 `110/110`；三后端真实链路为 `a/b/c/a/b/c`。

**简历/面试候选表达**

可如实表述为：“在 AI 辅助的 Linux C++ 反向代理中，参与确定跨客户端/上游两条连接的真流式转发边界，选择 256/192/64 KiB 有界缓冲、预响应 GET/HEAD 单次安全重试与健康阈值；通过 4 MiB 慢客户端、512 KiB 流式上传、部分响应故障、超时、主动健康恢复及三构建 110 项回归验证，并用 strace 定位旧上游 EOF 污染下一 Keep-Alive 请求的状态机缺陷。”

### D-014：管理 CLI 采用单次命令，并让 stop 与信号复用排空状态机

**日期**：2026-08-17

**项目阶段**：阶段 8 运行时管理

**状态**：已实现，已验证，用户尚未逐段复核源码。

**我发现的问题或提出的质疑**

在了解 `edgegatectl` 会查询状态、reload、drain 和 stop 后，我追问它为什么执行一条命令就退出，是否也可以一直挂起。经过比较，我决定当前版本采用单次管理命令，不做常驻交互 Shell。

**为什么值得追问**

CLI 是否常驻会影响协议状态、失败恢复、脚本化方式和后续 Dashboard 的职责。如果两种模式都不加区分地实现，会增加连接状态和用户界面代码，却没有对应的产品收益。

**候选方案和取舍**

| 方案 | 优点 | 风险/成本 | 是否采用 |
|---|---|---|---|
| 单次命令后退出 | 退出码明确、方便脚本和 systemd、连接无长期状态 | 连续观察需要重复执行 | 是 |
| 常驻交互 Shell | 可连续输入命令，体验像数据库客户端 | 要处理断线重连、提示符、半输入和长期状态 | v1 否 |
| 终端持续刷新监控 | 适合观察实时变化 | 与阶段 9 Dashboard 职责重复 | 否，由 Dashboard 承担 |

**我的决定**

1. `edgegatectl` 每次只执行一条命令并退出。
2. 查询结果默认输出人能读的文字；`stats --json` 提供脚本可读数据。
3. drain 只停止新代理工作，排空后仍保留管理通道；stop 和 SIGTERM/SIGINT 在排空完成或超时后退出。
4. reload 必须先完整验证，新配置失败时保留旧配置；监听端口、Socket 路径和缓冲容量等对象创建期参数不热换。

**AI 的作用**

AI 解释单次命令与常驻 Shell 的职责差异，给出安全边界，并直接实现 Unix Socket、JSON 协议、CLI、原子热重载、排空状态机、signalfd、异步轮转日志、自动测试和 Linux 实测。

**我亲自完成的部分**

我识别出 CLI 生命周期是一个需要明确的产品选择，提出常驻模式候选并最终决定采用单次命令。阶段 8 源码由 AI 主导，不能写成我独立编码。

**验证**

- Socket 权限为 `0600`。
- Debug、ASan/UBSan、Release 全量 `117/117`。
- 合法 reload 改变新请求路由；无效 reload 退出 1，旧配置继续服务。
- 重复 drain 成功且新连接被拒绝；500 ms 测试期限会回收卡住会话。
- CLI stop 与真实 SIGTERM 均退出 0，验收脚本输出 `STAGE8_CLI_ACCEPTANCE=PASS`。

**我的收获与反思**

“命令能不能一直挂起”不只是界面偏好，它会改变协议是否有会话状态。先把一次操作的输入、输出、失败和退出码做稳定，再把持续观察交给 Dashboard，可以让两个工具各自职责清楚。

**简历/面试候选表达**

可如实表述为：“在 AI 辅助的 Linux C++ 反向代理项目中，参与确定运行时管理边界：选择可脚本化的单次命令 CLI，要求配置重载失败保留旧快照，并统一 drain、CLI stop 与 SIGTERM 的平滑退出状态机；通过 Unix Socket 0600、三构建117项回归、无效重载、排空超时和真实信号进程测试验证。”

### D-015：聚合延迟指标，同时保留少量可排查的慢请求摘要

**日期**：2026-08-17

**项目阶段**：阶段 9 可观测与前端

**状态**：已实现，已验证，用户尚未逐段复核源码。

**我发现的问题或提出的质疑**

在确认阶段 9 是“后端统计、前端展示”后，我认为没有必要保存每一条请求记录，因为记录会随流量无限增加；但仅显示 P99 又只能说明“整体存在长尾”，不能告诉我具体应该排查哪个请求。因此我要求在聚合统计之外，保留少量真正有排查价值的慢请求信息。

**为什么值得追问**

可观测不是“数字越多越好”。保存全部请求会带来无界内存、隐私和检索成本；只保存分位数又失去诊断上下文。这个选择同时影响长期稳定性、数据安全和故障排查效率。

**候选方案和取舍**

| 方案 | 优点 | 风险/成本 | 是否采用 |
|---|---|---|---|
| 保存每一条请求详情 | 可以精确排序和回溯 | 内存无限增长，容易保存敏感数据 | 否 |
| 只保留固定延迟区间和 P50/P95/P99 | 内存固定，整体趋势清楚 | 无法定位具体慢请求 | 只作为总体指标 |
| 固定区间统计 + 有界慢请求摘要 | 内存固定，同时保留少量诊断线索 | 分位数为近似值，摘要不是完整审计日志 | 是 |

**我的决定**

1. Dashboard 使用独立的 `127.0.0.1:18081`，不与 `18080` 业务路由混用。
2. 延迟只累计固定区间，并展示近似 P50/P95/P99、最小、平均和最大值。
3. 默认仅保存最近 20 条超过 500 ms 的慢请求；数量和阈值可配置且有硬上限。
4. 慢请求只保留时间、方法、去查询参数路径、路由、上游、状态码、耗时和尝试次数；不保存正文、Cookie、Authorization 或查询参数。
5. Dashboard 只读，不加入 reload、drain、stop 或配置编辑入口。

**AI 的作用**

AI 解释 P99 是不断变化的总体阈值而不是一条固定请求，提出“固定桶 + 有界慢请求”的实现方案，并直接完成 C++ 指标模型、非阻塞 HTTP Dashboard、原生前端、配置、自动测试、Ubuntu 三构建和真实进程验收。

**我亲自完成的部分**

我先识别出阶段 9 本质是后端统计与前端展示，明确否决保存所有逐请求记录，并进一步要求不能只有 P99 数字，还要留下少量能用于排查的慢请求上下文。我决定采用独立端口和有界摘要。源码由 AI 主导，不能写成我独立编码。

**验证**

- Debug、ASan/UBSan（含泄漏检查）、Release 全量 `125/125`。
- 真实进程使用 150 ms 慢上游制造慢请求，JSON 中出现对应路径、路由、上游、状态和耗时。
- 六个带 `token=must-not-appear` 查询参数的请求完成后，Dashboard JSON 不含该字符串，慢请求路径也不含 `?`。
- `POST /api/dashboard` 返回 405，`GET /reload` 返回 404；网页不存在管理写入口。
- 验收脚本输出 `STAGE9_DASHBOARD_ACCEPTANCE=PASS`。

**我的收获与反思**

分位数适合回答“系统整体慢不慢”，慢请求样本适合回答“应该从哪里开始查”。产品可观测设计需要在总体趋势、诊断上下文、资源上限和隐私之间做取舍，而不是简单堆积日志。

**简历/面试候选表达**

可如实表述为：“在 AI 辅助的 Linux C++ 反向代理项目中，参与确定可观测方案：否决无界逐请求保存，选择固定延迟桶估算 P50/P95/P99，并用有界慢请求摘要补足排障上下文；要求 Dashboard 独立监听本机只读端口并剥离查询参数，通过三构建125项回归和真实慢上游端到端测试验证。”

### D-016：安装成功不等于服务验收成功

**日期**：2026-08-17

**项目阶段**：阶段 10 产品化交付

**状态**：已验证。

**我发现的问题或提出的质疑**

第一次执行 `cmake --install` 和 systemd 安装器后，终端显示服务已启动，但我没有在验收脚本中看到 PASS。我没有把“安装命令没报错”直接当成阶段通过，而是继续提供精确的失败行和终端证据。

**为什么值得追问**

产品化不只是把二进制复制到 `/usr/local/bin`。还必须证明运行用户、目录权限、管理 Socket、reload/restart、SIGTERM 排空和重复安装都符合预期。验收脚本如果静默退出或把 systemd 状态读错，会让真实缺陷和测试缺陷混在一起。

**根因与修复**

1. 脚本用普通 `zy` 用户检查 `0750 root:edgegate` 目录中的配置。普通用户无法遍历恰好证明最小权限正确，不应被当成产品失败。修正为 sudo 检查，并为 `set -e` 增加失败行号和命令。
2. 脚本在 `systemctl restart` 刚返回 active 时立即 stop。`Type=simple` 的 active 只表示进程已启动，不保证应用已注册 management Socket 和 signalfd。对照测试证明同一二进制在就绪后手动 SIGTERM 会退出 0 并记录排空；因此修正为 `edgegatectl status` 成功后才发 SIGTERM。
3. 脚本增加失败后恢复服务，避免验收本身留下不可展示的停止状态。

**AI 的作用**

AI 完成安装/systemd 代码与文档，并根据我提供的两次失败证据逐项定位脚本权限假设和就绪竞态，增加可观察失败与恢复逻辑。

**我亲自完成的部分**

我在 Ubuntu 中亲自执行了需要 sudo 权限的真实安装和验收，识别“没有 PASS”不是成功，并先后提供 line 69 进程身份断言、line 141 SIGTERM 日志断言和最终六项 PASS 证据。源码和脚本修正由 AI 完成，不写成我独立编码。

**验证**

- Debug、ASan/UBSan（含泄漏检查）、Release 均 `126/126`。
- 手动对照 SIGTERM：退出码 0，日志出现 `source=SIGTERM` 和 `all sessions closed`。
- 最终真实系统验收六项 PASS，包括低权限、代理/Dashboard、reload/restart、SIGTERM、配置不覆盖和总验收。
- 验收后 `edgegate.service` 为 enabled/active，Dashboard 返回 HTTP 200。

**我的收获与反思**

systemd 的 `active` 和应用层“已就绪”不是同一概念；权限拒绝也可能是安全设计正确，而不是产品故障。验收必须在合适权限下检查真实信号，并让失败本身可观测。

**简历/面试候选表达**

可如实表述为：“在 AI 辅助的 Linux C++ 反向代理产品化中，亲自执行 Ubuntu systemd 安装和验收，不将安装成功等同于运行验收；通过失败行证据区分最小权限的预期拒绝与脚本错误，并识别 `systemctl active` 早于应用就绪的竞态，最终完成低权限、reload/restart、SIGTERM 排空和配置不覆盖的端到端验收。”

### D-017：先测出单 Reactor 的真实边界，再决定是否迭代多线程

**日期**：2026-08-17

**项目阶段**：阶段 11，压测与加固

**状态**：已验证完成。

**我发现的问题或提出的质疑**

我认识到“已经能安装运行”还不足以让项目成为简历亮点，需要用多轮迭代和实际数据证明压力、故障及长时间运行下的行为。我允许 Ubuntu 虚拟机连续开机联网，接受真实执行 12 小时长稳，而不是用几分钟测试替代。

**为什么值得追问**

如果不先测量，直接加入多线程只能说明代码更复杂，不能证明性能或稳定性真的提高。没有请求正确率、尾延迟和资源趋势的数据，也无法区分产品能力与演示效果。

**已知事实与证据**

- 当前 EdgeGate 网络层是单 Reactor，异步日志另有一个工作线程。
- 虚拟机为 2 vCPU、3.8 GiB；客户端、代理和后端共用同一台虚拟机。
- 修复后的 100 并发/10 万请求为 100% HTTP 200、0 失败、0 `client_errors`、0 5xx；8623.29 QPS，P99 18.931 ms。
- Debug 和 ASan/UBSan 全量 127/127；故障矩阵全部 PASS。
- 12小时任务目录为 `/home/zy/projects/edgegate/artifacts/stage11/soak-20260817T094243Z`；最终连续采样43,203秒、1.2441亿请求、0失败，状态为 `PASS`。

**约束与能力边界**

- 数据是同一台 2 vCPU 虚拟机内的 localhost 小型 GET 测试，不代表生产机器或跨网络容量。
- 先完成 v1 单 Reactor 基线；是否增加多 Reactor/多线程要根据瓶颈证据另行立项。
- 长稳未结束前，不把阶段 11 或“12 小时无泄漏”写成已完成成果。

**候选方案和取舍**

| 方案 | 优点 | 风险/成本 | 是否采用 |
|---|---|---|---|
| 先加入多线程再压测 | 表面技术栈更多 | 没有基线，问题归因困难，扩大状态共享复杂度 | 否 |
| 先测当前版本，再按证据迭代 | 有真实基线，能发现正确性和指标问题 | 需要真实等待和报告边界 | 是 |
| 用短时测试代替 12 小时 | 很快得到结果 | 不能验证资源是否持续增长 | 否 |

**我的决定**

保留当前单 Reactor 作为 v1 基线，完成真实 100 并发/10 万请求、故障矩阵、Sanitizer 和 12 小时长稳。阶段结束后再根据 CPU、尾延迟和扩展目标决定是否做多线程版本，不为了简历关键词提前堆架构。

**AI 的作用**

AI 设计并实现压测器、采样器、故障及长稳脚本，执行 Linux 构建联调，分析第一轮指标异常，修复正常 Keep-Alive EOF 误计错误并补回归测试，整理实测报告。

**我亲自完成的部分**

我判断项目仍需迭代才能形成更强的简历证据，明确进入阶段 11，并提供虚拟机可连续开机和联网的运行条件；我接受以真实 12 小时结果而不是短测代替，并保留后续是否多线程的决策权。

**验证**

- `ctest --test-dir build-stage11-debug --output-on-failure`：127/127。
- ASan/UBSan 加泄漏检查：127/127，无报告。
- `tests/stage11_fault_acceptance.sh`：八类故障结果 PASS。
- `tests/stage11_load_acceptance.sh`：100 并发/100,000 请求 PASS。
- 12小时：8,599个资源样本最大间隔6秒，RSS/fd/线程均无持续增长，最终PASS。

**我的收获与反思**

性能工程不是先堆并发模型，而是先固定环境、负载和正确性标准，测出基线后再决定优化。稳定性也不是“跑起来没崩”，而是故障可复现、错误码正确、资源能回落，并且长时间趋势没有持续增长。

**简历/面试候选表达**

可如实表述为：“在 AI 辅助的 Linux C++ 反向代理项目中，主动要求从可运行 Demo 继续迭代为可测产品；先保留单 Reactor 建立受控基线，以100并发/10万请求、故障注入和Sanitizer证据发现并闭环Keep-Alive正常关闭误报问题，并完成连续12小时、1.2441亿请求、资源无持续增长的长稳验证，再依据数据决定多线程扩展，而非为技术关键词盲目增加复杂度。”

## 6. 后续记录模板

### D-xxx 或 L-xxx：标题

**日期**：

**项目阶段**：

**状态**：已决定，未实现 / 已实现，未验证 / 已验证 / 已否决 / 待继续。

**我发现的问题或提出的质疑**

描述具体现象，不写“好像不对”。

**为什么值得追问**

说明它影响正确性、性能、资源、安全、维护性、用户体验还是验收可信度。

**已知事实与证据**

- 代码位置。
- 日志或错误信息。
- 实际命令和输出。
- 环境和输入。

**约束与能力边界**

- 当前版本必须满足什么。
- 当前版本明确不解决什么。
- 哪些条件无法证明。

**候选方案和取舍**

| 方案 | 优点 | 风险/成本 | 是否采用 |
|---|---|---|---|
| 方案 A |  |  |  |
| 方案 B |  |  |  |

**我的决定**

写清选择什么、拒绝什么，以及为什么。

**AI 的作用**

写明 AI 提供的是讲解、候选方案、代码、审阅还是故障分析。

**我亲自完成的部分**

写明独立编码、修改、命令执行、数据采集、结果判断或复述理解。

**验证**

- 验收命令：
- 测试输入：
- 关键输出：
- 结果：

**我的收获与反思**

说明认识发生了什么变化，以及下次如何更早发现同类问题。

**简历/面试候选表达**

只有达到“已验证”后才形成成果表述；仍处于计划或阅读阶段时注明不得使用。

## 7. 维护规则

以下情况追加记录：

- 我主动发现设计、代码、数据或验收中的矛盾。
- 出现至少两个合理方案，需要作出取舍。
- 一个故障经过假设、证据、根因、修复和回归形成闭环。
- 我否决了 AI 的不准确假设、过度实现或未经验证结论。
- 我对某项机制的理解发生了可解释的变化。
- 得到了可以支撑简历或面试的实测数据。

以下情况不记录：

- 普通语法查询。
- 没有影响后续决定的临时聊天。
- 没有证据的猜测。
- 重复问题。
- 计划中存在但尚未开始的普通任务。

每次更新本文件后，同时检查 `00_项目上下文.md` 是否需要更新当前状态。每个里程碑结束时，从本文件提炼最有价值的 1～3 个案例，但不删除完整历史。

## 8. 阶段 12：Private 发布与完整历史取舍（2026-08-18）

**问题**

Git 历史中包含我的本地 Git 邮箱、Windows/Ubuntu 路径、虚拟机 IP，以及用于学习复盘的上下文和决策文档。如果未来公开仓库，可以导出一份干净发布历史；如果长期 Private，则可以保留完整过程。

**我的判断与决定**

我已经创建 `https://github.com/lktr-wq/edgegate`，并明确决定长期保持 Private，允许直接上传完整 Git 历史。我选择保留阶段演进、学习记录和 AI 贡献边界，而不是为了表面整洁重写历史。与此同时，我同意产品交付文件仍要去掉固定账号、虚拟机 IP 和绝对执行路径，使 README、脚本和发布包能够在别人的 Ubuntu 路径下复现。

**实施与证据**

- CMake、README 和发布说明统一为 `1.0.0`。
- 新增自动发布门禁，检查产品文件的版本、路径、常见凭据和 AI 标记。
- 初版路径正则误报 C++ 字符串 `:\\n`；依据命中行确认不是私有路径，再收紧规则并回归，而不是删除被误报的正常代码。
- Ubuntu 全新 Release、Debug、ASan/UBSan 三套构建均严格编译零警告、CTest `128/128`。

**我的收获与可用于面试的判断**

我能够区分“代码功能验收”“发布快照验收”和“仓库历史隐私”三个层次。Private 不代表可以把产品文档写死在个人机器上；保留完整历史也不等于对外夸大个人编码贡献。这个取舍体现了我对可复现性、证据边界和版本发布风险的判断。
