# EdgeGate 知识衔接

> 项目定位：用一个可安装、可配置、可监控的 Linux HTTP/1.1 反向代理，把已经学习的 C++ 与 Linux 应用层知识串成完整产品。
>
> 本文只负责建立知识地图和后续阅读顺序，不提前给出各里程碑的完整代码答案。

## 1. 当前起点：哪些已经具备，哪些还要补

下面的“已验证”指曾在 Ubuntu 上通过编译或运行结果验证，不等同于已经能独立设计大型网络服务。

| 层次 | 当前基础 | 在 EdgeGate 中的用途 | 仍需补充 |
|---|---|---|---|
| TCP Socket | 已完成 TCP 客户端/服务端、长消息、重复连接与进程回收验证 | 接收浏览器请求，连接上游服务，转发字节流 | 半包、粘包、短读写、非阻塞连接、连接状态机 |
| Unix Socket | 已完成本机通信和路径清理验证 | `edgegatectl` 与后台服务的本机管理通道 | 权限、协议边界、并发命令、异常断开 |
| 线程与同步 | 学过线程、互斥量、条件变量、有界队列 | 异步日志线程、后台任务以及线程退出 | 服务级生命周期、队列满策略、无丢失退出 |
| RAII 与所有权 | 学过资源封装、智能指针和移动语义 | 自动关闭 fd、管理连接、定时器和配置快照 | 事件回调期间的对象有效性、跨组件所有权 |
| RingBuffer/有界队列 | 学过固定容量设计及边界条件 | 连接输入/输出缓冲区、日志队列、背压水位 | 与 HTTP 消息边界结合、容量超限后的产品行为 |
| 信号 | 已在 TCP 服务中使用 `sigaction` 处理子进程回收 | SIGTERM 平滑退出、SIGHUP/管理命令触发重载 | 信号安全、事件循环集成、排空超时 |
| CMake/GDB/Sanitizer | 已学习多文件构建、调试和检查方法 | 多目标构建、单元测试、故障定位、内存与 UB 检查 | 安装规则、CTest、依赖管理、服务级诊断 |
| I/O 多路复用 | 接触过 `select` 思路，尚未完整实现 `epoll` 服务 | 一个线程管理大量连接 | `O_NONBLOCK`、LT `epoll`、`timerfd`、事件循环 |
| HTTP 与反向代理 | 本项目新增 | 整个产品的应用层协议和业务语义 | 增量解析、消息定界、路由、上游、错误映射 |
| 产品运行能力 | 本项目新增 | 配置、日志、指标、热重载、systemd、安装与文档 | 从“能跑”提升到“可运维、可验证、可交付” |

已有材料可继续作为基础参考：

- [Linux 应用层开发笔记](../嵌入式linux学习与成果/linux应用层开发.md)
- [嵌入式 Linux 学习路线](../嵌入式linux学习与成果/嵌入式Linux学习路线.md)
- [RAII 与资源管理](../c++学习与成果/c++进阶/02_RAII与资源管理.md)
- [条件变量与并发设计](../c++学习与成果/c++进阶/05_条件变量与并发设计.md)
- [容器失效与固定容量设计](../c++学习与成果/c++进阶/07_容器失效与固定容量设计.md)
- [编译、链接、构建与调试](../c++学习与成果/c++进阶/09_编译链接构建与调试.md)
- [传感器数据管线阶段项目](../c++学习与成果/c++进阶/10_阶段项目_传感器数据管线.md)

## 2. TCP 字节流如何变成 HTTP 消息

### 2.1 已学知识：TCP 只提供有序字节流

一次 `send()` 不对应接收端的一次 `recv()`。例如发送端写入一条 300 字节请求，接收端可能分成 80、120、100 字节收到，也可能与后续数据一起到达。因此：

- 不能把一次 `recv()` 当成一条完整 HTTP 请求。
- 必须根据协议规则判断消息是否完整。
- `recv()` 返回的实际字节数才是本次有效数据长度。
- `send()` 也可能只发送一部分，剩余数据必须保存并继续发送。

这正是之前长消息和真实字节长度练习在产品中的新用途。

### 2.2 新用途：HTTP/1.1 自己定义边界

一个普通请求可简化理解为：

```text
请求行\r\n
Header: value\r\n
...\r\n
\r\n
可选请求体
```

解析器要分阶段工作：请求行 → Header → 请求体 → 完成，而不是等待“一个完整字符串”。Header 结束由 `\r\n\r\n` 标记；若存在 `Content-Length`，还要继续累计指定长度的请求体。

响应的消息边界更复杂，v1 必须识别四种情况：

1. 按状态码或请求方法判断为无正文。
2. `Content-Length` 指定正文长度。
3. `Transfer-Encoding: chunked` 使用分块编码。
4. 没有上述长度信息时，以服务器关闭连接作为正文结束。

### 2.3 本项目补充内容

- 请求行和 Header 的语法、大小写规则与长度上限。
- HTTP/1.1 默认持久连接及 `Connection: close`。
- 增量解析器如何在任意分片下得到同一结果。
- 畸形消息和容量超限如何稳定映射到 `400`、`413`。
- 为什么“不支持”的协议能力也必须被明确识别并拒绝。

## 3. 从阻塞 Socket 到非阻塞事件循环

### 3.1 阻塞模型的价值与局限

阶段 2 会先实现单客户端、单上游的阻塞式最小链路。它用于验证反向代理的基本数据流和 HTTP 行为，不能作为最终架构。若一个线程在慢客户端或慢上游上阻塞，其他连接就无法及时处理；为每个连接创建线程又会引入线程数量、栈内存和调度成本。

### 3.2 `O_NONBLOCK` 改变的不是 API 名称，而是控制权

非阻塞 fd 上：

- `accept()`、`recv()`、`send()`、`connect()` 都可能不能立即完成。
- `EAGAIN`/`EWOULDBLOCK` 通常表示“现在没有更多进展”，而不是连接失败。
- 非阻塞 `connect()` 常返回 `EINPROGRESS`，之后通过可写事件和 `SO_ERROR` 判断结果。
- 还没写出的数据必须留在输出缓冲区，不能丢弃。

程序从“调用后等待结果”变为“记录当前状态，等事件到来后继续推进”。

### 3.3 `epoll` LT 模式

EdgeGate v1 使用水平触发（LT）：只要 fd 仍满足读写条件，后续 `epoll_wait()` 仍会报告事件。LT 更适合首次完整实现，便于观察和调试。

事件循环的基本职责：

1. 等待监听 fd、客户端 fd、上游 fd、定时器 fd 和管理 fd 的事件。
2. 将事件分派给对应连接对象。
3. 反复读到 `EAGAIN`，将收到的数据交给增量解析器或转发状态机。
4. 尽量写出输出缓冲区，写到 `EAGAIN` 后等待下一次可写事件。
5. 更新关注事件，处理超时、关闭和资源回收。

### 3.4 从 `select` 迁移时要建立的新认识

- `epoll` 不是 HTTP 解析器，也不替你处理半包。
- “fd 可读”不等于“一条请求完整”。
- “fd 可写”不等于“一次可以写完全部数据”。
- 事件可能同时包含错误、挂断和可读信息，处理顺序必须经过测试。
- fd 数字会被操作系统复用，不能把过期事件错误地关联到新连接。

## 4. RingBuffer、连接缓冲区与背压

### 4.1 已学知识的新用途

固定容量和边界检查在网络服务中不是算法练习，而是稳定性边界。每个连接至少需要：

- 输入缓冲区：保存已经读到、但尚未消费的数据。
- 输出缓冲区：保存尚未成功写出的数据。
- 读写位置或等价状态：区分有效区、已消费区和空闲区。

HTTP Header 需要连续查找分隔符，项目初期可以采用带读写下标的线性缓冲区并在必要时整理空间；是否使用真正的环形存储，要由接口清晰度和测试结果决定，不能为了使用 RingBuffer 而强行增加解析复杂度。

### 4.2 容量限制必须有产品语义

- 请求头或请求体超过配置上限：返回 `413` 或明确的 `400`，随后按策略关闭连接。
- 上游响应积压达到高水位：暂时停止读取上游或客户端对应方向。
- 缓冲区降到低水位：重新开启读取。
- 日志队列满：采用明确的阻塞、丢弃或降级策略，并计数告警。

这就是背压：下游处理不动时，上游不能无限制地继续生产数据。

## 5. RAII、所有权与连接生命周期

### 5.1 fd 也需要所有权

文件描述符是必须释放的资源。EdgeGate 将使用不可复制、可移动的 RAII 封装，保证对象析构时关闭 fd，并避免同一 fd 被关闭两次。

RAII 会用于：

- 监听 Socket、客户端 Socket、上游 Socket、Unix Socket。
- `epoll` fd、`timerfd`。
- 配置快照、日志资源和安装阶段的临时资源。

### 5.2 一个代理会话包含两端连接

一次代理通信通常同时持有：

```text
客户端连接 <-> 代理会话状态 <-> 上游连接
```

任意一端都可能先断开。需要明确：

- 谁拥有客户端与上游连接对象。
- 谁能发起关闭，关闭是否允许重复调用。
- `epoll` 删除关注后，对象何时真正销毁。
- 回调执行期间，对象是否可能因另一事件失效。
- Keep-Alive 完成一轮请求后，哪些状态复位、哪些连接保留。

项目优先采用清晰的单事件循环所有权，避免一开始引入复杂的跨线程共享连接对象。

### 5.3 配置快照的生命周期

热重载不会在原配置对象上逐字段修改。新配置先在临时对象中完整解析和校验，成功后原子替换当前只读快照；已有请求可以继续使用旧快照，新请求使用新快照。这样才能保证失败时旧配置仍然完整可用。

## 6. 线程、信号与 Unix Socket 的新组合

### 6.1 线程不是连接并发的主要手段

v1 的网络核心首先采用单个 Reactor 线程，连接并发由 `epoll` 管理。线程主要用于明确且可隔离的后台工作，例如异步日志写入。这样可以先把状态机做正确，再根据压测证据决定是否扩展多 Reactor。

日志线程会复用有界队列和条件变量知识，同时增加：

- 队列容量与满载策略。
- 服务停止时唤醒、排空并 `join()`。
- 写盘错误的可观测性。
- 不让日志故障破坏网络主循环。

### 6.2 安全退出

信号处理函数中不能执行复杂业务逻辑。常见做法是只设置原子标志，或通过可被事件循环监听的机制通知主循环，由主循环执行：

1. 停止接受新连接。
2. 进入 drain 状态。
3. 等待在途请求完成或达到排空超时。
4. 刷新日志并释放所有资源。

`SIGTERM` 用于服务管理器停止进程；管理命令 `drain`、`stop` 也复用同一套状态转换。

### 6.3 Unix Socket 管理通道

`edgegatectl` 不直接修改服务内存或配置。它连接本机 Unix Socket，发送受控命令，读取文本或 JSON 响应。需要补充：

- Socket 文件所在目录及文件权限。
- 命令如何分帧、如何限制长度。
- 非法命令、半条命令和客户端中途退出。
- 服务启动前清理“确实属于本服务”的陈旧 Socket 文件。
- 默认只允许本机可信用户访问管理接口。

## 7. HTTP 反向代理到底在做什么

正向代理代表客户端访问外部服务；反向代理代表服务端集群接受客户端请求。客户端通常只知道 EdgeGate，不直接选择内部上游。

```text
curl / 浏览器
      |
      v
EdgeGate 监听端口
      |
      +-- 解析请求、选择路由、调整 Header
      |
      v
健康上游池中的某个服务
      |
      v
EdgeGate 解析响应边界并转发给客户端
```

EdgeGate 不理解业务页面内容，但必须理解足够的 HTTP 语义，才能安全地判断边界、复用连接、生成错误响应和记录指标。

### 7.1 v1 支持范围

- 普通 HTTP 方法，以及带 `Content-Length` 的请求体。
- 无请求流水线的顺序 Keep-Alive。
- 无正文、`Content-Length`、chunked、关闭定界四类响应。
- Host 精确或通配匹配，加最长路径前缀匹配。
- 同一路由的健康上游轮询。
- 代理相关 Header 调整和明确的错误响应。

### 7.2 v1 明确不支持

- 请求流水线、chunked 请求体、`Expect: 100-continue`。
- CONNECT、WebSocket、HTTP/2、TLS。
- HTTP 缓存、通用 TCP 代理。

“不支持”不能表现为卡死或误转发：解析器或状态机必须识别边界，在有安全响应条件时返回明确错误并关闭连接。

## 8. 路由、上游池与健康检查

### 8.1 路由选择

每个请求先规范化并提取 Host 与路径，再按两层规则选择：

1. Host 精确匹配优先于符合条件的通配匹配。
2. 在选中的 Host 规则内，选择最长路径前缀。

规则冲突、重复定义和不存在的上游池应在配置加载阶段拒绝，而不是运行时猜测。

### 8.2 上游选择

一个路由指向一个上游池。池内只在健康节点中轮询。需要记录每个节点的：

- 地址和端口。
- 当前健康状态及最近一次检查结果。
- 连接、成功、失败、超时和重试计数。
- 最近错误信息与时间。

### 8.3 健康检查与安全重试

主动健康检查按间隔连接或请求指定探测端点，连续失败达到阈值后摘除，连续成功达到阈值后恢复。健康状态只是选择依据，不保证下一次请求一定成功。

重试必须保守：仅 GET/HEAD，且还没有向客户端返回任何响应字节时，才允许换一个健康上游重试。非幂等请求或已经部分响应的请求不能自动重放。

## 9. 超时、慢连接和故障映射

网络服务不能只靠“连接最终会结束”。每个阶段要定义可配置超时：

- 客户端读取请求头/请求体超时。
- 连接上游超时。
- 上游首字节及后续读取超时。
- 客户端写响应超时。
- Keep-Alive 空闲超时。
- drain 最大等待时间。

`timerfd` 让超时事件进入同一个 `epoll` 循环。具体可采用最小堆或其他定时器容器，但必须通过实际测试证明：过期准确、取消有效、对象销毁后不会触发悬空回调。

统一的故障语义：

| 状态码 | EdgeGate 中的典型含义 |
|---|---|
| `400 Bad Request` | 请求行/Header 畸形、协议边界非法 |
| `404 Not Found` | 没有匹配的 Host/路径路由 |
| `413 Payload Too Large` | 请求或缓冲容量超过配置上限 |
| `502 Bad Gateway` | 上游连接/协议/传输失败 |
| `503 Service Unavailable` | 路由存在但没有健康上游，或处于拒绝新请求状态 |
| `504 Gateway Timeout` | 连接或等待上游响应超时 |

错误码只是外部结果；内部还要记录结构化错误原因，便于区分“客户端问题、配置问题、上游问题、代理自身问题”。

## 10. YAML、CLI、JSON 和 Dashboard

这些并不是四套互相独立的功能，而是产品的配置、控制和观察界面。

### 10.1 YAML 配置

YAML 是适合人编辑的层级配置格式。`yaml-cpp` 只负责语法解析，EdgeGate 仍要自行做语义校验，例如：

- 端口是否合法、超时是否为正数。
- 缓冲区低水位是否小于高水位和最大容量。
- 路由引用的上游池是否存在。
- 地址、Host 模式和路径前缀是否合法。
- 名称是否重复、必填项是否缺失。

### 10.2 CLI 与 Unix Socket

CLI 是命令行客户端，不是后台服务的另一份实现：

```text
edgegatectl status
edgegatectl routes
edgegatectl upstreams
edgegatectl stats --json
edgegatectl reload
edgegatectl drain
edgegatectl stop
```

它把命令发送给正在运行的 `edgegate`，将响应格式化给用户，并用退出码区分成功与失败。

### 10.3 JSON 状态接口

JSON 是机器和网页容易消费的结构化数据。它承载连接数、请求数、状态码、延迟、路由、上游健康和错误摘要。`nlohmann/json` 用于可靠序列化，不让网页解析自由格式日志。

### 10.4 Web Dashboard

Dashboard 使用原生 HTML/CSS/JavaScript：

- HTML 定义页面结构。
- CSS 负责布局和视觉样式。
- JavaScript 定时请求 JSON 状态接口并更新页面。

v1 默认只读、只监听本机管理地址，不提供网页修改配置、重载或停止服务。后续若增加控制功能，必须先重新确定身份认证、CSRF、防误操作和暴露范围。

## 11. 日志、指标与“可监控”的区别

### 11.1 日志回答“发生了什么”

- 访问日志：时间、客户端、方法、Host、路径、路由、上游、状态码、字节数、总延迟。
- 错误日志：错误类别、系统错误码、连接阶段、关联请求和必要上下文。
- 管理日志：配置加载、重载、排空、停止和健康状态变化。

日志中不得记录密码、密钥、完整敏感 Header 或不受控请求体。

### 11.2 指标回答“整体是否健康”

- 当前连接数、累计请求数、在途请求数。
- 各状态码计数、代理错误计数。
- 请求延迟 P50/P95/P99。
- 各路由和上游的请求、失败、超时、健康状态。
- 缓冲区触发背压次数、重试次数。

计数器定义必须固定。例如“请求数”是在 Header 完成、成功选择路由，还是响应完成时增加，需要在实现和测试中保持一致。

## 12. CMake、测试、调试和产品交付

### 12.1 多目标构建

最终工程不仅有一个可执行文件，还包括：

- 核心库和 `edgegate` 服务。
- `edgegatectl` 管理程序。
- 单元测试与集成测试辅助程序。
- 安装规则、配置示例和静态 Dashboard 文件。

严格编译选项至少包括 `-Wall -Wextra -Wpedantic`。Debug 构建启用 ASan/UBSan；正式构建不能靠关闭警告掩盖问题。

### 12.2 五层验证

1. 单元测试：解析器、路由、配置、缓冲区、Header 改写、状态机小单元。
2. 集成测试：代理、三个上游、Keep-Alive、chunked 响应及故障码。
3. 故障测试：拒绝连接、崩溃、慢响应、无效重载、客户端提前断开。
4. 压测：并发、吞吐、延迟、CPU、RSS、fd 峰值。
5. 长稳测试：至少 12 小时观察连接、线程、内存和 fd 是否持续增长。

GDB 和 Sanitizer 用来定位根因；日志和指标用来复现运行时路径；压测结果用来验证改动，而不是只报告一个最高 QPS。

### 12.3 从程序到 Linux 产品

产品化阶段还要补：

- `cmake --install` 的文件布局。
- systemd 单元、运行用户、目录权限和停止语义。
- 示例配置、README、用户手册、架构说明、故障排查。
- 版本号、许可证、构建产物与敏感信息清理。
- 干净环境从源码构建、安装、启动和卸载验证。

## 13. 原项目的分阶段阅读路径

原则：先独立实现并留下测试结果，再带着具体问题局部对照。阅读目的是理解成熟设计的取舍，不是复制结构或代码。

| EdgeGate 阶段 | 阅读对象 | 只看什么 | 带着什么问题 |
|---|---|---|---|
| 文档与架构 | [Nginx 开发指南](https://nginx.org/en/docs/dev/development_guide.html) | 配置、事件、连接、请求处理的概念章节 | 成熟服务如何划分模块和生命周期？ |
| Reactor 完成后 | [muduo](https://github.com/chenshuo/muduo) | EventLoop、Channel、Buffer 的公开接口及相关小范围实现 | 事件关注、缓冲和对象所有权如何解耦？ |
| Reactor 完成后 | [libevent](https://github.com/libevent/libevent) | event_base、事件注册、定时器相关文档和局部实现 | I/O 与定时事件如何统一抽象？ |
| HTTP 状态机完成后 | [Nginx](https://github.com/nginx/nginx) | 与请求行、Header、上游状态相关的定向文件 | 成熟实现如何防御异常输入与处理状态迁移？ |
| 热重载/排空阶段 | Nginx 开发指南与 Nginx 源码 | 配置生命周期、进程/连接退出相关章节 | 如何保证旧请求不被新配置破坏？ |
| 压测与加固阶段 | 上述项目的测试、变更记录和安全公告 | 与本项目真实故障相匹配的部分 | 我们遗漏了哪些边界和失败模式？ |

阅读纪律：

- 每次先写下自己的现象、假设和最小复现。
- 只阅读能回答当前问题的章节或文件，不通读大型仓库。
- 记录链接、版本/提交和结论；区分“借鉴设计”与“复制代码”。
- 若引用或改编代码，先检查许可证并在项目中保留必要声明。
- 对照后用自己的接口重新表达，并用测试证明改动价值。

## 14. 开始第一个编程任务前的共同语言

文档审阅通过后，第一阶段环境任务开始前，应能用自己的话解释：

- TCP 为什么没有消息边界，HTTP 如何建立消息边界。
- 阻塞与非阻塞 `recv()`/`send()` 的结果差异。
- `epoll` 只报告就绪，为什么仍需要缓冲区和状态机。
- 一个代理会话为什么同时面对客户端与上游两个失败源。
- 什么是背压，为什么不能无限扩容缓冲区。
- RAII 如何避免 fd 泄漏，为什么 fd 复用会带来风险。
- YAML、Unix Socket CLI、JSON 状态接口和 Dashboard 各自解决什么问题。
- 为什么热重载必须“先完整校验，成功后切换”。
- 为什么压测、故障测试和长稳测试都不能被单元测试替代。

如果其中某一项尚不清楚，应在对应里程碑开始时补足，并通过小实验验证；不要求在项目开始前一次性学完全部内容。

## 15. 术语速查

| 术语 | 本项目中的含义 |
|---|---|
| Reactor | 等待事件并把事件分派给连接/定时器处理逻辑的核心循环 |
| LT | 水平触发；条件仍满足时会继续报告事件 |
| 增量解析 | 数据每到一部分就推进解析状态，不依赖一次收齐 |
| 上游 | EdgeGate 将请求转发到的真实后端服务 |
| 路由 | 根据 Host 和路径把请求映射到上游池的规则 |
| 背压 | 消费方变慢时限制生产方继续输入，避免无界积压 |
| 高/低水位 | 暂停读取和恢复读取的两个缓冲区阈值 |
| Keep-Alive | 同一 TCP 连接按顺序承载多轮 HTTP 请求/响应 |
| drain/排空 | 停止接受新工作，等待在途工作结束后退出 |
| 热重载 | 服务不停止地加载并切换到一份已验证的新配置 |
| P50/P95/P99 | 50%、95%、99% 请求完成时间不超过的延迟值 |
| 长稳测试 | 长时间运行并观察资源是否持续增长或状态是否退化 |

