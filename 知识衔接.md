# EdgeGate 知识衔接讲义

> 项目定位：用一个可安装、可配置、可监控的 Linux HTTP/1.1 反向代理，把已经学习的 C++ 与 Linux 应用层知识串成完整产品。
>
> 本文不是术语清单，也不会降低项目的技术难度。它负责在新术语出现前先解释术语，再用具体数据说明程序如何处理，最后说明它在 EdgeGate 中的用途。

## 0. 阅读规则

### 0.1 怎样判断一项知识是否“已经学过”

本讲义采用严格标准：

- 在现有学习文件中有明确笔记，并且做过编译、运行或联调验证的内容，才按“已学过”处理。
- 只在路线表或笔记中出现过名字，不算真正学过。
- 没有出现在学习文件里的内容，一律从零解释。
- 从零解释不等于降低难度。HTTP 消息定界、非阻塞 I/O、`epoll`、状态机、背压等核心内容都会完整学习。

根据现有记录，目前可以作为起点的内容包括：

- TCP、UDP、Unix Socket 的基本编程和实际联调。
- `socket()`、`bind()`、`listen()`、`accept()`、`connect()`、`send()`、`recv()` 等基本接口。
- TCP 是字节流，一次发送不保证对应一次接收。
- 线程、互斥量、条件变量、有界队列的基本用法。
- RAII、智能指针、移动语义和固定容量容器的基本思想。
- CMake、GDB、严格编译选项和 Sanitizer 的基本概念。

下面这些内容即使以前见过名字，也按新知识讲。这里先给出一句话含义，后文再完整展开：

- HTTP/1.1：Web 客户端和服务器交换请求、响应的一套通信规则。
- 反向代理：在客户端和真实后端之间接收并转发 HTTP 请求的服务。
- 路由、上游和健康检查：决定请求交给哪台后端，并判断后端是否可用。
- `O_NONBLOCK`：让 Socket 当前不能操作时立即返回，而不是让线程一直等待。
- `epoll`：Linux 用来等待多个 fd 就绪事件的接口。
- Reactor：统一等待事件并把事件分派给对应对象的程序结构。
- `timerfd`：把定时器到期也表示为 fd 可读事件的 Linux 接口。
- 增量解析：数据分批到达时保存进度，等新数据到来后继续解析。
- 连接状态机：用明确状态和转换描述一条连接当前处理到哪里。
- 背压和高低水位：数据积压时暂停输入，下降到安全容量后再恢复。
- 热重载和排空：不停机切换配置，以及退出前等待在途请求完成。
- 指标和 JSON 状态接口：用固定字段和数值描述服务整体运行状态。
- Dashboard：浏览器中只读展示运行状态的管理页面。
- systemd 服务：交给 Ubuntu 服务管理器启动、停止和监督的正式进程。

### 0.2 每一章学到什么程度

学习一项新技术时分为三层：

1. **能解释**：知道它解决什么问题，能用自己的话说明。
2. **能跟踪**：给出一组实际数据，能说出程序每一步处于什么状态。
3. **能实现和验证**：能够编写代码，并用正常、分片、超限和故障输入验证。

阅读本讲义时先达到前两层。第三层在对应项目阶段通过编程完成，不要求现在一次性写出全部代码。

### 0.3 统一阅读方法

遇到每个新术语，依次回答：

1. 它是什么？
2. 没有它会出现什么问题？
3. 程序输入了什么数据？
4. 程序需要保存什么状态？
5. 程序最后输出什么结果？
6. EdgeGate 在哪里使用它？

不要孤立背 API。项目真正关心的是数据、状态、失败路径和验证结果。

## 1. 从已经会的 TCP 开始

### 1.1 先区分连接和消息

TCP 连接可以理解为两台程序之间建立的一条双向字节通道。连接建立以后，两端都可以向通道中写入字节，也可以从通道中读取字节。

TCP 保证的是：

- 正常情况下，字节按发送顺序到达。
- 重复、丢失和乱序等网络问题由操作系统中的 TCP 实现处理。
- 程序看到的是连续字节流。

TCP 不保证的是：

- 一次 `send()` 对应一次 `recv()`。
- 一次 `recv()` 正好得到一条完整业务消息。
- 一次 `send()` 一定把传入的所有字节都发送完成。

假设发送端连续发送两条业务数据：

~~~text
第一条：HELLO
第二条：WORLD
~~~

接收端可能得到：

~~~text
第一次 recv：HEL
第二次 recv：LOWORLD
~~~

也可能一次得到：

~~~text
HELLOWORLD
~~~

TCP 只知道这里有 10 个有序字节，不知道 `HELLO` 和 `WORLD` 是两条消息。怎样划分消息，必须由应用层协议规定。

### 1.2 什么是应用层协议

“协议”就是通信双方共同遵守的数据格式和处理规则。

例如可以自己规定：

~~~text
前 4 个字节表示正文长度
后面是对应长度的正文
~~~

也可以规定每条消息以换行符结束。只要发送方和接收方使用同一套规则，接收方就能从 TCP 字节流中恢复出消息。

HTTP 就是一套应用层协议。它规定了浏览器、服务器和代理之间怎样表示请求、响应、Header、正文和连接行为。

### 1.3 短读和短写

**短读**表示本次 `recv()` 得到的数据少于当前业务希望得到的数据。它不一定是错误，可能只是剩余数据还在网络中。

**短写**表示 `send()` 的返回值小于准备发送的字节数。例如准备发送 1000 字节，返回值是 400，说明本次只成功交给内核 400 字节，剩下 600 字节以后还要继续发送。

因此网络程序必须遵守：

- 只处理 `recv()` 返回值所表示的有效字节。
- 不能因为数据暂时不完整就当成协议错误。
- 没写完的数据不能丢失，必须记住已发送位置。
- `recv()` 返回 0 表示对端已经有序关闭发送方向，不表示“暂时没有数据”。

这些规则后面会直接演变成 HTTP 输入缓冲区、输出缓冲区和增量解析器。

## 2. TCP 字节流如何变成 HTTP 消息

### 2.1 先认识客户端、服务器、请求和响应

主动发起访问的一方叫**客户端**，例如 `curl`、浏览器或另一个程序。

接受访问并提供结果的一方叫**服务器**，例如一个返回网页或 JSON 数据的 Web 服务。

客户端发给服务器的一条 HTTP 消息叫**请求**。服务器处理请求后发回的 HTTP 消息叫**响应**。

一次最普通的交互是：

~~~text
客户端 --HTTP请求--> 服务器
客户端 <--HTTP响应-- 服务器
~~~

EdgeGate 位于中间，所以它既扮演服务器，也扮演客户端：

- 面对外部客户端时，EdgeGate 是服务器。
- EdgeGate 连接真实后端时，EdgeGate 又是客户端。

### 2.2 HTTP 请求由哪些部分组成

一个 HTTP/1.1 请求通常由三部分组成：

1. 请求行。
2. Header，也叫请求头。
3. 可选的 Body，也叫请求体或正文。

下面是一条完整的原始请求：

~~~http
POST /login HTTP/1.1\r\n
Host: example.com\r\n
Content-Type: application/json\r\n
Content-Length: 32\r\n
User-Agent: curl/8.0\r\n
\r\n
{"username":"zy","password":"x"}
~~~

这里为了看清协议边界，把回车换行显示成了 `\r\n`。真实网络中它们是两个控制字符，不是四个普通字符。

#### 请求行

第一行：

~~~text
POST /login HTTP/1.1
~~~

由三个字段组成：

- `POST`：方法，表示客户端想执行哪类操作。
- `/login`：请求目标，这里是服务器中的路径。
- `HTTP/1.1`：使用的 HTTP 版本。

三个字段之间由一个空格分隔，请求行最后由 `\r\n` 结束。

常见方法包括：

- `GET`：获取资源。
- `HEAD`：只获取响应头，不接收响应体。
- `POST`：提交数据。
- `PUT`、`DELETE` 等方法也可以出现，但是否允许由具体服务决定。

EdgeGate v1 不靠方法猜测请求体长度，而是按照 HTTP 的消息定界规则处理。

#### Header

Header 是请求的描述信息。每一行基本形式是：

~~~text
字段名: 字段值\r\n
~~~

上面例子中的字段含义：

- `Host: example.com`：客户端希望访问的目标主机。EdgeGate 会使用它选择路由。
- `Content-Type: application/json`：正文的媒体类型，这里表示正文是 JSON。JSON 是一种使用键和值组织数据的文本格式，示例正文中的 `username` 和 `password` 就是两个字段。
- `Content-Length: 33`：正文占 33 个字节，不是 33 个字符。
- `User-Agent: curl/8.0`：客户端声明自己的软件身份。
- `Cookie`：如果存在，通常携带服务器之前交给客户端的会话数据。

HTTP Header 字段名不区分大小写，所以 `Host`、`host` 和 `HOST` 在协议语义上是同一个字段。实现时不能只接受一种大小写。

Header 并不是遇到一行就结束。连续读取 Header 行，直到遇到一个空行。这个空行在字节流中表现为：

~~~text
\r\n\r\n
~~~

前一个 `\r\n` 结束最后一个 Header，后一个 `\r\n` 表示出现空行。

#### Body

Body 是可选正文，可以放表单、JSON、图片或其他字节数据。

“可选”表示有些请求没有 Body，例如多数 GET 请求；有些请求包含 Body，例如提交 JSON 的 POST 请求。

Body 可以是二进制数据，不能用 `strlen()` 判断长度。项目必须始终使用实际字节数。

示例中的账号密码只是用来说明正文结构。真实系统不能在未加密的公网 HTTP 中传输密码；EdgeGate v1 不实现 TLS，因此只用于受控学习环境。

### 2.3 HTTP 请求的结束位置怎么判断

解析器不能把一次 `recv()` 当成完整请求。它要分阶段工作：

~~~text
读取请求行
    ↓
逐行读取 Header
    ↓
遇到 \r\n\r\n，Header 完成
    ↓
判断是否存在 Body
    ↓
如果有 Content-Length，读够对应字节
    ↓
一条请求完成
~~~

例如 Header 中写着：

~~~text
Content-Length: 10
~~~

那么 `\r\n\r\n` 之后必须再读 10 个字节。即使当前 `recv()` 只读到 4 个字节，也不能认为请求错误，而是保存这 4 个字节，继续等待剩余 6 个字节。

EdgeGate v1 在请求方向只支持使用 `Content-Length` 定界的请求体。其他请求体定界方式会在后面先解释，再列入 v1 的明确边界。

### 2.4 什么是增量解析

**解析**是把原始字节转换成有含义的字段，例如把：

~~~text
GET /api HTTP/1.1\r\n
~~~

转换为：

~~~text
方法 = GET
路径 = /api
版本 = HTTP/1.1
~~~

**增量解析**表示数据每到一部分，解析器就尽可能向前处理，并保存当前进度。下次数据到来后，从上次位置继续，而不是要求一次获得完整请求。

假设请求被拆成三次到达：

~~~text
第1次：GET /api HT
第2次：TP/1.1\r\nHost: exa
第3次：mple.com\r\n\r\n
~~~

解析器可能经历：

~~~text
第1次后：请求行不完整，需要更多数据
第2次后：请求行完成，Host 行不完整
第3次后：Host 完成，发现空行，请求完成
~~~

解析器至少需要返回三类结果：

- 已完成一条消息。
- 当前数据合法，但还需要更多数据。
- 已确定违反协议或超过限制。

这三类结果不能混在一起，否则正常的网络分片很容易被误判成非法请求。

### 2.5 HTTP 响应由哪些部分组成

HTTP 响应通常由：

1. 状态行。
2. 响应 Header。
3. 可选响应 Body。

例如：

~~~http
HTTP/1.1 200 OK\r\n
Content-Type: text/plain\r\n
Content-Length: 5\r\n
\r\n
hello
~~~

状态行中的字段是：

- `HTTP/1.1`：协议版本。
- `200`：数字状态码。
- `OK`：给人阅读的原因短语。

常见状态码类别：

- 1xx：临时信息。
- 2xx：请求成功。
- 3xx：重定向或缓存相关结果。
- 4xx：客户端请求存在问题。
- 5xx：服务器或代理处理失败。

### 2.6 为什么响应体有四种结束方式

客户端或代理必须知道一条响应在哪里结束，才能继续处理下一条消息。EdgeGate v1 要识别四种情况。

#### 情况一：协议规定没有响应体

以下响应没有消息体：

- 对 HEAD 请求的响应。
- 1xx 状态码。
- `204 No Content`。
- `304 Not Modified`。

此时 Header 结束后，响应就结束。即使 Header 中出现容易引起歧义的信息，也必须按照协议规则判断。

#### 情况二：Content-Length

~~~http
Content-Length: 5\r\n
\r\n
hello
~~~

Header 结束后再读 5 个字节，响应完成。

如果上游只发送 3 个字节就断开，说明响应不完整，EdgeGate 要把它记录为上游传输失败，而不是正常完成。

#### 情况三：Transfer-Encoding: chunked

`chunk` 的意思是“数据块”。chunked 编码不知道正文总长度，而是把正文拆成多个块，每块前面写本块长度。

例如正文 `Wikipedia` 可以表示为：

~~~http
4\r\n
Wiki\r\n
5\r\n
pedia\r\n
0\r\n
\r\n
~~~

说明：

- `4` 和 `5` 是十六进制块长度。
- 每个块的数据后面有 `\r\n`。
- 长度为 0 的块表示正文块结束。
- 0 块之后还可能有 Trailer Header。Trailer 是放在正文块结束之后的可选附加 Header，最后仍由空行结束整个 chunked 消息。

因此不能只在字节流中搜索字符 `0`。解析器必须按照“块长度行 → 块数据 → 换行 → 下一块”的状态工作。

#### 情况四：关闭连接定界

某些响应没有 `Content-Length`，也没有 chunked 编码。此时响应体一直延续到上游关闭连接。

这种响应不能继续复用该上游连接，因为“连接关闭”本身就是消息结束标记。

关闭定界只能用于响应，不能把客户端请求的断开当作正常请求体定界方式。

### 2.7 Keep-Alive 是什么

建立 TCP 连接需要时间。HTTP/1.1 默认允许在同一个 TCP 连接上按顺序发送多轮请求和响应，这叫**持久连接**，常称 Keep-Alive。

~~~text
建立一次 TCP 连接
请求1 -> 响应1
请求2 -> 响应2
请求3 -> 响应3
最后关闭
~~~

正因为连接不会在每条消息后自动关闭，所以解析器必须准确判断一条消息的结束位置。

`Connection: close` 表示发送方希望当前消息完成后关闭连接。

**请求流水线**表示客户端在响应 1 还没回来时，就连续发送请求 2、请求 3。EdgeGate v1 不支持流水线，只支持“上一轮响应完成后再处理下一轮请求”的顺序 Keep-Alive。

### 2.8 本章在 EdgeGate 中的用途

EdgeGate 需要两个增量解析方向：

- 从客户端字节流中识别完整请求。
- 从上游字节流中识别完整响应。

本章最终会落实为：

- 请求行和状态行解析。
- Header 解析和大小写处理。
- `Content-Length`、chunked 和关闭定界。
- 请求/响应大小限制。
- 正常、需要更多数据和错误三类结果。
- 任意 TCP 分片下得到相同解析结果的测试。

### 2.9 EdgeGate v1 的 HTTP 边界

下面的范围限制不是为了回避错误，而是为了让第一版在有限范围内做到行为明确、测试完整。

v1 支持：

- 普通 HTTP/1.1 请求方法。
- 无请求体和由 `Content-Length` 定界的请求体。
- 顺序 Keep-Alive。
- 无正文、`Content-Length`、chunked、关闭连接四类响应。

v1 明确不支持：

- 请求流水线。
- chunked 请求体。
- `Expect: 100-continue`：客户端先询问服务器是否愿意接收正文，再决定是否继续发送。
- CONNECT 隧道：客户端要求代理建立一条只转发字节的网络隧道。
- WebSocket 升级：把 HTTP 连接切换为可以长期双向推送消息的 WebSocket 协议。
- HTTP/2：使用与 HTTP/1.1 不同的二进制分帧和多路复用机制。
- TLS：为网络数据提供加密、完整性保护和对端身份验证的安全协议。
- HTTP 缓存：由代理保存响应，并在以后直接复用保存结果。
- 通用 TCP 转发：不理解 HTTP 消息，只在两条 TCP 连接之间转发原始字节。

解析器和代理状态机要主动识别这些边界。遇到不支持的形式时，要在仍能安全回复的情况下返回明确错误并关闭连接，不能误转发、卡死或把后续字节解释成另一条请求。

## 3. 从阻塞 Socket 到非阻塞 Socket

### 3.1 先确定：你以前写的是阻塞 Socket

你以前创建 TCP Socket 的代码类似：

~~~c
int socket_fd = socket(AF_INET, SOCK_STREAM, 0);
~~~

`SOCK_STREAM` 表示创建 TCP 使用的字节流 Socket。这里没有加入非阻塞标志，后面也没有调用 `fcntl()` 修改工作模式，因此它默认是**阻塞 Socket**。

你以前的服务端主流程可以简化为：

~~~c
int client_fd = accept(listen_fd, NULL, NULL);

char buffer[1024];
ssize_t count = recv(client_fd, buffer, sizeof(buffer), 0);

send(client_fd, buffer, count, 0);
~~~

这几个函数都可能让当前进程或线程停下来等待：

- 没有新客户端时，`accept()` 等待。
- 客户端连上但没有发送数据时，`recv()` 等待。
- 对端长期不读取、我方内核发送缓冲区又满时，`send()` 等待。

这就是你已经写过并验证过的阻塞 Socket。

### 3.2 用一个运行过程观察“阻塞”

假设一个单进程服务端依次处理客户端：

~~~c
while (1) {
    int client_fd = accept(listen_fd, NULL, NULL);
    recv(client_fd, buffer, sizeof(buffer), 0);
    /* 处理并回复这个客户端 */
    close(client_fd);
}
~~~

现在发生下面的事情：

~~~text
10:00:00  客户端 A 建立连接，但故意不发送数据
10:00:01  服务端进入 recv(A)，开始等待
10:00:02  客户端 B 建立连接并立即发送完整请求
10:00:03  服务端仍停在 recv(A)
~~~

虽然客户端 B 的数据已经到达内核，程序仍然执行不到下一次 `accept()`，因为当前执行流程被客户端 A 的 `recv()` 卡住了。

你以前使用过 fork 服务器。fork 后，每个子进程可以阻塞处理自己的客户端，所以客户端 A 不会卡住处理客户端 B 的另一个子进程。但它仍属于：

~~~text
一个子进程 + 一个阻塞连接
~~~

它不是非阻塞事件循环。

### 3.3 旧知识：fd；新增理解：fd 还带有工作状态

你以前已经接触过 fd。`socket()` 返回的 `socket_fd`、`accept()` 返回的 `client_fd` 都是文件描述符。

文件描述符，简称 fd，是当前进程用来引用一个已打开内核资源的非负整数。

例如：

~~~text
listen_fd  = 3   -> 监听 Socket
client_fd  = 5   -> 客户端连接
file_fd    = 7   -> 普通文件
~~~

下面是本章新增的理解：

> fd 不仅代表“哪个资源”，它还带有一些可以查询或修改的工作状态，其中就包括是否使用非阻塞模式。

`fcntl()` 是 Linux 用来操作 fd 属性的系统调用，声明在 `<fcntl.h>` 中。把一个已有 Socket 改成非阻塞模式，基本步骤是：

~~~c
int flags = fcntl(fd, F_GETFL, 0);
fcntl(fd, F_SETFL, flags | O_NONBLOCK);
~~~

逐项解释：

- `F_GETFL`：读取 fd 当前的状态标志。
- `flags`：保存原来的状态，不能随意丢掉。
- `O_NONBLOCK`：非阻塞标志。
- `flags | O_NONBLOCK`：在保留原标志的基础上加入非阻塞标志。
- `F_SETFL`：把新的状态标志写回 fd。

实际项目必须检查两次 `fcntl()` 的返回值。上面的代码只展示“旧状态怎样变成新状态”，不是完整错误处理答案。

还有一个容易犯错的地方：

- 把监听 fd 设置成非阻塞，不代表 `accept()` 返回的客户端 fd 在 Linux 上自动继承非阻塞状态。所以新客户端 fd 也要单独设置，或者以后使用能直接指定 `SOCK_NONBLOCK` 的 `accept4()`。

#### fd 数字会复用

假设：

~~~text
客户端 A 的 fd = 5
关闭客户端 A
新客户端 B 到来，操作系统再次分配 fd = 5
~~~

数字相同不代表还是同一条连接。这叫 fd 复用。EdgeGate 后面不能拿一个已经过期的 fd 数字继续访问旧连接对象。

### 3.4 新知识：非阻塞 recv() 不再替程序等待

给 Socket fd 加入 `O_NONBLOCK` 后，`recv()` 的函数名和参数没有改变，改变的是“当前没数据时怎么办”。

#### 以前的阻塞行为

~~~c
ssize_t count = recv(client_fd, buffer, sizeof(buffer), 0);
/* 没数据时，程序停在上面，下面暂时不执行。 */
handle_other_connection();
~~~

运行过程：

~~~text
客户端没数据
    ↓
recv 等待
    ↓
当前线程不能处理其他连接
~~~

#### 改成非阻塞后的行为

~~~c
ssize_t count = recv(client_fd, buffer, sizeof(buffer), 0);

if (count > 0) {
    /* 本次确实读到了 count 个字节。 */
} else if (count == 0) {
    /* 对端已经有序关闭连接。 */
} else {
    /* 返回 -1，需要检查 errno 才知道原因。 */
}
~~~

`errno` 是系统调用失败后提供的具体错误编号。只有函数已经表示失败时才检查它，不能在成功后读取旧的 `errno` 判断本次结果。

非阻塞 `recv()` 返回 -1 后，常见情况是：

~~~c
if (errno == EAGAIN || errno == EWOULDBLOCK) {
    /* 当前已经没有更多数据，不是连接故障。 */
} else {
    /* 其他真实错误。 */
}
~~~

`EAGAIN` 可以直白理解为：

> 现在再读也读不到数据，先做别的事情，以后有数据时再回来。

`EWOULDBLOCK` 表示“这个操作如果继续就需要等待”。在 Socket 非阻塞读写中，它通常与 `EAGAIN` 表示同一种暂时状态，所以代码同时判断两者。

完整运行时间线：

~~~text
第一次 recv：返回 120
    -> 得到 120 字节，放进输入缓冲区

第二次 recv：返回 80
    -> 又得到 80 字节，继续追加

第三次 recv：返回 -1，errno = EAGAIN
    -> 当前能读的数据已经取完
    -> 保存已有 200 字节和解析状态
    -> 去处理其他连接

以后这个 fd 再次可读
    -> 继续 recv 和解析
~~~

因此 `EAGAIN` 不是“HTTP 请求错误”，也不是“客户端断开”，更不能直接关闭连接。

如果系统调用因为信号到来而被中断，还可能得到 `EINTR`。它的含义是“这次调用被信号打断”，后续应根据当前操作和退出状态决定重试或停止。它与“当前没有数据”的 `EAGAIN` 不是同一个原因。

### 3.5 新知识：非阻塞 accept()

#### 以前的阻塞 accept

~~~c
int client_fd = accept(listen_fd, NULL, NULL);
printf("accepted\n");
~~~

如果没有新连接，程序停在 `accept()`，`printf()` 不执行。这正是简单服务端希望的行为：什么都没有时就等待。

#### 非阻塞 accept

监听 fd 设置为非阻塞后：

~~~c
int client_fd = accept(listen_fd, NULL, NULL);

if (client_fd >= 0) {
    /* 成功取出一个已经建立的客户端连接。 */
} else if (errno == EAGAIN || errno == EWOULDBLOCK) {
    /* 当前等待队列里没有更多连接。 */
} else {
    /* accept 真实失败。 */
}
~~~

一次收到“监听 fd 可读”的通知时，等待队列中可能已经有三个客户端：

~~~text
第一次 accept -> 客户端 A
第二次 accept -> 客户端 B
第三次 accept -> 客户端 C
第四次 accept -> EAGAIN，队列暂时取空
~~~

所以事件驱动程序通常循环 `accept()`，直到得到 `EAGAIN`，然后回去处理其他事件。

这里的旧知识是 `accept()` 从等待队列取出一个连接；新知识是：

- 没有连接时不再停住，而是返回 `EAGAIN`。
- 一次事件可能需要连续取出多个连接。
- 每个新客户端 fd 还要设置为非阻塞。

### 3.6 旧知识 send_all() 与新知识输出缓冲区

你以前的 `send_all()` 核心逻辑是：

~~~c
while (total_sent < length) {
    ssize_t sent = send(fd,
                        buffer + total_sent,
                        length - total_sent,
                        0);

    if (sent == -1) {
        if (errno == EINTR) {
            continue;
        }
        return -1;
    }

    if (sent == 0) {
        return -1;
    }

    total_sent += (size_t)sent;
}
~~~

它已经正确认识到一次 `send()` 可能没有完成全部字节，这是需要保留的旧知识。

但在阻塞 Socket 上，如果内核发送缓冲区暂时装不下更多数据，`send()` 会等待。循环最终可能发完，但当前专门用于send的线程在这段时间不能服务其他连接。

#### 改成非阻塞后会多出一种结果

假设要发送 5000 字节：

~~~text
第一次 send -> 返回 1200
还剩 3800 字节

第二次 send -> 返回 -1，errno = EAGAIN
当前一个字节也写不进去
~~~

这时不能：

- 把 `EAGAIN` 当成连接失败。
- 丢掉剩余 3800 字节。
- 原地不停调用 `send()`，否则会空转消耗 CPU。

需要把剩余数据和发送位置保存在连接对象中：

~~~text
输出数据总长 = 5000
已经发送     = 1200
下次发送位置 = 1200
待发送       = 3800
~~~

这块保存“还没写完数据”的内存叫**输出缓冲区**。

以后 `epoll` 告诉程序这个 fd 再次可写时，从位置 1200 继续发送。

所以二者的区别是：

| 以前的 `send_all()` | EdgeGate 的非阻塞发送 |
|---|---|
| 当前调用流程里循环直到发完或失败 | 当前能发多少就发多少 |
| 缓冲区满时可能阻塞等待 | 缓冲区满时得到 `EAGAIN` |
| 剩余位置主要保存在局部循环变量 | 剩余数据和位置保存在连接对象中 |
| 发完之前当前线程难以处理其他连接 | 暂停这个连接，先处理其他连接 |

### 3.7 新知识：非阻塞 connect()

EdgeGate 接收客户端请求后，要主动连接上游服务器。你以前客户端使用的阻塞 `connect()` 类似：

~~~c
int result = connect(fd,
                     (struct sockaddr *)&server_addr,
                     sizeof(server_addr));
~~~

#### 阻塞 connect

~~~text
调用 connect
    ↓
等待连接成功、被拒绝或超时
    ↓
函数返回
~~~

等待期间当前线程不能处理其他客户端。

#### 非阻塞 connect

先不看代码，假设 EdgeGate 正在连接上游 A。

非阻塞连接的过程是：

~~~text
EdgeGate：请帮我连接上游 A。

Linux 内核：连接需要一些时间，我已经开始处理了。
           你先去处理其他客户端。

connect()：返回“正在连接”。

过一会儿……

Linux 内核：刚才那次连接已经有结果了，你来查看结果。

EdgeGate：查询结果。
          可能是连接成功，也可能是连接被拒绝或超时。
~~~

这种“先启动操作，程序继续做其他事情，以后再取得结果”的方式叫**异步完成**。

这里最容易误解的是：

> “连接过程有结果了”不等于“连接成功了”。

可以类比医院通知“检查报告已经出来了”。这句话只代表报告可以查看，不代表报告结果一定正常。必须打开报告才能知道最终结果。

#### connect() 第一次返回什么

Socket 设置非阻塞后，`connect()` 可能得到三类结果：

~~~c
int result = connect(fd,
                     (struct sockaddr *)&server_addr,
                     sizeof(server_addr));

if (result == 0) {
    /* 连接立即成功。 */
} else if (errno == EINPROGRESS) {
    /* 连接已经开始，但最终结果还没产生。 */
} else {
    /* 这次已经能确定连接失败。 */
}
~~~

`EINPROGRESS` 可以拆成：

~~~text
IN PROGRESS = 正在进行中
~~~

所以它的含义是：

~~~text
连接任务已经交给 Linux 内核
但现在还不知道最终成功还是失败
~~~

它不是连接成功，也不是连接失败，更不能在收到它后立即发送 HTTP 请求。

EdgeGate 收到 `EINPROGRESS` 后要保存：

~~~text
这个上游连接的状态 = 正在连接
连接开始/截止时间
它属于哪个客户端会话
~~~

#### 稍后怎么知道“结果出来了”

第四章会学习 `epoll`。它是 Linux 用来通知“哪个 Socket 现在可以继续处理”的工具。

现在暂时只需要知道：

~~~text
连接还在进行
    ↓
程序先处理其他连接
    ↓
Linux 稍后通知：这个 Socket 的连接过程有结果了
    ↓
程序查询最终结果
~~~

Linux 把非阻塞连接的完成通知表现为这个 Socket **可写**。

这里的“可写”不是在保证连接成功。它只是在通知：

> 连接尝试已经结束，现在可以检查成功还是失败。

为什么失败也要通知？因为如果上游拒绝连接，程序同样不能永远停留在“正在连接”状态，也需要被唤醒并处理失败。

#### 怎样查询成功还是失败

Socket 是 Linux 内核管理的对象。连接过程失败时，内核会把错误原因暂存在这个 Socket 中。

`getsockopt()` 的意思是“读取 Socket 的一个选项或状态”。`SO_ERROR` 表示要读取这个 Socket 保存的错误结果。

~~~c
int socket_error = 0;
socklen_t length = sizeof(socket_error);

int result = getsockopt(fd,
                        SOL_SOCKET,
                        SO_ERROR,
                        &socket_error,
                        &length);
~~~

这五个参数逐项解释：

| 参数 | 含义 |
|---|---|
| `fd` | 要查询哪一个 Socket |
| `SOL_SOCKET` | 查询的是 Socket 自身这一层的通用状态 |
| `SO_ERROR` | 本次要读取“保存的错误结果” |
| `&socket_error` | 让内核把查询结果写入这个变量 |
| `&length` | 告诉内核结果变量有多大，调用后也可得到实际长度 |

首先检查 `getsockopt()` 自己是否成功：

~~~text
result == -1
    -> 查询动作本身失败，需要处理系统调用错误
~~~

如果查询成功，再看 `socket_error`：

- `socket_error == 0`：连接成功。
- `socket_error != 0`：连接失败，这个值说明失败原因。

完整判断过程是：

~~~text
connect() 返回 0
    -> 立即连接成功

connect() 返回 -1，errno = EINPROGRESS
    -> 状态记为“正在连接”
    -> 先去处理其他连接
    -> 等待 Linux 通知连接过程有结果
    -> 使用 getsockopt(..., SO_ERROR, ...) 查询
       -> socket_error == 0：成功
       -> socket_error != 0：失败

connect() 返回 -1，errno 不是 EINPROGRESS
    -> 已经确定失败
~~~

#### 当前阶段需要记住什么

现在不要求记住 `getsockopt()` 的五个参数，也不要求会写 `epoll`。当前只需记住三点：

1. 阻塞 `connect()` 会等到成功或失败后才返回。
2. 非阻塞 `connect()` 的 `EINPROGRESS` 表示还在连接，最终结果未知。
3. 稍后收到的通知只表示结果出来了，还要查询才能区分成功和失败。

第四章学习 `epoll` 后，再把“等待通知”落实成具体代码。

### 3.8 select 和非阻塞解决的不是同一个问题

你以前接触过 `select`。它解决的是：

> 多个 fd 中，哪些 fd 当前可能读、写或出现异常？

`O_NONBLOCK` 解决的是：

> 如果当前操作仍然不能完成，调用函数是否允许把线程停住？

因此它们不是替代关系：

~~~text
select / epoll
    -> 帮你选择现在应该尝试哪个 fd

O_NONBLOCK
    -> 保证尝试操作时不会因为情况变化或继续读写而无限等待
~~~

即使 `select` 告诉你 fd 可读：

- 本次 `recv()` 可能只得到一部分 HTTP 数据。
- 读完这一部分后再次 `recv()`，阻塞 Socket 可能等待。
- `send()` 仍然可能因对端太慢而阻塞。
- 非阻塞 `connect()` 仍需要单独处理 `EINPROGRESS`。

正式 Reactor 通常组合使用 I/O 多路复用、非阻塞 Socket、输入输出缓冲区和连接状态。

### 3.9 旧知识和本章新知识对照

| 内容 | 你以前的基础 | 本章新增内容 |
|---|---|---|
| 创建 Socket | `socket(AF_INET, SOCK_STREAM, 0)` | 给 fd 设置 `O_NONBLOCK` |
| `accept()` | 没连接时等待 | 没连接时返回 `EAGAIN`，一次取到队列暂空 |
| `recv()` | 没数据时等待 | 没数据时返回 `EAGAIN`，保存已有数据和解析状态 |
| `send()` | `send_all()` 在当前流程里继续发送 | 剩余数据进入输出缓冲区，等下次可写 |
| `connect()` | 等到成功或失败才返回 | `EINPROGRESS` 后等待事件，再查 `SO_ERROR` |
| 并发 | fork 子进程分别阻塞 | 少量线程管理大量非阻塞连接 |
| `select` | 知道哪些 fd 可能就绪 | 与 `O_NONBLOCK`、缓冲区和状态机组合 |

本章真正的新知识不是换了一套 Socket 函数。`accept()`、`recv()`、`send()`、`connect()` 仍是原来的函数；变化的是：

> 函数暂时不能完成时不再替程序等待，而是返回状态。程序必须保存进度，先处理其他连接，以后再继续。

## 4. epoll、事件循环和 Reactor

### 4.1 为什么非阻塞还不够

如果有 1000 个非阻塞连接，程序不能不停遍历 1000 个 fd 调用 `recv()`，因为多数调用只会得到 `EAGAIN`，浪费 CPU。

我们需要让内核告诉程序：

> 哪些 fd 现在具备继续处理的条件？

这种能力叫**I/O 多路复用**：一个线程等待多个 I/O 对象，谁准备好就处理谁。

`select`、`poll` 和 `epoll` 都属于 I/O 多路复用。EdgeGate 使用 Linux 专有的 `epoll`。

### 4.2 什么是“就绪”

`epoll` 报告的是**就绪**，不是“业务已经完成”。

- fd 可读：调用读取操作大概率能立即取得数据、EOF 或错误。
- fd 可写：发送缓冲区当前有空间，或者非阻塞连接过程已经得到结果。
- 挂断/错误事件：连接状态发生变化，需要进一步读取或检查错误。

因此：

- “可读”不等于“一条 HTTP 请求完整”。
- “可写”不等于“全部响应一定能一次写完”。
- `epoll` 不理解 HTTP，也不会替程序管理缓冲区。

### 4.3 epoll 的三个基本操作

项目会用到三个核心接口：

1. `epoll_create1()`：创建一个 epoll 实例，得到 epoll fd。
2. `epoll_ctl()`：添加、修改或删除需要关注的 fd 和事件。
3. `epoll_wait()`：等待就绪事件并返回事件数组。

概念流程：

~~~text
创建 epoll
    ↓
把监听 Socket 加入 epoll
    ↓
epoll_wait 等待
    ↓
监听 Socket 可读 -> accept 新连接
    ↓
把新连接 fd 加入 epoll
    ↓
某个连接可读/可写 -> 推进对应状态
~~~

### 4.4 LT 水平触发是什么

触发方式决定 `epoll` 什么时候重复报告事件。

**LT（Level Triggered，水平触发）**可以理解为：

> 只要当前条件仍然成立，下次等待时还会继续提醒。

例如连接输入缓冲中仍有未读取数据，即使上一次通知后没有全部读完，LT 模式以后仍会报告可读。

**ET（Edge Triggered，边缘触发）**主要在状态从“不就绪”变化成“就绪”时通知，对读取循环要求更严格。

EdgeGate v1 使用 LT。我们仍然会尽量读到 `EAGAIN`，但不在第一版引入 ET 的额外复杂度。

### 4.5 什么是事件循环

**事件**是程序需要处理的外部变化，例如：

- 新客户端到达。
- 某个客户端发来数据。
- 上游连接完成。
- 某个输出 fd 可以继续写。
- 定时器到期。
- 管理端发来命令。

不断等待事件、处理事件、再回到等待的循环叫**事件循环**。

伪代码形式：

~~~text
while 服务没有结束:
    等待一批事件
    for 每个事件:
        找到这个事件对应的对象
        根据对象当前状态处理事件
    处理到期定时任务
    回收已经关闭的对象
~~~

### 4.6 Reactor 是什么:Reactor 是组织网络程序代码的一种设计思想/程序结构
它的核心思想是：
统一等待多个事件
    ↓
某个 fd 就绪
    ↓
找到这个 fd 对应的对象
    ↓
调用该对象的处理代码
#
epoll告诉程序“哪个 fd 有事”，Reactor 找出“这个 fd 属于谁”，再调用“它该执行的代码”

Reactor 不是一个新的 Linux 函数，也不是线程调度器。它只是我们组织事件处理代码的一种方法。

先看一个具体例子。

#### 第一步：程序中已经有哪些 fd

假设 EdgeGate 当前保存了：

~~~text
fd 3 -> 监听 Socket，负责接收新客户端
fd 5 -> 客户端 A 的 Socket
fd 8 -> 客户端 A 对应的上游 Socket
fd 4 -> 定时器
~~~

程序不能只保存数字，还要记录每个 fd 属于谁、应该由哪段代码处理：

~~~text
fd 3 -> 监听对象
fd 5 -> 客户端 A 的连接对象
fd 8 -> 上游 A 的连接对象
fd 4 -> 定时器对象
~~~

这里的**对象**就是一个保存相关数据和操作的 C++ 对象。例如客户端 A 的连接对象会保存：

- 客户端 fd。
- 输入缓冲区。
- 输出缓冲区。
- HTTP 当前解析到哪里。
- 当前是否准备关闭。

#### 第二步：客户端 A 发来数据

客户端 A 发来的字节进入 Linux 内核中 fd 5 的接收缓冲区，于是 fd 5 变成可读。

`epoll_wait()` 返回一条事件：

~~~text
fd = 5
事件 = 可读 EPOLLIN
~~~

`EPOLLIN` 是 `epoll` 用来表示“这个 fd 现在可以尝试读取”的标志。

注意，`epoll_wait()` 只告诉程序：

> fd 5 可读。

它不知道 fd 5 属于客户端还是上游，也不知道应该解析 HTTP 还是接收管理命令。

#### 第三步：根据 fd 找到处理对象

程序查询自己保存的对应关系：

~~~text
fd 5 -> 客户端 A 的连接对象
~~~

然后调用这个对象的“处理可读事件”代码：

~~~text
客户端 A 连接对象处理可读事件
    ↓
调用 recv(fd 5)
    ↓
把字节加入输入缓冲区
    ↓
推进 HTTP 解析
    ↓
更新客户端 A 的连接状态
~~~

“根据事件找到对应对象，并调用相应处理代码”叫**分派**。

Reactor 最核心的工作就是：

~~~text
等待事件
    ↓
得到 fd 和事件类型
    ↓
根据 fd 找到对象
    ↓
把事件分派给这个对象
~~~

#### 一次可能返回多个事件

某一时刻 `epoll_wait()` 可能一次返回：

~~~text
fd 3 可读 -> 有新客户端
fd 5 可读 -> 客户端 A 发来数据
fd 8 可写 -> 上游 A 可以继续发送
fd 4 可读 -> 定时器到期
~~~

事件循环中的同一个线程依次处理：

~~~text
fd 3 -> 调用监听对象，accept 新客户端
fd 5 -> 调用客户端连接对象，recv 客户端数据
fd 8 -> 调用上游连接对象，继续 send
fd 4 -> 调用定时器对象，检查哪些连接超时
~~~

这不是把四个线程推进到运行态。这里通常仍是一个线程，只是它在一次循环中依次推进四个对象的状态。

#### 用伪代码表示

~~~text
记录表：
    fd 3 -> 监听对象
    fd 5 -> 客户端 A 对象
    fd 8 -> 上游 A 对象
    fd 4 -> 定时器对象

while 服务仍在运行:
    事件列表 = epoll_wait()

    for 每一条事件:
        fd = 事件中的 fd
        对象 = 根据 fd 查询记录表
        对象.处理事件(事件类型)
~~~

“处理对象”有时也称为 Handler，即事件处理者。它不是 Linux 强制提供的类型，而是程序自己设计的 C++ 接口或类。

#### 截图中的几个名称分别是什么

- `Listener`：程序设计的监听对象，拥有监听 fd，收到可读事件后调用 `accept()`。
- `ClientConnection`：程序设计的客户端连接对象，保存客户端的 fd、缓冲区和处理状态。
- `UpstreamConnection`：程序设计的上游连接对象，负责连接后端并处理上游读写。
- `Timer`：程序设计的定时任务对象，负责判断连接是否超过截止时间。
- `EventLoop`：执行“等待一批事件 → 逐个分派 → 再等待”的循环。

这些只是为了说明职责而使用的名称，不代表代码骨架已经确定，也不要求现在记住英文类名。

#### epoll、EventLoop 和 Reactor 的区别

~~~text
epoll
    -> Linux 提供的工具
    -> 返回哪些 fd 当前就绪

EventLoop
    -> 程序中的循环代码
    -> 不断调用 epoll_wait() 并处理返回结果

Reactor
    -> 整体组织方式
    -> 把等待、fd 与对象的对应关系、事件分派组合起来
~~~

可以把它压缩成一句话：

> `epoll` 给出“谁有事”，Reactor 找到“这是谁”，再调用“它该做什么”。

所以 Reactor 不是某一个 Linux API，而是整个事件处理结构的称呼。`epoll` 是实现这个结构的底层工具之一。

#### 当前阶段需要掌握什么

现在只需能跟踪下面这条路径：

~~~text
客户端 fd 可读
    ↓
epoll_wait() 返回 fd 和 EPOLLIN
    ↓
程序根据 fd 找到客户端连接对象
    ↓
连接对象调用 recv()
    ↓
把数据放入缓冲区并推进解析状态
~~~

具体的 C++ 类、记录表使用什么容器、对象什么时候销毁，会在实现 Reactor 时再确定。

### 4.7 什么是 timerfd

网络服务不能无限等待慢客户端或慢上游，所以需要定时器。

`timerfd` 是 Linux 提供的一种定时器接口。它把“定时器到期”也表示成一个可读 fd，因此可以和 Socket 一起放进 `epoll`：

~~~text
epoll_wait 返回：
    客户端 fd 可读
    上游 fd 可写
    timerfd 可读
~~~

这样事件循环不需要另外创建一个只负责睡眠的定时线程。

`timerfd` 只负责通知“时间到了”。哪个连接超时、超时类型是什么，仍需 EdgeGate 自己维护定时任务和连接截止时间。

## 5. 缓冲区、容量限制和背压

### 5.1 为什么连接需要输入缓冲区

**缓冲区**是一块暂时保存数据的内存。

HTTP 请求可能被拆成多次到达：

~~~text
第1次 recv：GET / HTTP/1.
第2次 recv：1\r\nHost: ex
第3次 recv：ample.com\r\n\r\n
~~~

输入缓冲区保存前几次收到但还不能组成完整消息的数据。每次有新数据到达，就追加到缓冲区，再让解析器消费其中已完整的部分。

### 5.2 为什么需要输出缓冲区

假设 EdgeGate 要向客户端发送 5000 字节，但非阻塞 `send()` 本次只写了 1200 字节。

输出缓冲区需要保留剩余 3800 字节，并记住下次从哪里继续：

~~~text
总数据：5000
已发送：1200
待发送：3800
~~~

只有存在待发送数据时，才需要持续关注该 fd 的可写事件。否则 Socket 通常长期可写，会让事件循环不断被无意义唤醒。

### 5.3 线性缓冲区和 RingBuffer

**线性缓冲区**通常用一段连续内存和两个位置表示：

~~~text
[ 已消费 | 可读取数据 | 空闲空间 ]
           ^读位置       ^写位置
~~~

已经消费的空间可以通过移动数据或调整策略重新利用。

**RingBuffer（环形缓冲区）**把数组尾部和头部逻辑连接起来，写到末尾后可以绕回开头。它能减少整体搬移，但一段逻辑数据可能分布在数组尾部和头部两段。

HTTP 解析经常需要连续查找 `\r\n\r\n`。项目初期优先选择接口清晰、容易验证的缓冲设计，不会为了展示 RingBuffer 而强行增加解析复杂度。

### 5.4 为什么缓冲区不能无限增长

如果客户端每秒只能接收 10 KB，而上游每秒产生 10 MB，EdgeGate 来不及把响应发给客户端。

若代理继续无限读取上游并扩容：

~~~text
1秒积压约 10 MB
10秒积压约 100 MB
100个慢客户端可能积压约 10 GB
~~~

最终会耗尽内存。这不是单个请求的性能问题，而是整个服务的稳定性问题。

### 5.5 背压、高水位和低水位

**背压**表示消费方处理不动时，生产方必须减速或暂停，避免数据无限积压。

例如给输出缓冲设置：

~~~text
高水位：1 MB
低水位：512 KB
硬上限：2 MB
~~~

处理规则可以是：

1. 缓冲达到 1 MB，暂停读取上游。#理想上不再读取
2. 客户端继续接收，缓冲降到 512 KB。
3. 重新允许读取上游。
4. 如果因不可避免的数据到达而超过 2 MB，终止该会话并记录容量错误。

使用高、低两个不同阈值，可以避免缓冲大小在一个阈值附近波动时频繁开关读取事件。

## 6. RAII、所有权、状态机和生命周期

### 6.1 fd 为什么需要 RAII

Socket fd 使用完必须调用 `close()`。忘记关闭会产生 fd 泄漏；重复关闭可能误伤后来复用同一数字的新 fd。

RAII 的做法是让一个 C++ 对象独占 fd：

- 构造或接管时获得 fd。
- 对象不可复制，避免两个对象都认为自己拥有同一个 fd。
- 对象可以移动，把所有权转交给另一个对象。
- 析构时如果 fd 有效，就关闭一次。

这样正常返回、异常或中途失败都能走同一资源释放规则。

### 6.2 所有权是什么

所有权回答：

> 哪个对象负责让这个资源一直有效，并最终释放它？

如果多个地方只保存裸指针，却没有明确谁负责销毁对象，就容易产生：

- 内存泄漏：没人销毁。
- 重复释放：多人都销毁。
- 悬空指针：对象已销毁，其他地方还在访问。

EdgeGate 首版优先让单个事件循环拥有连接容器，连接对象拥有自己的 fd 和缓冲区，减少跨线程共享所有权。

### 6.3 一个代理会话为什么有两端

一次反向代理请求至少涉及：

~~~text
客户端连接 <-> EdgeGate 会话 <-> 上游连接
~~~

两端可能独立发生事件：

- 客户端发完请求后断开。
- 上游连接被拒绝。
- 上游返回一半响应后崩溃。
- 客户端接收太慢导致输出积压。
- 客户端保持连接，但上游必须关闭。

因此“关闭连接”不是一个简单布尔值。程序要知道关闭的是哪一端、是否还有待发送数据、是否已经向客户端发出部分响应。

### 6.4 状态机是什么

**状态**表示对象当前处于哪个阶段。

**状态机**规定：

- 有哪些状态。
- 某个事件到来时允许从什么状态转到什么状态。
- 转换时执行什么动作。

一个简化代理会话可能经历：

~~~text
读取客户端请求
    ↓
选择路由和上游
    ↓
连接上游
    ↓
发送请求
    ↓
读取上游响应
    ↓
向客户端发送响应
    ↓
完成本轮 / 继续 Keep-Alive / 关闭
~~~

如果在“连接上游”阶段发生超时，处理方式不同于“已经向客户端发送一半响应后上游断开”。状态机让这些失败路径有明确依据。

### 6.5 配置快照和原子切换

**快照**是在某个时刻完整、只读的一份配置对象。

热重载不能直接逐字段修改正在使用的配置，否则某个请求可能看到：

~~~text
旧路由 + 新上游地址 + 尚未更新的超时
~~~

正确流程是：

1. 读入新 YAML。
2. 构建一份新的完整配置。
3. 校验所有字段和引用关系。
4. 全部成功后，一次性把“当前配置”指向新快照。
5. 失败则丢弃新快照，继续使用旧配置。

这里的“原子切换”表示其他代码不会看到切换到一半的状态，不一定等同于必须使用某一个特定的 `std::atomic` 类型。

## 7. 线程、信号和 Unix Socket

### 7.1 网络并发和线程不是同一个概念

**并发**表示多个任务在同一时间段内都在推进，不等于每个任务必须拥有一个线程。

EdgeGate 首版由一个 Reactor 线程轮流处理许多已经就绪的连接。只要每次处理都短小、不会阻塞，多个连接就能并发推进。

后台线程只承担边界明确的工作，例如异步写日志。是否增加更多网络线程，要由压测数据决定，而不是一开始预设复杂架构。

### 7.2 日志线程怎样复用有界队列

网络线程生成日志记录后，把记录放入有界队列；日志线程从队列取出记录并写文件：

~~~text
网络线程 -> 有界日志队列 -> 日志线程 -> 日志文件
~~~

仍需新定义：

- 队列满时阻塞、丢弃还是降级。
- 丢弃时如何计数并让用户知道。
- 服务退出时怎样停止生产、排空队列并 `join()`。
- 磁盘写入失败时如何处理，不能让网络线程崩溃。

### 7.3 信号和安全退出

信号是操作系统向进程发送的一种异步通知。例如 systemd 停止服务时通常发送 `SIGTERM`。

信号可能在程序执行任意普通代码时到来，因此信号处理函数只能做极少量安全操作，不能直接在里面执行复杂的日志、配置解析或容器修改。

安全退出由主事件循环完成：

~~~text
收到退出通知
    ↓
停止接受新工作
    ↓
等待正在处理的请求完成
    ↓
达到排空超时后结束剩余连接
    ↓
刷新日志、关闭 fd、退出
~~~

### 7.4 什么是 drain

`drain` 直译为“排空”。在服务中表示：

- 不再接受新的工作。
- 已经开始的工作尽量完成。
- 等待不能无限持续，必须有最大排空时间。

这比直接杀死进程更适合正在处理请求的服务。

### 7.5 Unix Socket 管理通道

Unix Domain Socket 是同一台 Linux 主机上进程间通信的一种 Socket。它不通过外部网络端口，而是通常由文件系统路径标识，例如：

~~~text
/run/edgegate/edgegate.sock
~~~

`edgegatectl` 连接这个 Socket，向 `edgegate` 发送管理命令。

管理协议仍需要消息边界。例如可以规定一条命令以换行结束，并限制最大长度。还要处理：

- 只收到半条命令。
- 一次收到多条命令。
- 非法命令。
- 客户端中途断开。
- Socket 文件权限。
- 服务异常退出后留下陈旧路径。

## 8. HTTP 反向代理、路由和上游

### 8.1 正向代理和反向代理

正向代理代表客户端。客户端知道自己正在使用代理，由代理替它访问外部服务器。

反向代理代表服务器端。外部客户端只访问代理，代理再选择内部真正处理请求的服务器。

EdgeGate 是反向代理：

~~~text
curl / 浏览器
      |
      v
EdgeGate
      |
      v
真实后端服务
~~~

### 8.2 上游和上游池

代理要连接的真实后端称为**上游**。

多个提供相同服务的上游组成**上游池**：

~~~text
api_pool:
    127.0.0.1:9001
    127.0.0.1:9002
    127.0.0.1:9003
~~~

这样一个上游故障时可以选择其他节点，也可以把请求分散到多个节点。

### 8.3 路由是什么

路由是“什么请求应该送到哪个上游池”的规则。

EdgeGate v1 根据两个信息路由：

- Host：客户端要访问的主机。
- 路径：请求行中的请求目标路径。

例如：

~~~text
Host = api.example.com
路径以 /users 开头
-> user_pool

Host = api.example.com
路径以 /orders 开头
-> order_pool
~~~

### 8.4 精确 Host、通配 Host 和最长路径前缀

**精确匹配**表示完整相等：

~~~text
规则：api.example.com
请求：api.example.com
结果：匹配
~~~

**通配匹配**允许某一部分变化，例如计划中的 `*.example.com` 可以匹配 `api.example.com`。通配语法和边界会在路由阶段固定并测试，不能临时猜测匹配行为。

同一个请求同时匹配精确 Host 和通配 Host 时，精确 Host 优先，因为它表达了更具体的配置意图。

**路径前缀**表示路径从某段文本开始。

若同时存在：

~~~text
/
/api
/api/users
~~~

请求路径是 `/api/users/42` 时，三个前缀都符合，但 `/api/users` 最具体，所以采用最长路径前缀。

### 8.5 轮询是什么

轮询（Round Robin）表示按顺序选择可用上游：

~~~text
请求1 -> 上游A
请求2 -> 上游B
请求3 -> 上游C
请求4 -> 上游A
~~~

轮询不是保证每台机器实际负载完全相同。请求耗时、连接失败和健康状态都会影响真实负载，但它适合作为第一个可重复验证的选择算法。

### 8.6 Header 调整

反向代理不能机械地把所有 Header 原样转发。

有些 Header 只描述当前这一段连接，不应该跨越代理继续传递，称为**逐跳 Header（hop-by-hop header）**。例如 `Connection` 中声明的连接级字段需要按协议处理。

代理还可能：

- 设置正确的 `Host`。
- 记录或追加客户端地址信息。
- 删除与当前连接相关、不能继续转发的字段。

具体改写规则会在阶段 6 固化，并用原始字节级测试验证。

## 9. 健康检查、故障摘除和安全重试

### 9.1 健康检查是什么

健康检查是代理定期确认上游是否还能提供服务。

最简单的检查可以是：

- 尝试建立 TCP 连接。
- 或发送一个受控 HTTP 请求，检查返回状态。

一次失败不一定立刻判死，因为短暂网络抖动可能很快恢复。通常设置：

~~~text
连续失败 N 次 -> 标记不健康
连续成功 M 次 -> 恢复健康
~~~

不健康上游暂时不参加轮询，这叫**故障摘除**。

健康检查只能说明“最近检查时可用”，不能保证下一次真实请求一定成功，所以请求路径仍要处理连接失败和超时。

### 9.2 什么是幂等

如果同一个操作执行一次和执行多次，对最终业务状态的影响相同，这类操作称为**幂等**。

GET 和 HEAD 通常用于读取，可以在严格条件下重试。POST 可能创建订单或扣款，重复发送可能产生两次业务影响，因此不能由代理随意重试。

### 9.3 EdgeGate 的安全重试条件

v1 只有同时满足以下条件才重试：

- 方法是 GET 或 HEAD。
- 还没有向客户端发送任何响应字节。
- 还有其他健康上游可选。
- 重试次数没有超过配置限制。

如果已经向客户端发送一半响应，再换上游会把两台服务器的响应拼在一起，必须禁止。

### 9.4 没有健康上游

路由存在但上游池中没有健康节点时，EdgeGate 返回：

~~~text
503 Service Unavailable
~~~

这与 `404 Not Found` 不同：

- 404：没有匹配的路由。
- 503：路由存在，但当前没有可服务的上游。

## 10. 超时、慢连接和错误码

### 10.1 为什么每个等待都要有期限

客户端或上游可能：

- 建立连接后长期不发送完整 Header。
- 请求体只发送一半。
- 上游连接迟迟不能建立。
- 上游接受请求后一直不返回响应。
- 客户端长期不读取响应。

如果没有超时，这些连接会长期占用 fd、内存和连接对象。

### 10.2 不同阶段需要不同超时

- 请求头超时：限制发送完整 Header 的时间。
- 请求体超时：限制发送完整 Body 的时间。
- 上游连接超时：限制 `connect()` 完成时间。
- 上游首字节超时：请求发完后，限制等待第一个响应字节的时间。
- 上游读取超时：限制响应传输中长期无进展。
- 客户端写超时：限制响应长期无法写给客户端。
- Keep-Alive 空闲超时：一轮完成后，限制等待下一请求的空闲时间。
- drain 超时：限制平滑退出的总等待时间。

超时通常使用截止时间，而不是只统计某个函数调用耗时，因为非阻塞操作会分多次事件推进。

### 10.3 慢连接攻击与资源限制

客户端可以非常缓慢地发送 Header，使每条连接都看似还没出错，却长期占用资源。这类行为常被称为慢连接或 Slowloris 类问题。

应对手段不是只加一个超时，还包括：

- Header 最大字节数。
- 请求体最大字节数。
- 连接数限制。
- 各阶段超时。
- 缓冲区硬上限。
- 清晰的关闭和指标记录。

### 10.4 EdgeGate 错误码

| 状态码 | 直白含义 | EdgeGate 中的典型原因 |
|---|---|---|
| `400 Bad Request` | 请求本身不合法 | 请求行/Header 畸形、冲突的消息长度 |
| `404 Not Found` | 没找到对应处理规则 | Host/路径没有匹配路由 |
| `413 Payload Too Large` | 请求超过允许容量 | Header、Body 或受控缓冲区超限 |
| `502 Bad Gateway` | 代理从上游得到了失败结果 | 连接失败、响应协议错误、响应中途断开 |
| `503 Service Unavailable` | 当前没有服务能力 | 没有健康上游或服务正在拒绝新工作 |
| `504 Gateway Timeout` | 等待上游超时 | 连接或响应超过配置期限 |

返回给客户端的正文保持简短，不泄露内部文件路径和系统细节。详细原因写入错误日志和指标。

## 11. YAML 配置、CLI、JSON 和 Dashboard

### 11.1 配置文件解决什么问题

如果监听端口、上游地址和超时都写死在 C++ 代码里，每次修改都要重新编译。

配置文件把“经常变化的运行参数”从程序实现中分离出来。

EdgeGate 使用 YAML。YAML 用缩进表示层级，例如：

~~~yaml
listen:
  address: 0.0.0.0
  port: 8080

upstreams:
  api_pool:
    - host: 127.0.0.1
      port: 9001
    - host: 127.0.0.1
      port: 9002
~~~

`yaml-cpp` 可以把 YAML 语法转换成 C++ 可读取的数据，但它不会自动判断业务是否合理。

EdgeGate 仍需检查：

- 端口是否在合法范围。
- 超时是否为正数。
- 低水位是否小于高水位和硬上限。
- 路由引用的上游池是否存在。
- 名称是否重复。
- 必填字段是否缺失。

### 11.2 CLI 是什么

CLI 是 Command Line Interface，即命令行界面。

`edgegatectl` 是一个独立命令行程序。它不直接访问 `edgegate` 的内部变量，而是通过 Unix Socket 发送命令：

~~~text
edgegatectl status
edgegatectl routes
edgegatectl upstreams
edgegatectl stats --json
edgegatectl reload
edgegatectl drain
edgegatectl stop
~~~

查询命令输出信息；控制命令请求服务执行受控状态转换；进程退出码告诉脚本命令是否成功。

### 11.3 热重载是什么

**热重载**表示服务不停机地加载新配置。

流程是：

~~~text
读取新文件
    ↓
完整解析
    ↓
完整校验
    ↓
成功：切换新配置
失败：继续使用旧配置并报告原因
~~~

热重载的核心不是“重新读了一次文件”，而是失败时旧服务仍然可靠。

### 11.4 JSON 状态接口

JSON 是一种结构化文本格式，例如：

~~~json
{
  "connections": 12,
  "requests_total": 1050,
  "upstreams": [
    {"name": "api-1", "healthy": true},
    {"name": "api-2", "healthy": false}
  ]
}
~~~

与自由格式日志相比，程序和网页可以稳定地读取字段。

`nlohmann/json` 用于把 C++ 中的状态数据可靠地序列化成 JSON，不负责定义哪些指标应该存在。

### 11.5 Dashboard 前端怎样工作

Dashboard 使用三种浏览器技术：

- HTML：定义标题、表格、卡片等页面结构。
- CSS：控制颜色、间距和布局。
- JavaScript：定时请求 JSON 状态接口，并把新数据更新到页面。

数据流是：

~~~text
浏览器打开 Dashboard
    ↓
JavaScript 请求 /api/status
    ↓
EdgeGate 返回 JSON
    ↓
JavaScript 更新连接数、延迟和上游状态
~~~

v1 Dashboard 只读并默认只监听本机管理地址。网页修改配置、重载和停止会引入身份认证、请求伪造和误操作风险，不在当前范围。

## 12. 日志、指标和延迟百分位

### 12.1 日志是什么

日志是一条条带上下文的事件记录，用来回答“某次请求或故障发生了什么”。

EdgeGate 有三类主要日志：

- 访问日志：方法、Host、路径、路由、上游、状态码、字节数和延迟。
- 错误日志：失败阶段、错误类别、系统错误码和必要上下文。
- 管理日志：启动、重载、健康变化、drain 和停止。

日志不能无控制地记录密码、Cookie、Authorization、私钥或完整请求体。

日志文件也不能无限增大。**日志轮转**表示按文件大小或时间关闭当前日志文件，改写新文件，并按配置保留有限数量的旧日志。轮转时必须保证日志线程不会继续向已经关闭的文件对象写入。

### 12.2 指标是什么

指标是可以累计、聚合和比较的数值，用来回答“服务整体是否健康”。

例如：

- 当前连接数。
- 累计请求数。
- 当前在途请求数。
- 各状态码数量。
- 各上游成功、失败和超时次数。
- 触发背压次数。
- 当前内存和 fd 数量。

每个指标必须定义何时增加和减少。否则代码不同位置对“请求总数”的理解可能不同。

### 12.3 P50、P95 和 P99

假设记录 100 个请求延迟，从小到大排列：

- P50：大约第 50 个值，表示一半请求不超过这个延迟。
- P95：大约第 95 个值，表示 95% 请求不超过这个延迟。
- P99：大约第 99 个值，用于观察少量很慢的请求。

平均值可能掩盖尾部慢请求，所以压测同时记录吞吐量和延迟百分位。

### 12.4 QPS、CPU、RSS 和 fd 峰值

- QPS：每秒完成的请求数。
- CPU：处理请求消耗的处理器资源。
- RSS：进程当前驻留在物理内存中的主要内存量指标。
- fd 峰值：测试期间进程同时持有文件描述符的最高数量。

这些数字必须带上机器、构建类型、配置、请求类型和上游条件，不能脱离环境比较。

## 13. CMake、调试、测试和 Linux 产品化

### 13.1 为什么不只是一个 cpp 文件

最终工程包含多个目标：

- `edgegate` 服务程序。
- `edgegatectl` 管理程序。
- 可复用的核心库。
- 单元测试。
- 三个测试后端和集成测试工具。
- Dashboard 静态文件。

CMake 负责描述源文件如何编译、目标如何链接、测试如何运行以及安装到什么位置。

### 13.2 编译、链接和第三方依赖

**编译**把一个个 C++ 源文件转换成目标文件。

**链接**把目标文件和库组合成最终可执行程序。

第三方库分工：

- `yaml-cpp`：YAML 语法解析。
- `spdlog`：日志格式和输出。
- `nlohmann/json`：JSON 序列化。
- GoogleTest：单元测试组织和断言。

这些库不替代事件循环、HTTP 解析、状态机和产品语义。

严格编译至少启用：

~~~text
-Wall -Wextra -Wpedantic
~~~

目标是零警告，而不是通过关闭警告让输出变干净。

### 13.3 GDB 和 Sanitizer 分别解决什么

GDB 用于暂停程序、查看调用栈、变量和线程状态，适合定位“程序在什么路径出错”。

Sanitizer 在运行时检测某些难以肉眼发现的问题：

- ASan：越界访问、释放后使用等内存错误。
- UBSan：部分未定义行为，例如某些非法运算。

它们不能证明程序没有业务错误，所以仍需协议和故障测试。

### 13.4 五层测试

#### 单元测试

不启动完整服务，单独验证一个小组件，例如 HTTP 分片解析、路由优先级和缓冲区边界。

#### 集成测试

启动 EdgeGate 和测试上游，验证多个组件组合后能否完成真实请求。

#### 故障测试

主动制造上游拒绝连接、崩溃、慢响应、客户端提前断开和无效配置。

#### 压测

用并发请求测量 QPS、P50/P95/P99、CPU、RSS 和 fd 峰值。

#### 长稳测试

持续运行至少 12 小时，观察连接、线程、内存和 fd 是否持续增长。

“编译成功”只证明代码能生成程序，不能替代上述测试。

### 13.5 cmake --install

构建目录中的程序只适合开发。`cmake --install` 根据安装规则把：

- 可执行文件。
- 示例配置。
- Dashboard 文件。
- systemd 单元。
- 文档。

安装到约定位置。安装路径、权限和卸载方法都属于产品交付的一部分。

### 13.6 systemd 是什么

systemd 是 Ubuntu 中常用的系统服务管理器。正式服务通常保持前台运行，由 systemd 负责：

- 启动和停止。
- 开机启动。
- 传递 `SIGTERM`。
- 设置运行用户和部分安全限制。
- 失败后按配置重启。
- 查看服务状态。

EdgeGate 不需要自己实现传统的双 fork 后台化，应该与 systemd 的服务生命周期配合。

## 14. 分阶段阅读成熟项目

阅读大型项目的原则是：先自己实现最小版本并得到测试结果，再带着具体问题局部对照。

### 14.1 为什么不能一开始通读

Nginx、muduo 和 libevent 都包含大量当前阶段用不到的兼容性、优化和抽象。如果没有自己的问题，容易只看到类名和函数，却不知道设计解决了什么故障。

正确顺序：

~~~text
自己实现
    ↓
运行测试并暴露问题
    ↓
写下现象和假设
    ↓
只读成熟项目中对应部分
    ↓
用自己的接口表达改进
    ↓
重新测试
~~~

### 14.2 阅读路径

| EdgeGate 阶段 | 阅读对象 | 只看什么 | 要回答的问题 |
|---|---|---|---|
| 文档与架构 | [Nginx 开发指南](https://nginx.org/en/docs/dev/development_guide.html) | 连接、事件、请求和配置的概念章节 | 成熟服务怎样划分模块和生命周期？ |
| Reactor 完成后 | [muduo](https://github.com/chenshuo/muduo) | EventLoop、Channel、Buffer 的相关小范围代码 | 事件关注、缓冲和对象所有权怎样分离？ |
| Reactor 完成后 | [libevent](https://github.com/libevent/libevent) | event_base、事件注册和定时器文档 | I/O 事件和定时事件怎样统一管理？ |
| HTTP 状态机完成后 | [Nginx](https://github.com/nginx/nginx) | 请求行、Header、上游状态相关文件 | 成熟实现怎样防御异常输入？ |
| 热重载与排空 | Nginx 文档和相关源码 | 配置生命周期和退出流程 | 新旧配置和在途请求怎样共存？ |
| 压测加固 | 上述项目的测试和安全公告 | 与实际故障相符的部分 | 当前实现遗漏了什么失败模式？ |

阅读时记录链接、版本或提交、自己的问题和最终结论。若引用或改编代码，必须检查许可证并保留必要声明。

## 15. 按项目阶段学习本讲义

不要求项目开始前一次掌握全部章节。

| 项目阶段 | 重点章节 | 达到的目标 |
|---|---|---|
| 文档审阅 | 0、1、2、8 | 能解释 TCP、HTTP 消息和反向代理数据流 |
| 阻塞式链路 | 1、2、8 | 能跟踪一次请求和响应的实际字节 |
| HTTP 解析器 | 2 | 能判断任意分片下消息是否完整 |
| Reactor | 3、4、5、6 | 能解释非阻塞、就绪、缓冲、状态和所有权 |
| 路由和可靠性 | 8、9、10 | 能解释选择上游、失败、重试、超时和错误码 |
| 运行时管理 | 7、11、12 | 能解释 CLI、重载、退出、日志和指标 |
| 产品化与发布 | 13、14 | 能完成构建、测试、安装、长稳和参考阅读 |

开始第一个编程任务前，当前只要求能用自己的话回答：

- TCP 为什么不能用一次 `recv()` 代表一条请求？
- HTTP 请求行、Header 和 Body 分别是什么？
- `\r\n\r\n` 表示什么，为什么不一定表示整个请求结束？
- 响应体为什么有四种结束方式？
- EdgeGate 为什么同时是服务器和客户端？
- 反向代理、路由和上游分别是什么？

`epoll`、背压、热重载等内容会在对应阶段再次精读和验证，现在不要求背 API。

## 16. 术语速查

| 术语 | 直白含义 |
|---|---|
| TCP 字节流 | 有顺序但没有业务消息边界的一串字节 |
| 应用层协议 | 通信双方共同遵守的数据格式和处理规则 |
| 请求/响应 | 客户端提出操作，服务器返回处理结果 |
| Header | 描述 HTTP 消息的字段集合 |
| Body | HTTP 消息携带的可选正文数据 |
| 消息定界 | 判断一条消息从哪里开始、在哪里结束 |
| 增量解析 | 数据分批到达时保存进度并继续解析 |
| Keep-Alive | 一个 TCP 连接按顺序承载多轮 HTTP 请求和响应 |
| fd | Linux 进程中代表已打开内核资源的整数 |
| 非阻塞 | 当前不能完成时立即返回，让程序先处理其他工作 |
| 就绪 | 某个 I/O 操作当前大概率可以取得进展 |
| I/O 多路复用 | 一个线程等待多个 I/O 对象的就绪变化 |
| epoll | Linux 提供的 I/O 多路复用接口 |
| LT | 条件持续成立时会继续报告的水平触发方式 |
| 事件循环 | 不断等待事件、处理事件、再继续等待的循环 |
| Reactor | 统一等待事件并分派给对应对象的程序结构 |
| timerfd | 把定时器到期表示成可被 epoll 监听的 fd |
| 缓冲区 | 暂时保存尚未处理或尚未写完数据的内存 |
| 背压 | 消费方变慢时限制生产方继续输入 |
| 高/低水位 | 暂停输入和恢复输入的两个容量阈值 |
| RAII | 让 C++ 对象生命周期自动管理资源生命周期 |
| 所有权 | 哪个对象负责保持资源有效并最终释放 |
| 状态机 | 明确状态、事件和状态转换的模型 |
| 反向代理 | 代表服务器端接收请求并转发到真实后端 |
| 上游 | 代理要连接的真实后端服务 |
| 上游池 | 提供同类服务的一组上游 |
| 路由 | 把符合条件的请求映射到上游池的规则 |
| 轮询 | 按顺序循环选择可用上游 |
| 健康检查 | 定期检查上游是否能继续服务 |
| 幂等 | 重复执行不会额外改变最终业务状态 |
| drain | 停止接受新工作并等待在途工作结束 |
| 热重载 | 不停止服务地加载并安全切换新配置 |
| CLI | 命令行界面 |
| JSON | 便于程序和网页读取的结构化文本格式 |
| 指标 | 可累计、聚合和比较的运行数值 |
| P50/P95/P99 | 50%、95%、99% 请求不超过的延迟值 |
| QPS | 每秒完成的请求数 |
| RSS | 进程驻留在物理内存中的主要内存量指标 |
| systemd | Ubuntu 中常用的系统服务管理器 |
