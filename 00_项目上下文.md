# EdgeGate 项目上下文

> 用途：这是新对话恢复项目状态时的第一入口，只保存当前仍然有效的事实、约束和下一步。
>
> 最近更新：2026-08-12
>
> 当前状态：阶段 2“阻塞式最小链路”已通过；阶段 3“HTTP 增量解析”尚未开始。

## 0. 30 秒摘要

- 目标：实现完整的 Linux C++17 HTTP/1.1 反向代理 EdgeGate。
- 当前：阶段 2 最终回归已通过，等待创建并核对本地提交。
- 工作区：阶段 2 提交完成后应保持干净；新对话必须用 `git status` 复核。
- 权威源码：`D:\桌面\EI\反向代理项目`；Ubuntu 只作执行副本。
- 下一步：进入阶段 3 前，先生成 HTTP 增量解析的当前任务说明、接口和验收方法。
- 禁止：把计划写成成果、覆盖未提交修改、双端同时编辑、未经确认直接生成完整实现。

## 1. 新对话启动规则

新的 Codex 对话开始后，先执行以下只读步骤：

1. 完整阅读本文件。
2. 阅读 [项目计划.md](项目计划.md) 中当前阶段及验收标准。
3. 若任务涉及新知识，读取 [知识衔接.md](知识衔接.md) 对应章节。
4. 若要继续问题复盘或作出新决策，读取 [01_工程决策与成长记录.md](01_工程决策与成长记录.md)。
5. 运行 `git status`，检查是否存在用户尚未提交的修改。
6. 检查当前文件和实际运行结果，不把本文中的计划当成已经实现的事实。
7. 在写代码或修改项目范围前，先按仓库要求完成需求对齐。

可以在新对话中直接使用：

~~~text
请先完整阅读 D:\桌面\EI\反向代理项目\00_项目上下文.md，
再检查当前 Git 状态和相关文件。先告诉我目前已验证、未验证和下一步，
不要把计划当成已实现，也不要覆盖未提交修改。
~~~

## 2. 项目目标

EdgeGate 是一个用于 Linux C/C++ 应用层开发训练的 HTTP/1.1 反向代理。

最终目标不是只做“能转发一次请求”的 Demo，而是形成一个：

- 可从干净源码构建和安装的 Linux 服务；
- 可通过 YAML 配置路由、上游和运行参数的代理；
- 可通过 Unix Socket 和 `edgegatectl` 管理的后台服务；
- 有日志、指标和只读 Dashboard 的可观测产品；
- 有单元、集成、故障、压测和长稳证据的完整项目。

项目不绑定硬件、视觉、ROS 2 或具身智能。

## 3. 权威路径与运行环境

| 项目 | 当前约定 |
|---|---|
| Windows 正式源码 | `D:\桌面\EI\反向代理项目` |
| Ubuntu 执行副本 | `/home/zy/projects/edgegate` |
| 目标系统 | Ubuntu 22.04.5 |
| 编译器基线 | G++ 11.4 |
| CMake 基线 | CMake 3.22 |
| C++ 标准 | C++17 |
| Git 分支 | `main` |
| 文档起始提交 | `6bbddd9 docs: establish EdgeGate project baseline` |
| 最近里程碑 | 阶段 2，提交主题 `feat: complete stage 2 blocking proxy chain` |
| GitHub | 尚未创建正式远端；结项清理后再上传 |

Windows 目录是唯一权威源码和正式 Git 仓库。Ubuntu 副本只用于 Linux 构建、调试和运行，原则上不编辑源码。

当前已验证的单向工作流：

1. 只在 Windows 修改正式源码。
2. 修改后先检查 Windows Git 状态。
3. 通过 SSH/SCP 将源码和文档复制到 Ubuntu `/home/zy/projects/edgegate`。
4. 不复制 `.git`、`build/`、日志、Socket、PID 和编辑器临时文件。
5. 首次同步或重要阶段使用 SHA-256 比较两端文件。
6. 只在 Ubuntu 的 `build/` 中配置、编译、测试、GDB 和 Sanitizer 调试。
7. 发现问题后回到 Windows 修改，再重新同步；不让 Ubuntu 形成第二套权威源码。

首次 SCP 与 9 个文件 SHA-256 比较已于 2026-08-11 通过。

## 4. 当前真实进度

### 4.1 已完成

- 建立 EdgeGate 产品方向、HTTP/1.1 v1 范围和 12 阶段计划。
- 创建并审阅式迭代 [项目计划.md](项目计划.md)。
- 创建 [知识衔接.md](知识衔接.md)，之后根据实际基础重写为可直接学习的讲义。
- 讲义已覆盖 TCP 到 HTTP、阻塞到非阻塞、`epoll`、Reactor、缓冲区、背压、RAII、路由、健康检查、管理、日志、指标和产品化。
- 用户已在 2026-08-06 报告阅读完 `知识衔接.md` 全部内容。
- 初始化独立本地 Git 仓库并建立文档基线提交 `6bbddd9`。
- 创建本上下文文档和工程决策/成长记录。
- 用户于 2026-08-06 明确同意开始完整阶段 1，前置文档门禁通过。
- 为当前仓库配置 Git 身份 `lkdr <lktr-wq@users.noreply.github.com>`，未修改全局 Git 配置。
- 用户亲手输入 `CMakeLists.txt`、`.gitignore`、两个程序入口和最小测试。
- 修正 4 处 CMake 拼写/路径错误后完成静态复核。
- 将 9 个正式文件首次同步到 Ubuntu，Windows 与 Ubuntu SHA-256 全部一致。
- Ubuntu 干净配置与构建通过，三个目标实际使用 `-g -Wall -Wextra -Wpedantic -std=c++17`。
- CTest `1/1` 通过，`edgegate` 和 `edgegatectl` 均正常输出基线信息。

### 4.2 阶段 1 本地提交内容

- `知识衔接.md` 已从最初知识地图大幅重写，并多次根据阅读反馈补充实例。
- 新增 `00_项目上下文.md`。
- 新增 `01_工程决策与成长记录.md`。
- 更新 `项目计划.md` 的路径、门禁和阶段状态。
- 新增 `.gitignore`、`CMakeLists.txt`、`apps/` 与 `tests/` 最小基线。

上述文件连同用户输入的最小基线代码已纳入阶段 1 本地提交，不上传远端。

### 4.3 尚未完成或尚未验证

- 只有最小构建入口和冒烟测试，没有代理业务代码。
- `yaml-cpp`、`spdlog`、`nlohmann/json` 和 GoogleTest 开发包尚未安装；在首次使用前再安装。
- 没有实现阻塞式代理、HTTP 解析器、非阻塞 Reactor 或任何产品功能。
- 已验证手动 SSH/SCP 同步流程，但尚未建立自动同步脚本。
- 没有压测、Sanitizer、故障测试或长稳结果。

任何后续对项目能力的描述都必须区分“计划”“已实现”和“已验证”。

## 5. 用户当前知识基线

### 5.1 以前已经编写和验证

- 默认阻塞模式的 TCP、UDP 和 Unix Socket。
- TCP 客户端/服务端及 fork 多进程服务端。
- `send_all()`、真实字节长度和长消息联调。
- 重复连接和 `sigaction(SIGCHLD, ...)` 子进程回收。
- 基本线程、互斥量、条件变量、有界队列。
- RAII、智能指针、移动语义和固定容量设计。
- CMake、GDB、严格编译和 Sanitizer 的基础概念。
- `select` 的基本思想；不等于已经完成非阻塞 Socket 服务。

### 5.2 已阅读，但还没有通过 EdgeGate 代码验证

- HTTP 请求、响应和消息边界。
- `Content-Length`、chunked 和关闭连接定界。
- `O_NONBLOCK`、`EAGAIN`、`EWOULDBLOCK`、`EINPROGRESS`。
- `epoll` LT、事件循环、Reactor 和 `timerfd`。
- 输入/输出缓冲区、背压和高低水位。
- 代理状态机、路由、上游、健康检查和安全重试。
- YAML、管理 CLI、JSON 状态接口、Dashboard、日志和指标。
- 热重载、drain、systemd、压测和长稳。

“阅读完成”不能写成“已经掌握或实现”。后续需要通过小任务、代码审阅和实际运行逐项确认。

## 6. 已锁定的产品范围

### 6.1 核心技术

- C++17 + Linux POSIX API。
- 非阻塞 Socket、LT `epoll`、`timerfd`、Unix Socket 和 RAII。
- 不使用 Boost.Asio、muduo 或 libevent 代写核心事件循环。
- `yaml-cpp`、`spdlog`、`nlohmann/json` 和 GoogleTest 可作为辅助依赖。
- HTTP 增量解析器自行实现受控子集。

### 6.2 对外程序和配置

- `edgegate`：后台反向代理服务。
- `edgegatectl`：Unix Socket 管理客户端。
- `edgegate.yaml`：监听、路由、上游、超时、缓冲、健康检查和日志配置。

管理命令：

~~~text
edgegatectl status
edgegatectl routes
edgegatectl upstreams
edgegatectl stats --json
edgegatectl reload
edgegatectl drain
edgegatectl stop
~~~

### 6.3 HTTP v1 支持

- 普通 HTTP/1.1 请求。
- 无正文和 `Content-Length` 请求体。
- 顺序 Keep-Alive。
- 无正文、`Content-Length`、chunked、关闭连接四类响应。
- Host 精确或通配匹配，加最长路径前缀。
- 健康上游轮询。
- 仅 GET/HEAD 且尚未返回响应字节时允许安全重试。

### 6.4 明确不支持

- 请求流水线。
- chunked 请求体。
- `Expect: 100-continue`。
- CONNECT、WebSocket、HTTP/2 和 TLS。
- HTTP 缓存和通用 TCP 转发。

### 6.5 Dashboard

- 原生 HTML/CSS/JavaScript。
- 默认只读，只监听本机管理地址。
- 展示连接、请求、状态码、延迟、路由、上游健康和最近错误。
- 网页控制、配置编辑和认证不在当前范围；若要加入必须重新对齐安全边界。

## 7. 关键工程规则

### 7.1 学习与实现方式

每个里程碑按以下顺序：

1. 只给当前任务说明、接口、约束和验收方法。
2. 用户先独立实现。
3. 卡住时先解释、提示或给伪代码。
4. 再共同完成最小函数。
5. 只有用户明确同意后，Codex 才接管困难部分的完整实现。

讲解新知识时：

- 不降低技术难度。
- 新术语出现前先判断用户是否有学习证据。
- 未学术语必须先解释“是什么、为什么需要、具体例子、项目用途”。
- 不用一串类名或 API 代替教学。

### 7.2 验收方式

- 代码审阅不能代替实际运行。
- 编译成功不能代替正常、边界和故障联调。
- 结论必须附命令、输入、关键输出和环境。
- 严格编译使用 `-Wall -Wextra -Wpedantic`，目标零警告。
- Debug 构建最终通过 ASan/UBSan。
- 性能结果必须记录 QPS、P50/P95/P99、CPU、RSS 和 fd 峰值。

### 7.3 参考项目

- 先独立实现并记录问题，再局部对照 Nginx、muduo 和 libevent。
- 不从头通读，不直接复制大型实现。
- 记录版本、文件、设计结论和许可证影响。

## 8. 当前工作区和风险

阶段 1 提交后的预期 Git 状态：

~~~text
main 分支
工作区无未提交修改
~~~

注意事项：

- 不得使用 `git reset --hard` 或其他方式丢弃未提交讲义修改。
- 本仓库曾因沙箱账户与目录所有者不同触发 Git `dubious ownership`。只使用命令级精确 `safe.directory`，不要擅自修改全局 Git 配置。
- 当前仓库已配置 `user.name=lkdr`、`user.email=lktr-wq@users.noreply.github.com`；只作用于本仓库。基线提交仍保留临时身份 `Codex <codex@local>`。
- 目录已从最初的 `项目二` 重命名为 `反向代理项目`，以后使用新路径。
- `项目计划.md` 中的旧路径和旧目录树名称已于 2026-08-06 统一更新。

## 9. 下一步

阶段 2“阻塞式最小链路”已经通过。阶段 3 尚未开始，下一步只生成 HTTP 增量解析的当前任务说明、接口、约束和验收方法。

当前受控条件：

- 测试上游监听 `127.0.0.1:19080`。
- 后续 EdgeGate 临时监听 `127.0.0.1:18080`。
- 测试请求先限定为不带请求体的单个 `GET / HTTP/1.1`。
- 阶段 2 只处理一个客户端、一个上游和一次请求，不处理并发与 Keep-Alive。
- 测试上游使用 Python 标准库临时启动，只是联调工具，不是 EdgeGate 产品依赖。
- `curl → 测试上游` 已于 2026-08-11 验证：返回 `HTTP/1.0 200 OK`、`Content-Length: 21` 和正文 `edgegate-upstream-ok\n`。
- Windows 继续是唯一权威源码和 Git 仓库；Ubuntu 只保存执行副本及 `/tmp` 下的临时测试数据。

`curl → EdgeGate → 测试上游 → EdgeGate → curl` 已于 2026-08-12 验证：Ubuntu 严格编译零警告；EdgeGate 完整接收并转发 79 字节请求，随后通过固定 1024 字节临时缓冲区持续读取上游，向客户端转发 207 字节完整 HTTP 响应；Python 上游记录 `"GET / HTTP/1.1" 200` 且无 `BrokenPipeError`；curl 退出码为 0，正文精确为 21 字节 `edgegate-upstream-ok\n`，末字节十六进制为 `0a`。当前采用上游关闭连接作为响应结束标志，只适用于本阶段受控 HTTP/1.0 上游。所有 Windows 修改仍按单向流程同步到 Ubuntu 编译、运行和联调。

阶段 2 故障路径已于 2026-08-12 验证：在 `19080` 无监听时，EdgeGate 接收 79 字节请求后由 `connect()` 返回 `Connection refused`，关闭资源并退出；客户端只发送 3 字节 `GET` 后关闭连接时，EdgeGate 输出 `Client closed before sending a complete Header`，没有进入上游连接步骤。两次测试后 `18080`、`19080` 均无残留监听。

当前没有已知技术阻塞。阶段 3 需要先确定 GoogleTest 的安装和接入方式；其他第三方开发包仍按首次使用阶段安装。

## 10. 本文件维护规则

本文件采用覆盖更新，不保存完整历史。以下情况必须更新：

- 完成一个实质任务或里程碑。
- 验收结果由未验证变为通过或失败。
- 项目范围、接口、环境或协作规则发生变化。
- 准备结束当前对话或切换到新对话。
- Git 基线、当前修改和下一步发生变化。

更新时必须：

- 删除或修正已经失效的状态。
- 保留明确的“已验证/未验证”边界。
- 将详细问题过程写入 `01_工程决策与成长记录.md`，不要让本文件膨胀成聊天历史。
