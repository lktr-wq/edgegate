# EdgeGate：Linux C++ 反向代理项目计划

> 文档状态：阶段 6 已通过，下一阶段为阶段 7
>
> 基线日期：2026-08-01
>
> 当前门禁：两份前置文档审阅通过前，不创建代码骨架，不开始业务实现。

## 1. 项目目标

在 `D:\桌面\EI\反向代理项目` 从零实现一个可安装、可配置、可监控的 Linux HTTP/1.1 反向代理产品 EdgeGate。

项目的首要目的不是复刻 Nginx，也不是追求极限性能，而是用可测量的端到端成果掌握：

- Linux C/C++ 应用层网络开发。
- 非阻塞 I/O、`epoll`、定时器、连接状态机和资源生命周期。
- HTTP 增量解析、路由、上游选择、超时、背压和故障恢复。
- 配置、CLI、日志、指标、Dashboard、systemd 和安装交付。
- 单元、集成、故障、压测和长稳验证。

最终成果应能作为简历项目展示，也能让陌生使用者仅依据 README 完成安装、运行、观察和故障复现。

## 2. 已确定范围与假设

### 2.1 技术与环境

- 主体语言：C++17。
- 系统接口：Linux POSIX API。
- 目标环境：Ubuntu 22.04.5、G++ 11.4、CMake 3.22。
- 正式源码：`D:\桌面\EI\反向代理项目`。
- Linux 执行副本：`/home/zy/projects/edgegate`。
- 预计周期：10～12 周，按实际掌握速度推进，不以日历强行压缩质量。
- 本地建立独立 Git 仓库；完成发布审查后再上传 GitHub。

### 2.2 项目边界

- 项目只服务于 Linux C/C++ 与软件工程训练。
- 不绑定硬件、视觉、ROS 2 或具身智能。
- v1 只承诺受控的 HTTP/1.1 产品范围。
- TLS 和通用 TCP 转发不是结项条件。
- Dashboard v1 默认只读；网页控制或配置编辑需重新对齐安全和范围。

### 2.3 协作原则

自 2026-08-13 起，用户授权 Codex 主导后续代码实现，以加快项目推进：

1. 每个大阶段开始前，用通俗语言说明目标、输入、处理、输出和验收标准。
2. 出现会改变架构、范围、安全边界或交付物的抉择时，解释各选项、代价和推荐方案；普通实现细节由 Codex 自主处理。
3. Codex 直接修改 Windows 权威源码，统一完成 Ubuntu 同步、构建、测试和排障。
4. 用户不再逐段输入代码，改为在小阶段或大阶段结束后审阅思路、关键机制、能力边界和实测证据。
5. 由 Codex 直接写入或实质重构的代码、测试、CMake 和脚本使用 `AI-CODE-BEGIN/END` 标记，并登记到 `AI实现索引.md`。
6. 每个大阶段完成后创建一次本地 Git 提交；结项前仍不上传 GitHub。

不得为了追求进度跳过错误处理、测试和证据，也不得把 AI 直接实现虚构成用户独立手写。

## 3. 源码权威性、同步与版本管理

### 3.1 唯一权威副本

Windows 目录是正式源码和 Git 历史的权威来源。Ubuntu 副本用于 Linux 编译、调试和运行。两端不得同时编辑同一文件，以免覆盖修改或形成两个事实来源。

建议单次工作循环：

1. 确认 Windows Git 工作区状态。
2. 将源码同步到 Ubuntu `/home/zy/projects/edgegate`。
3. 在 Ubuntu 完成配置、构建、测试和运行验证。
4. 若为调试临时修改，明确将有效修改同步回 Windows，之后停止修改 Ubuntu 对应文件。
5. 在 Windows 复核差异并提交本地 Git。

项目进入编码阶段前，要选定并实测一种可重复的同步方式，且默认排除：

- `.git/`
- `build/`、Sanitizer 构建目录和压测输出大文件
- 日志、Unix Socket 文件、PID 文件
- 本机密钥、口令、私有地址和其他敏感信息

本地 Git 提供版本历史，但不等同于异机备份。正式远端仓库在结项清理后建立。

### 3.2 提交门禁

每个里程碑至少满足以下条件才提交阶段成果：

- 当前范围的验收命令已运行并保存关键结果。
- 严格编译无新增警告。
- 相关测试通过，失败项有明确记录。
- 文档、配置样例与实现保持一致。
- 不包含构建产物、运行时文件和敏感信息。

提交应按可验证的小步组织，避免把多个阶段压成一次大提交。

## 4. 产品接口

### 4.1 可执行程序

- `edgegate`：后台反向代理服务。
- `edgegatectl`：通过 Unix Socket 管理正在运行的服务。

### 4.2 配置文件

`edgegate.yaml` 配置：

- 服务监听地址与端口。
- 本机管理地址或 Unix Socket 路径。
- Host/路径路由。
- 上游池与后端地址。
- 连接、读写、Keep-Alive 和排空超时。
- 请求、响应及连接缓冲区限制与高低水位。
- 健康检查间隔、成功/失败阈值和探测目标。
- 访问日志、错误日志、级别和轮转参数。
- Dashboard 与状态接口的本机监听参数。

`yaml-cpp` 负责解析 YAML；EdgeGate 负责完整的类型、范围、引用关系、重复项和组合约束校验。

### 4.3 路由规则

- Host 支持精确匹配和受控通配匹配。
- 精确 Host 优先于通配 Host。
- 同一 Host 规则中采用最长路径前缀。
- 每条路由引用一个已定义的上游池。
- 同一路由只在健康上游中轮询。
- 无匹配路由返回 `404`。
- 路由存在但没有健康上游返回 `503`。

通配规则的具体语法、Host 是否包含端口、路径规范化和大小写行为，要在阶段 6 的任务说明中固化为可测试规范。

### 4.4 管理命令

`edgegatectl` 通过 Unix Socket 提供：

```text
edgegatectl status
edgegatectl routes
edgegatectl upstreams
edgegatectl stats --json
edgegatectl reload
edgegatectl drain
edgegatectl stop
```

约定：

- 查询命令默认输出适合人阅读的文本。
- `stats --json` 输出稳定的结构化 JSON。
- 命令失败时打印明确原因，并返回非零退出码。
- `reload` 先加载临时配置，完整校验成功后原子切换；失败保留旧配置。
- `drain` 停止接受新连接或新请求，等待在途请求完成，达到超时后结束剩余连接。
- `stop` 走统一的安全退出路径，不直接粗暴终止进程。

### 4.5 Dashboard

Dashboard 使用原生 HTML/CSS/JavaScript，显示：

- 服务状态和运行时间。
- 当前连接、累计请求和在途请求。
- 状态码分布与代理错误。
- 请求延迟及 P50/P95/P99。
- 路由列表与匹配信息。
- 上游健康、请求、失败和超时。
- 最近错误摘要。

Dashboard 通过 JSON 状态接口读取数据，默认只读并仅监听本机管理地址。v1 不提供网页重载、停止、编辑配置或登录系统。

## 5. HTTP/1.1 支持范围

### 5.1 请求

支持：

- 受控的 HTTP/1.1 请求行和 Header。
- 普通方法，包括 GET、HEAD、POST 等；安全重试只限 GET/HEAD。
- 无正文请求和 `Content-Length` 请求体。
- 同一连接上的顺序 Keep-Alive。

不支持：

- 请求流水线。
- chunked 请求体。
- `Expect: 100-continue`。
- CONNECT、WebSocket、HTTP/2、TLS。

### 5.2 响应

必须正确处理：

- HEAD 或特定状态码对应的无正文响应。
- `Content-Length` 响应。
- `Transfer-Encoding: chunked` 响应。
- 由上游关闭连接定界的响应。

### 5.3 转发行为

- 增量解析请求和响应边界，不能依赖单次读写。
- 统一处理短读、短写、`EAGAIN`、客户端中断和上游中断。
- 按阶段定义并测试 Hop-by-hop Header 处理、Host 与转发信息调整。
- 尚未向客户端返回任何响应字节时，GET/HEAD 才可安全重试另一健康上游。
- 非幂等请求、已经部分发送响应或请求体不可安全重放时，不自动重试。
- 不实现缓存，也不把 EdgeGate 当成通用 TCP 隧道。

### 5.4 错误响应

| 状态码 | 产品语义 |
|---|---|
| `400 Bad Request` | 请求语法或消息边界非法 |
| `404 Not Found` | 无匹配路由 |
| `413 Payload Too Large` | 请求或受控缓冲容量超限 |
| `502 Bad Gateway` | 上游连接、协议或传输失败 |
| `503 Service Unavailable` | 没有健康上游或服务处于拒绝新工作的状态 |
| `504 Gateway Timeout` | 上游连接或响应超时 |

错误响应应包含最小且稳定的正文，不泄露内部路径、配置或敏感系统信息。

## 6. 技术边界与依赖

### 6.1 核心网络层

必须直接使用：

- 非阻塞 Socket。
- `epoll` 水平触发（LT）。
- `timerfd`。
- POSIX 信号与 Unix Domain Socket。
- C++ RAII 和明确的连接所有权。

不得使用 Boost.Asio、muduo 或 libevent 代写事件循环。muduo、libevent 和 Nginx 仅作为分阶段阅读与对照材料。

### 6.2 允许的第三方库

| 依赖 | 用途 | 不负责的部分 |
|---|---|---|
| `yaml-cpp` | YAML 语法解析 | 业务语义校验、原子重载 |
| `spdlog` | 日志格式与输出 | 请求状态机、错误分类、指标定义 |
| `nlohmann/json` | JSON 序列化 | 管理协议和 Dashboard 业务模型 |
| GoogleTest | 单元测试框架 | 集成、故障、压测和长稳测试设计 |

HTTP 增量解析器自行实现受控子集，不引入现成 HTTP 服务器或代理框架替代核心训练内容。

## 7. 目标架构

```text
客户端
  |
  v
监听 Socket -> LT epoll Reactor <-> timerfd
                   |
                   +-> 客户端连接/输入输出缓冲
                   +-> HTTP 请求增量解析器
                   +-> 路由与健康上游选择
                   +-> 上游连接/响应增量解析
                   +-> 超时、背压、重试、资源回收
                   |
                   +-> 指标快照 -> JSON 状态接口 -> Dashboard
                   |
                   +-> 有界日志队列 -> 日志线程

edgegatectl -> Unix Socket 管理通道 -> 状态/重载/drain/stop
                                      |
edgegate.yaml -> 解析与校验 -> 不可变配置快照 --+
```

核心设计约束：

- 网络连接首先由单 Reactor 线程拥有，避免跨线程并发修改连接状态。
- 客户端与上游通过代理会话状态机绑定，但各自有独立 fd、缓冲区和超时。
- 只在确实存在待写数据时关注可写事件。
- 输入/输出缓冲区有硬上限和高低水位。
- 配置重载生成新快照，失败不影响当前快照。
- 日志与指标不能阻塞或破坏网络主循环。

## 8. 计划中的目录结构

以下结构在两份文档审阅通过后才创建：

```text
反向代理项目/
├─ CMakeLists.txt
├─ README.md
├─ LICENSE
├─ cmake/
├─ config/
│  └─ edgegate.yaml
├─ include/edgegate/
├─ src/
├─ apps/
│  ├─ edgegate_main.cpp
│  └─ edgegatectl_main.cpp
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  └─ fault/
├─ test_backends/
├─ web/
├─ scripts/
├─ bench/
├─ packaging/systemd/
└─ docs/
```

实际模块会在相应里程碑开始时根据已验证设计确定，不为追求目录完整而提前创建空模块。

## 9. 10～12 周实施阶段

每个阶段的时间是参考区间。进入阶段时再生成当前任务说明，不提前给出完整实现。

### 阶段 1：文档与环境基线

目标：建立明确范围和可重复的 Linux 开发基线。

交付物：

- `知识衔接.md` 与 `项目计划.md`。
- 独立本地 Git 仓库和首个文档提交。
- 文档审阅后创建最小目录与 CMake 骨架。
- Windows → Ubuntu 单向同步流程及冲突规约。
- Ubuntu 编译器、CMake、依赖和严格编译选项检查记录。

验收：

- 两份文档经用户明确审阅通过。
- 干净构建目录能完成配置、编译和最小测试。
- 明确记录环境版本与依赖来源。
- 业务代码在文档门禁通过前不存在。

### 阶段 2：阻塞式最小链路

目标：以最少机制跑通客户端 → EdgeGate → 单上游 → 客户端。

范围：单客户端、单上游、阻塞 Socket；只作为后续演进基线。

验收：

- 使用 `curl` 转发一条受控请求并得到真实上游响应。
- 以实际字节长度验证读写，不依赖字符串终止符。
- 覆盖上游拒绝连接和客户端提前退出。
- 记录一次抓包或等价的字节流证据。

### 阶段 3：HTTP 增量解析

目标：把任意 TCP 分片稳定还原为受控 HTTP 消息。

范围：请求行、Header、`Content-Length` 请求体、响应四类定界及错误分类。

验收：

- 同一消息一次输入、逐字节输入和随机分片输入得到相同结果。
- 畸形换行、非法长度、冲突 Header、超限数据得到稳定错误。
- 单元测试不依赖真实网络即可复现边界。

阶段阅读：先独立实现，再定向对照 Nginx 请求解析相关代码，不复制大型实现。

### 阶段 4：非阻塞 Reactor

目标：完成 LT `epoll` 事件循环和多客户端并发。

范围：非阻塞监听/连接、连接 RAII、输入输出缓冲、部分写、`timerfd` 基础。

验收：

- 多客户端同时连接和收发，不因一个慢连接阻塞全部连接。
- 读写循环正确处理 `EAGAIN`、EOF、错误和部分写。
- 连接关闭后 fd、缓冲区和事件注册全部释放。
- 严格编译和 ASan/UBSan 基础测试通过。

阶段阅读：带着自己的接口和故障，对照 muduo Buffer/EventLoop/Channel 与 libevent 事件模型。

### 阶段 5：代理状态机

目标：把客户端和上游两端组织成可恢复、可释放的完整会话。

范围：非阻塞上游连接、请求/响应双向转发、顺序 Keep-Alive、关闭定界。

验收：

- 支持连续多轮顺序 Keep-Alive。
- 覆盖上游先关、客户端先关、半关闭、部分响应和异常响应边界。
- 状态转换有单元测试或确定性集成测试。
- 任意失败路径无 fd 泄漏和悬空对象。

### 阶段 6：路由和上游池

目标：从单一硬编码上游演进为可配置代理。

范围：YAML、Host/路径匹配、健康节点轮询、Header 调整、三个测试后端。

验收：

- 三个后端可重复启动并能标识自身。
- 精确 Host、通配 Host、最长路径前缀和无匹配路径均有测试。
- 轮询序列可重复验证。
- 无效配置在启动阶段给出具体位置和原因。

### 阶段 7：可靠性机制

目标：在慢连接和后端故障下保持有界资源与明确行为。

范围：各类超时、缓冲区高低水位、背压、健康检查、故障摘除、安全重试。

验收：

- 慢客户端和慢上游不会导致缓冲区无界增长。
- 健康状态按阈值变化，失败节点被摘除并能恢复。
- GET/HEAD 仅在未返回响应时安全重试。
- 全部上游不健康返回 `503`；上游超时返回 `504`。
- 记录背压触发和恢复的可观察证据。

### 阶段 8：运行时管理

目标：在不中断有效请求的前提下观察和管理服务。

范围：Unix Socket 管理协议、`edgegatectl`、热重载、drain、SIGTERM、安全退出、日志轮转。

验收：

- 所有约定 CLI 命令有确定输出和退出码。
- 有效配置重载后新请求使用新配置。
- 无效配置重载失败且旧配置继续服务。
- drain 停止新工作并完成在途请求，超时行为可验证。
- SIGTERM 与 CLI stop 复用统一关闭流程。

### 阶段 9：可观测与前端

目标：用稳定的 JSON 数据和只读网页观察运行状态。

范围：指标模型、JSON 状态接口、原生 Dashboard、最近错误摘要。

验收：

- 页面展示连接、请求、状态码、延迟、路由和上游健康。
- 指标与人工构造请求结果一致。
- 页面在服务重启、接口暂时失败和空数据时有明确状态。
- 默认只监听本机管理地址，无写操作入口。

### 阶段 10：产品化交付

目标：让陌生使用者能安装、启动、停止和排障。

范围：`cmake --install`、systemd、示例配置、README、用户手册、架构说明、故障排查和许可证。

验收：

- 从干净源码完成配置、编译、测试和安装。
- systemd 启停、重启、日志和 SIGTERM 行为正确。
- 运行用户、配置、日志、运行目录权限最小且明确。
- 文档命令逐条在目标 Ubuntu 环境复现。

### 阶段 11：压测与加固

目标：用故障和长时间数据证明稳定性边界。

范围：Sanitizer、异常输入、慢客户端、后端崩溃、fd 泄漏、并发压测和长稳。

验收：

- Debug 构建通过 ASan/UBSan。
- 健康上游条件下完成至少 100 并发、100,000 请求，代理自身不产生请求丢失或 5xx。
- 记录 QPS、P50/P95/P99、CPU、RSS 和 fd 峰值。
- 至少 12 小时长稳运行，无连接、线程、内存和 fd 持续增长。
- 每个失败结论可由脚本、命令和日志复现。

### 阶段 12：发布

目标：形成可公开、可复现的 v1.0.0 项目。

范围：敏感信息清理、构建产物清理、提交历史整理、实测报告、GitHub 仓库和版本标签。

验收：

- 仓库扫描不包含密钥、口令、私有环境路径和运行时垃圾。
- README 的完整新手路径再次从干净环境验证。
- 发布说明准确列出能力、限制、测试环境和实测数据。
- 打 `v1.0.0` 标签后再上传 GitHub。

## 10. 测试矩阵与最终验收

### 10.1 构建质量

- Ubuntu 22.04.5、G++ 11.4、CMake 3.22 下可从干净源码配置、编译、测试、安装和运行。
- `-Wall -Wextra -Wpedantic` 零警告。
- Debug 构建通过 ASan/UBSan。

### 10.2 单元测试

- HTTP 请求和响应任意分片。
- 路由优先级和冲突处理。
- 配置类型、范围、引用和组合校验。
- 缓冲区边界与高低水位。
- 代理状态机关键迁移。
- Header 删除、保留和补充规则。

### 10.3 集成测试

- 三个上游和轮询。
- 并发客户端。
- 顺序 Keep-Alive。
- chunked 与关闭定界响应。
- 可控短写或等价发送受限场景。
- 客户端提前退出。
- `400/404/413/502/503/504` 全部错误码。

### 10.4 故障测试

- 后端拒绝连接、进程崩溃和慢响应。
- 全部上游不健康。
- 无效配置重载。
- SIGTERM、drain 和排空超时。
- 日志写入异常、连接上限和 fd 紧张场景在安全条件下验证。

### 10.5 性能与长稳

- 至少 100 并发、100,000 请求。
- 健康上游下，EdgeGate 自身不产生请求丢失或 5xx。
- 报告 QPS、P50/P95/P99、CPU、RSS、fd 峰值和测试工具参数。
- 至少 12 小时持续运行，资源无持续增长。
- 性能数字必须附机器、构建类型、配置、请求类型和上游条件，不跨环境做无依据比较。

### 10.6 可使用性

陌生使用者仅依据 README 可以：

1. 安装依赖并构建、测试、安装 EdgeGate。
2. 启动三个测试后端。
3. 配置 Host/路径路由和上游池。
4. 发起请求并观察轮询结果。
5. 打开 Dashboard。
6. 执行全部 CLI 管理命令。
7. 复现至少一组上游故障、无效重载和排空测试。

## 11. 参考项目使用规则

参考资料：

- [Nginx 开发指南](https://nginx.org/en/docs/dev/development_guide.html)
- [Nginx](https://github.com/nginx/nginx)
- [muduo](https://github.com/chenshuo/muduo)
- [libevent](https://github.com/libevent/libevent)

使用规则：

- 按 `知识衔接.md` 的阶段阅读路径使用，不从头通读。
- 先完成自己的最小实现、测试和问题记录，再局部对照。
- 不用参考库替代核心事件循环或 HTTP 解析训练。
- 记录所参考版本、文件、设计结论和许可证影响。
- 不直接粘贴无法解释、无法测试或与当前范围无关的实现。

## 12. 进度记录

状态取值：`未开始`、`进行中`、`待审阅`、`已通过`、`阻塞`。

| 阶段 | 状态 | 开始日期 | 通过日期 | 实测证据/提交 |
|---|---|---|---|---|
| 1. 文档与环境基线 | 已通过 | 2026-08-01 | 2026-08-11 | Ubuntu 验证通过；本地提交 `chore: complete stage 1 development baseline` |
| 2. 阻塞式最小链路 | 已通过 | 2026-08-11 | 2026-08-12 | 本地提交 `feat: complete stage 2 blocking proxy chain`；双向链路和两项故障测试通过 |
| 3. HTTP 增量解析 | 已通过 | 2026-08-12 | 2026-08-13 | 请求28项、响应16项、冒烟1项；Debug与ASan/UBSan均45/45通过；本地提交 `feat: complete stage 3 HTTP incremental parsing` |
| 4. 非阻塞 Reactor | 核心已通过，timerfd 延至可靠性阶段 | 2026-08-13 | 2026-08-15 | 基础/集成16项；用户复核后全量Debug与ASan/UBSan 84/84；本地实现提交 `feat: complete stage 4 nonblocking reactor` |
| 5. 代理状态机 | 已通过 | 2026-08-13 | 2026-08-13 | 真实TCP集成10项；全量Debug与ASan/UBSan 73/73；本地提交 `feat: complete stage 5 proxy state machine` |
| 6. 路由和上游池 | 已通过 | 2026-08-13 | 2026-08-16 | YAML、Header双向调整、三个后端、404/503和代理接入完成；Debug、Sanitizer、Release均94/94；真实轮询链路通过 |
| 7. 可靠性机制 | 未开始 |  |  |  |
| 8. 运行时管理 | 未开始 |  |  |  |
| 9. 可观测与前端 | 未开始 |  |  |  |
| 10. 产品化交付 | 未开始 |  |  |  |
| 11. 压测与加固 | 未开始 |  |  |  |
| 12. 发布 | 未开始 |  |  |  |

### 12.1 阶段 1 实测记录

2026-08-11：

- 用户在 Windows 权威源码中亲手创建 `.gitignore`、`CMakeLists.txt`、两个程序入口和冒烟测试。
- 静态检查发现并修正 3 处 CMake 标识符拼写错误和 1 处源文件路径错误。
- 通过 SSH/SCP 首次复制 9 个正式文件到 Ubuntu `/home/zy/projects/edgegate`；没有复制 `.git`。
- Windows 与 Ubuntu 的 9 个 SHA-256 全部一致。
- 环境版本：G++ 11.4.0、CMake 3.22.1、Git 2.34.1、Make 4.3、GDB 12.1。
- Ubuntu Debug 配置和三个目标构建成功，编译输出零警告。
- 三个目标实际使用 `-g -Wall -Wextra -Wpedantic -std=c++17`。
- CTest `1/1` 通过；两个可执行程序均正常输出基线信息并返回 0。
- `libyaml-cpp-dev`、`libspdlog-dev`、`nlohmann-json3-dev`、`libgtest-dev` 当前未安装，按首次使用阶段安装，不影响阶段 1 最小基线。

### 12.2 阶段 2 实测记录

2026-08-12：

- 用户在 Windows 权威源码中亲手实现阻塞式监听、客户端请求接收、上游连接、`send_all()` 和响应流式回传。
- Ubuntu Debug 全量构建成功且零警告，CTest `1/1` 通过。
- 正常链路中，EdgeGate 接收并转发 79 字节请求，向客户端转发 207 字节完整 HTTP 响应。
- curl 经 `127.0.0.1:18080` 获得 `HTTP/1.0 200 OK` 和精确 21 字节正文 `edgegate-upstream-ok\n`，末字节为 `0a`。
- Python 上游记录 `GET / HTTP/1.1 200`，响应回传完成后不再出现 `BrokenPipeError`。
- `19080` 无监听时，EdgeGate 输出 `create_upstream_connection: Connection refused`并退出，无端口残留。
- 客户端只发送 3 字节 `GET` 后关闭时，EdgeGate 输出 `Client closed before sending a complete Header`，没有连接上游。
- 当前范围仍为单客户端、单上游、单个无请求体 GET；响应以关闭连接定界，不代表阶段 3 的正式 HTTP 解析能力。

### 12.3 阶段 3 实测记录

2026-08-13：

- 新增独立的请求和响应增量解析器；阶段 2 阻塞式链路暂未接入解析器，留到后续代理状态机整合。
- 请求支持请求行、Header、大小写无关查询、必需且唯一的 `Host`、`Content-Length` 请求体和容量上限；明确拒绝 chunked 请求体、`Expect: 100-continue` 和请求流水线。
- 响应支持 HEAD/1xx/204/304 无正文、`Content-Length`、chunked 和关闭连接四种定界；支持 chunk 扩展与 Trailer，并区分提前 EOF 和正常关闭定界。
- 单元测试覆盖单次输入、逐字节输入、每个二分位置、固定种子的 100 轮随机分片、畸形行、冲突长度、非法 Header、超限正文、非法 chunk 和剩余字节。
- Ubuntu Debug 严格编译零警告，CTest `45/45` 通过；独立 ASan/UBSan 构建同样 `45/45` 通过。
- 首轮测试曾发现“无 Header 响应”的边界偏移判断错误；修正空 Header 区与状态行末尾 CRLF 的位置关系后全量回归通过。该失败与修正保留为测试有效性的证据。
- AI 直接实现范围以源码 `AI-CODE-BEGIN/END` 和 `AI实现索引.md` 为准；不计为用户独立手写。

### 12.4 阶段 4 实测记录

2026-08-13：

- 新增 `UniqueFd`，用独占所有权和析构自动关闭管理监听 fd、epoll fd 与连接 fd。
- 新增有硬容量上限的 `ByteBuffer`，消费已处理前缀后可复用空间；溢出时保持原字节不变并明确失败。
- 新增 LT `epoll` 事件循环，将就绪 fd 映射到 `EventHandler` 对象；支持回调内请求删除，并延迟到本轮分派结束后销毁，避免执行成员函数时删除自身。
- 新增阶段验收 Echo 连接与服务器：监听和客户端 Socket 均为非阻塞；`accept4`、`recv`、`send` 循环处理 `EINTR`、`EAGAIN`、EOF、部分写与 `EPOLLRDHUP`。
- 输出缓冲采用 64/48/16 KiB 测试水位验证暂停读和恢复读；512 KiB 字节序列完整回显。
- 集成测试验证 24 个并发客户端、一个未结束慢连接不阻塞快速连接、50 次连接关闭后 `/proc/self/fd` 回到原值。
- 最初使用“2 MiB 数据 + 客户端 4 KiB 接收缓冲”的测试在 20 秒超时；没有出现内容不一致。该输入过度依赖 Linux TCP 窗口恢复计时，调整为 512 KiB 应用水位测试后稳定通过。
- Ubuntu 严格 Debug 构建零警告，全量 CTest `61/61`；ASan/UBSan 全量同样 `61/61`。
- Echo 只证明阶段 4 的网络发动机，不宣称已经具备反向代理状态机；阶段 2 阻塞代理仍保留作行为对照，阶段 5 才整合客户端和上游连接。
- 2026-08-15 用户完成阶段 4 的全部源码阅读与笔记复核。Windows 到 Ubuntu 的 9 个阶段 4 源码文件 SHA-256 全部一致；两个新建的独立 Debug、ASan/UBSan 构建均全量 `84/84` 通过，严格编译零警告。
- 原范围中的 `timerfd` 基础尚未实现，不计入当前成果；它与真实超时状态机一起延至阶段 7 可靠性机制，避免把未实现内容写成阶段 4 证据。

### 12.5 阶段 5 实测记录

2026-08-13：

- 新增 `ProxySession`，让客户端和上游两个 fd 共享同一会话状态；状态依次覆盖读请求、连接上游、发请求、读响应、发响应和关闭。
- 非阻塞 `connect()` 返回 `EINPROGRESS` 后等待可写，并用 `getsockopt(SO_ERROR)` 判断真实连接结果；不把“可写”直接当成连接成功。
- 客户端可在同一连接上顺序发起多轮请求；每轮上游使用新连接，避免在路由和上游池尚未实现前引入上游复用歧义。
- 支持 `Content-Length` 请求体，以及无正文、固定长度、chunked、关闭定界四类响应；响应在受控容量内完整解析后再写客户端。
- 当前返回 `400`、`413` 和 `502`；路由 `404/503`、超时 `504` 分别留到阶段 6/7。
- 真实 TCP 集成测试 10 项覆盖 POST、HEAD、两轮客户端 Keep-Alive、chunked/关闭定界、客户端半关闭、上游拒绝、提前关闭、畸形响应、16 并发会话和 40 次会话 fd 回收。
- 测试初版曾因测试上游成员初始化顺序把内核分配的端口覆盖为 0，导致正常场景全部返回 `502`；修正测试夹具后 10/10 通过。该故障未归因于代理实现。
- 请求和响应解析器增加“Header 上限 + Body 上限”的绝对线长上限，在追加前拒绝超限输入，避免 chunk 元数据或单次大输入先造成无界内存增长。
- Ubuntu 严格 Debug 构建零警告；全量普通 CTest `73/73`，ASan/UBSan `73/73`。
- 当前阶段程序为 `edgegate_proxy_demo`，固定监听 `127.0.0.1:18082` 并转发 `127.0.0.1:19080`；`edgegate` 仍保留阶段 2 对照入口。YAML 路由和正式入口切换尚未完成。

### 12.6 阶段 6 实测记录

2026-08-16：

- 新增 `edgegate.yaml`、完整 YAML 类型/范围/未知字段/引用校验和带文件行列的启动错误；当前配置只暴露监听、容量、上游池和路由，不提前加入尚未实现的超时、健康检查与日志项。
- 上游地址限定为数字 IPv4；DNS 域名、解析缓存和异步解析明确延后，不把可能阻塞 Reactor 的同步解析混入 v1 当前阶段。
- 正式 `edgegate` 已切换为读取 YAML 的非阻塞代理入口；阶段 2 阻塞源码仍保留在 `apps/edgegate_main.cpp`，阶段 5 固定上游程序仍保留为 `edgegate_proxy_demo`。
- `ProxySession` 在完整请求解析后按 Host 与目标路径查询路由；无路由返回 `404`，路由存在但无健康节点返回 `503`，命中后把当轮 IPv4/端口交给原有非阻塞连接状态机。
- 请求侧删除 Hop-by-hop Header 和客户端伪造的 `X-Forwarded-*`，根据真实客户端地址重建 `X-Forwarded-For/Host/Proto`，并让上游连接使用 `Connection: close`。
- 响应侧隔离上游与客户端的连接语义；已完整缓存的 chunked 或关闭定界正文转换为明确 `Content-Length`，上游关闭不再强迫客户端 Keep-Alive 一同关闭。
- 新增可重复启动的 `edgegate_test_backend <name> <port>`。真实进程联调连续六次得到 `backend-a/b/c/a/b/c`，通配 Host 路由命中 `backend-a`，未匹配路径得到 `404`。
- Debug、ASan/UBSan、Release 三套独立构建均严格编译无警告，全量 CTest 均为 `94/94`。
- Header 测试初版用普通子串统计 `Host`，误把 `X-Forwarded-Host` 当成第二个 Host；改为带 CRLF 行边界的完整 Header 断言后，三套构建全量回归通过。该故障属于测试误报，不归因于代理实现。
- 当前“健康”仍是路由表中的状态位；主动探测、失败阈值、超时、背压水位和安全重试属于阶段 7，不能写成阶段 6 已实现能力。

### 12.7 阶段 7 实测记录

2026-08-16：

- 正式 `edgegate` 切换到 `ReliableProxyServer`；旧 `ProxyServer/ProxySession` 原样保留作整包转发对照。
- 请求头和响应头单独解析/改写，`Content-Length`、chunked 和关闭定界正文改为有界流式转发。
- 默认每方向容量/高/低水位为 `262144/196608/65536`；4 MiB 慢客户端集成测试实际产生暂停与恢复计数且字节完整。
- `timerfd` 驱动客户端头、上游连接/响应头、I/O 空闲、请求总时长和 Keep-Alive 空闲超时；100 ms 上游响应头测试得到 504。
- 仅无正文 GET/HEAD 可在客户端收到 0 字节时换一个不同健康节点重试一次；GET 成功重试、POST 禁止重试均有真实 TCP 测试。
- `/health` 主动探测与真实请求被动结果共同维护连续成功/失败计数；阈值摘除得到 503，恢复 2xx 后重新路由成功。
- Debug、ASan/UBSan、Release 全量 `110/110`，严格编译无警告；正式三个后端连续六次为 `backend-a/b/c/a/b/c`。
- 512 KiB POST 通过 32 KiB缓冲完成上传；部分 200 后上游断开时只关闭客户端、不追加 502 的故障边界通过。
- 逐字节 chunk 测试修复 `npos` 长度错误；`strace` 修复旧上游 EOF 污染下一轮 Keep-Alive 状态的缺陷。

每阶段结束后在此追加：

- 实际完成范围和未完成范围。
- 验收命令、关键输出和数据文件位置。
- 发现的缺陷、根因、修复提交。
- 与原计划的偏差及是否需要变更范围。
- 下一阶段开始前必须解决的遗留项。

## 13. 需求与决策记录

| 日期 | 决策 | 原因/影响 |
|---|---|---|
| 2026-08-01 | 项目命名为 EdgeGate，定位为 Linux C++ HTTP/1.1 反向代理 | 突出 Linux 应用层、网络协议和产品工程 |
| 2026-08-01 | Windows 为正式源码，Ubuntu 为构建调试运行副本 | 适配现有环境，同时保持唯一权威来源 |
| 2026-08-01 | 核心事件循环直接使用非阻塞 Socket、LT `epoll`、`timerfd` | 保留 Linux C/C++ 核心训练价值 |
| 2026-08-01 | HTTP 解析器自行实现受控子集 | 学习消息边界与增量状态机，同时控制范围 |
| 2026-08-01 | Dashboard 使用原生前端且 v1 只读、本机监听 | 提供可视化成果，避免过早引入认证和写操作风险 |
| 2026-08-01 | 本地 Git 从第一阶段开始，结项审查后上传 GitHub | 保留学习和迭代历史，公开前统一清理 |
| 2026-08-16 | 阶段 6 配置只暴露已实现能力，新增能力时再扩展同一 YAML | 避免配置项存在但运行时不生效 |
| 2026-08-16 | 上游仅接受数字 IPv4，DNS 域名支持延后 | 避免同步解析阻塞单 Reactor；公网访问与否取决于解析结果和路由，不由“域名”本身决定 |
| 2026-08-16 | EdgeGate 作为信任边界重建 `X-Forwarded-*` 并隔离两侧 Connection | 防止客户端伪造来源信息，避免把一条 TCP 连接的控制语义错误带到另一条连接 |
| 2026-08-16 | 阶段 7 采用真正的双向流式转发，关闭定界响应不重新编码 | 将慢客户端背压传播到上游并保持内存有界；控制 v1 状态机复杂度 |
| 2026-08-16 | 仅无正文 GET/HEAD 在预响应阶段最多换节点一次 | 避免重复非幂等副作用和半条响应后拼接错误消息 |
| 2026-08-01 | 两份前置文档审阅通过前不创建代码骨架 | 先固定知识衔接、范围和验收，避免实现先行造成返工 |
| 2026-08-12 | 用户编码任务细讲；AI 执行的同步、构建和测试按小阶段合并汇报 | 保留学习所需解释，同时减少机械执行过程的碎片化信息 |

后续任何会改变协议范围、核心架构、对外接口、安全边界或最终验收的决定，都先在此记录并重新对齐。

## 14. 文档门禁结果

2026-08-06，用户报告已阅读 `知识衔接.md` 全部内容，并明确同意开始完整阶段 1。前置文档门禁通过。

门禁通过后允许：

- 统一文档路径和状态。
- 创建最小目录、`.gitignore` 和 CMake 基线。
- 建立 Windows → Ubuntu 单向同步流程。
- 检查工具链、依赖、严格编译和最小测试。

阶段 2 及阶段 3 前半段采用“用户亲手输入、AI 审核验证”的学习方式。自 2026-08-13 起改为 AI 主导实现，具体贡献以源码标记、`AI实现索引.md` 和 Git 历史为准。
