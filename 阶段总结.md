# EdgeGate 阶段总结

> 用途：完成一个阶段后，用这份文档回顾该阶段真正做了什么，并作为以后阅读源码的入口。
>
> 写法：不逐行翻译代码，而是以“文件 → 函数或代码块 → 输入、处理、输出”为单位解释，同时记录实际验收和能力边界。
>
> 判断标准：“完成”必须有文档或代码成果；“已验证”必须有真实构建、测试或联调证据。计划中的功能不能写成已经完成。

## 阅读方法

1. 先读“阶段目标”，知道本阶段解决什么问题。
2. 再看整体流程，先建立数据怎样流动的全局认识。
3. 然后以一个函数或完整代码块为单位阅读，不必一开始纠结每一行语法。
4. 最后看验收和能力边界，分清已经证明了什么、还没有证明什么。

---

# 阶段 0：立项、知识衔接与范围冻结

## 0.1 阶段目标

阶段 0 还不写业务代码，先回答：为什么做 EdgeGate、最终做到什么程度、哪些功能明确不做，以及换一个 Codex 对话后怎样继续项目。

最终确定 EdgeGate 是一个用于训练 Linux C/C++ 应用层开发的 HTTP/1.1 反向代理，不绑定硬件、视觉、ROS 2 或具身智能。

## 0.2 `知识衔接.md`

### 这份文档在做什么

它把以前学过的知识连接到反向代理中的实际用途：

- TCP 字节流 → HTTP 请求和响应边界。
- 阻塞 Socket → 非阻塞 Socket、`epoll` 和事件循环。
- RingBuffer/有界队列 → 每条连接的输入、输出缓冲区。
- RAII → Socket fd 和连接对象的生命周期。
- 线程、信号、Unix Socket → 日志线程、安全退出和管理通道。
- CMake、GDB、Sanitizer → 构建、排错和内存检查。

同时补充此前没有实际学习证据的新知识：HTTP/1.1、反向代理、路由、上游、超时、背压、健康检查、热重载、日志、指标、systemd、压测、YAML、CLI、JSON 和 Dashboard。

### 输入、处理、输出

- 输入：以前完成的 Linux/C++ 练习和 EdgeGate 的目标能力。
- 处理：找出旧知识的新用途；新术语出现前先解释是什么、为什么需要和具体例子。
- 输出：一份可以在写代码前直接学习的项目讲义。

### 形成的重要认识

- TCP 只提供有序字节流，一次 `send()` 不对应一次 `recv()`。
- `epoll` 报告哪些 fd 当前可以处理，不负责调度线程。
- Reactor 是组织事件处理代码的思想，不是 Linux API。
- 非阻塞 `send()` 返回 `EAGAIN` 时，说明本机发送缓冲区暂时放不下更多数据，未发送部分必须保留。

## 0.3 `项目计划.md`

### 这份文档在做什么

它冻结产品范围、技术路线、阶段安排和最终验收标准，防止开发中无限增加功能。

### 输入、处理、输出

- 输入：Linux 应用层训练目标和最终形成可安装产品的要求。
- 处理：把大目标拆成阶段，规定交付物和最终测试条件。
- 输出：EdgeGate 的产品合同和实施路线。

### 锁定的核心范围

- C++17 + Linux POSIX API。
- 核心网络层直接使用非阻塞 Socket、LT `epoll`、`timerfd` 和 RAII。
- 自行实现受控的 HTTP/1.1 增量解析器。
- 最终提供 `edgegate`、`edgegatectl` 和 `edgegate.yaml`。
- 支持路由、健康上游轮询、日志、指标、热重载、平滑退出和只读 Dashboard。

### 明确不支持

- 请求流水线、chunked 请求体、`Expect: 100-continue`。
- CONNECT、WebSocket、HTTP/2、TLS。
- HTTP 缓存和通用 TCP 代理。

明确“不支持”同样是产品设计，因为程序遇到这些输入时必须稳定拒绝，不能产生不可预测行为。

## 0.4 `00_项目上下文.md`

这是新对话恢复状态的第一入口，只保存当前仍有效的事实：当前阶段、已实现/已验证/未完成内容、Windows 和 Ubuntu 的职责、Git 状态、环境限制以及下一步。

- 输入：每个里程碑结束后的真实代码、Git 和测试状态。
- 处理：删除过时状态，区分计划、实现和验证。
- 输出：新对话可以快速读取的项目快照。

## 0.5 `01_工程决策与成长记录.md`

它不记录普通聊天，而是保存能体现个人判断的工程闭环：

```text
问题 → 假设 → 证据 → 根因 → 修复 → 验收 → 反思
```

每条记录还区分 AI 提供了什么、我亲自提出或验证了什么，以及结论能否作为简历和面试证据。

- 输入：真正影响正确性、性能、安全、维护或验收可信度的问题。
- 处理：记录判断依据、候选方案、取舍和验证结果。
- 输出：证明我进行过质疑、判断和验证，而不是只接受 AI 结果的材料。

## 0.6 权威源码与执行环境

最终确定：

```text
Windows：D:\桌面\EI\反向代理项目
唯一权威源码 + 唯一 Git 仓库

Ubuntu：/home/zy/projects/edgegate
Linux 编译、运行和调试副本
```

工作流：

```text
Windows 修改源码
        ↓ 单向同步
Ubuntu 编译、测试、调试、运行
        ↓ 发现问题
返回 Windows 修改权威源码
```

不能两端同时修改同一文件，否则会形成两套互相冲突的权威源码。

## 0.7 阶段 0 验收与边界

### 已完成

- 建立并审阅 `知识衔接.md` 和 `项目计划.md`。
- 用户读完知识衔接讲义，并针对不清楚处补充理解。
- 建立跨对话上下文和工程决策记录。
- 冻结 v1 产品范围和实施阶段。
- 创建本地 Git 文档基线提交 `6bbddd9`。

### 尚未证明

- 阶段 0 只完成理解和规划，不代表已经实现 HTTP、非阻塞 Socket、`epoll`、路由或后台服务。
- 阅读过一个概念不等于已经通过代码和测试掌握它。

---

# 阶段 1：建立可编译、可测试、可追踪的 C++ 工程

## 1.1 阶段目标

阶段 1 不追求业务功能，而是先建立可持续开发的工程链路：

```text
源码 → CMake 配置 → Ubuntu 编译 → 程序运行 → CTest 测试 → Git 本地保存
```

## 1.2 `.gitignore` 各代码块

### CMake 构建目录块

忽略 `build/` 和其他构建目录。构建产物能从源码重新生成，不应进入 Git。

### 源码目录中的 CMake 自动文件块

忽略 `CMakeFiles/`、`CMakeCache.txt`、`Makefile` 等，防止误在源码目录构建后污染仓库。

### 编译产物块

忽略 `.o`、`.a`、`.so`、`.dll`、`.exe`。Git 保存可审阅源码，不保存某台机器生成的二进制文件。

### 运行时和编辑器文件块

忽略日志、PID、Unix Socket，以及 VS Code、IDEA、Vim 的本机配置或临时文件。

## 1.3 `CMakeLists.txt` 各代码块

### `cmake_minimum_required`

- 输入：最低 CMake 版本 `3.22`。
- 处理：配置开始前检查工具版本。
- 输出：版本足够才继续，过低则直接报错。

### `project`

- 输入：项目名 `EdgeGate`、版本 `0.1.0`、语言 `CXX`。
- 处理：建立项目身份。
- 输出：后续构建目标都属于这个 C++ 项目。

### C++17 配置块

- 输入：C++17、必须支持、不使用编译器私有扩展。
- 处理：CMake 生成对应的编译参数。
- 输出：所有目标按标准 C++17 编译。

### 严格警告块

判断编译器是 GCC 或 Clang 后加入：

```text
-Wall -Wextra -Wpedantic
```

- 输入：编译器类型。
- 处理：只给兼容的编译器添加这些参数。
- 输出：编译器主动报告可疑代码和不标准写法。

这里形成的重要经验是：CMake 名字拼错后，有时工程仍能构建，但警告参数可能根本没启用。所以不能只看“构建成功”，还要检查真实编译参数。

### `edgegate` 目标

- 输入：`apps/edgegate_main.cpp`。
- 处理：编译并链接服务程序入口。
- 输出：`edgegate` 可执行程序。

阶段 1 的它还没有代理能力，只证明服务目标可以构建和启动。

### `edgegatectl` 目标

- 输入：`apps/edgegatectl_main.cpp`。
- 处理：独立编译管理程序入口。
- 输出：`edgegatectl` 可执行程序。

未来才会实现 `status`、`reload`、`stop` 等命令，阶段 1 只是创建程序目标。

### CTest 块

依次完成：

```text
enable_testing()
→ 生成 edgegate_smoke
→ add_test() 注册到 CTest
```

运行 `ctest --test-dir build` 时，CTest 启动测试程序：返回 `0` 为通过，返回非 `0` 为失败。

## 1.4 三个 `main()` 函数

### `edgegate_main.cpp::main()`

- 输入：无。
- 处理：输出 `EdgeGate baseline`。
- 输出：返回 `0`。

只验证 `edgegate` 的构建目标和入口有效，不含网络功能。

### `edgegatectl_main.cpp::main()`

- 输入：无。
- 处理：输出 `EdgeGate control baseline`。
- 输出：返回 `0`。

证明同一个项目可以生成第二个独立程序。

### `smoke_test.cpp::main()`

- 输入：无。
- 处理：输出最小测试信息。
- 输出：返回 `0`，让 CTest 判定通过。

这是冒烟测试，只证明“找到源码、生成测试、运行测试”的链路没有损坏，不验证代理业务。

## 1.5 Git 身份和版本管理

仓库单独配置：

```text
user.name  = lkdr
user.email = lktr-wq@users.noreply.github.com
```

它只作用于本项目，没有修改全局 Git 身份。结项前只进行本地提交，不上传 GitHub。

## 1.6 阶段 1 验收

- 用户亲手创建并输入 `.gitignore`、`CMakeLists.txt`、两个程序入口和最小测试。
- 修正 4 处 CMake 拼写或路径错误。
- Windows 的 9 个正式文件与 Ubuntu 副本 SHA-256 一致。
- Ubuntu CMake 配置和三个目标构建成功。
- 实际编译参数包含 `-g -Wall -Wextra -Wpedantic -std=c++17`。
- 严格编译零警告，CTest `1/1` 通过。
- `edgegate` 和 `edgegatectl` 正常运行并输出基线信息。
- 本地提交为 `006edb3`。

阶段 1 的成果是：

> 项目已经具备可编译、可运行、可测试、可追踪版本的工程基础。

## 1.7 阶段 1 能力边界

尚未实现 Socket 监听、HTTP、代理转发、非阻塞 I/O、`epoll` 和真实业务测试。

---

# 阶段 2：阻塞式最小反向代理链路

## 2.1 阶段目标

第一次实现真实可运行的闭环：

```text
curl 客户端
  ↓ 127.0.0.1:18080
EdgeGate
  ↓ 127.0.0.1:19080
Python 测试上游
  ↓ HTTP 响应
EdgeGate
  ↓
curl 客户端
```

它是故意保留的阻塞式基线：一次只处理一个客户端、一个固定上游和一次请求/响应。

## 2.2 头文件代码块

### C++ 标准库头文件

- `iostream`：标准输出和错误输出。
- `array`：固定容量临时缓冲区。
- `string`：累计多次收到的请求字节。
- `algorithm`：用 `std::min()` 限制本次读取量。
- `cstddef`、`cstdint`：字节数量和固定宽度整数。
- `cerrno`：读取 `errno`、判断 `EINTR`。
- `cstdio`：使用 `std::perror()`。

### Linux/POSIX 头文件

提供 IPv4 地址结构、网络字节序转换，以及 `socket()`、`bind()`、`listen()`、`accept()`、`connect()`、`send()`、`recv()`、`close()`。

这些头文件不存在于普通 Windows C++ 环境，所以 Windows 编辑器可能爆红；正式代码在 Ubuntu 编译和运行。

## 2.3 匿名 `namespace`

匿名命名空间存放当前 `.cpp` 专用的常量和辅助函数。它当然可以定义函数，含义只是 `create_listener()` 等名字不向其他 `.cpp` 文件公开。

## 2.4 常量代码块

- `kListenPort = 18080`：curl 连接 EdgeGate 的端口。
- `kUpstreamPort = 19080`：EdgeGate 连接测试上游的端口。
- `kListenBacklog = 8`：交给 `listen()` 的等待连接队列提示值，不代表程序支持 8 个并发客户端。
- `kReadChunkSize = 1024`：一次 `recv()` 最多读 1024 字节，不是整个消息的上限。
- `kMaxHeaderSize = 8192`：请求头累计上限，避免恶意或错误客户端让内存无限增长。

## 2.5 `create_listener()`

### 输入

没有参数，固定监听 `127.0.0.1:18080`。

### 处理

```text
socket()
→ setsockopt(SO_REUSEADDR)
→ 填写 sockaddr_in
→ bind()
→ listen()
```

- `socket()` 创建 IPv4 TCP Socket。
- `SO_REUSEADDR` 方便退出后较快重新绑定测试端口。
- `sockaddr_in` 保存 IPv4、18080 和回环地址。
- `bind()` 把 Socket 绑定到本机地址和端口。
- `listen()` 把普通 Socket 转为监听 Socket。

中间失败时关闭已经创建的 fd，避免泄漏。

### 输出

- 成功：监听 fd。
- 失败：`-1`，调用者根据 `errno` 输出原因。

## 2.6 `create_upstream_connection()`

### 输入

无参数，固定连接 `127.0.0.1:19080`。

### 处理

```text
socket() → 填写上游地址 → connect()
```

curl 到 EdgeGate 时，EdgeGate 是服务端；EdgeGate 到上游时，EdgeGate 又是客户端。客户端/服务端身份取决于讨论的是哪一条 TCP 连接。

`connect()` 失败后先保存 `errno`，关闭 fd，再恢复 `errno`，因此调用者仍能输出真正错误，例如 `Connection refused`。

### 输出

- 成功：已经连接上游的 fd。
- 失败：`-1` 并保留失败原因。

## 2.7 `send_all()`

### 输入

- `fd`：向哪条 Socket 发送。
- `data`：数据起始地址。
- `length`：总共必须发送多少字节。

### 解决的问题

一次 `send()` 成功不保证全部发送。例如请求发送 5000 字节，它可能只接受 2000，剩余 3000 必须继续发送。

### 处理

用 `total_sent` 记录累计数量，每轮计算：

```text
起点 = data + total_sent
剩余 = length - total_sent
```

- `EINTR` 表示被信号临时打断，重新调用。
- `MSG_NOSIGNAL` 避免对端关闭时 `SIGPIPE` 直接终止进程。
- 返回 `-1` 时通过 `perror()` 输出系统错误。
- 尚有数据却返回 `0` 时，通过 `std::cerr` 报告程序判断的异常。

### 输出

- 所有字节交给本机 Socket 发送系统：`true`。
- 失败或异常：`false`。

“发送完成”不表示对方应用已经处理数据，只表示本程序已把字节交给本机内核。

## 2.8 `relay_upstream_response()`

### 输入

- `upstream_fd`：EdgeGate 与上游的连接。
- `client_fd`：EdgeGate 与 curl 的连接。

### 处理

```text
从 upstream_fd 读一批
→ send_all() 把本批发送到 client_fd
→ 再读下一批
```

`response_buffer` 只保存当前一批。阻塞式 `send_all()` 处理完本批后，下一次 `recv()` 才覆盖数组，所以不会丢失上一批。最后一批若只有 183 字节，也只发送真实的 183 字节。

阶段 2 没有正式解析响应，采用关闭连接定界：

- `recv() > 0`：收到响应数据。
- `recv() == 0`：上游关闭，响应结束。
- `recv() == -1`：读取失败。

`total_forwarded` 累计响应总转发字节数，供联调核对。

### 输出

- 正常关闭且转发完成：`true`。
- 读取或发送失败：`false`。

## 2.9 `main()` 的功能块

### 1. 创建监听 Socket

调用 `create_listener()` 监听 18080；失败就输出系统错误并退出。

### 2. 等待客户端

阻塞在 `accept()`。curl 到来后取得 `client_fd`。`listen_fd` 代表监听入口，`client_fd` 代表当前客户端连接。

### 3. 准备接收容器

- `buffer`：一次 `recv()` 的 1024 字节临时区。
- `request`：累计多次读取的请求字节。

### 4. 寻找请求头结束标志

```cpp
request.find("\r\n\r\n") == std::string::npos
```

它只判断是否出现 Header 结束标志，是简易边界判断，不是真正解析器。它不能验证请求行、Header、Host、`Content-Length` 和请求体。

### 5. 限制读取容量

本次最大读取量取 1024 和“距离 8192 上限的剩余空间”中的较小值。达到上限仍没找到结束标志就拒绝，防止内存无限增长。

### 6. 处理 `recv()` 结果

- 大于 0：只追加本次真实收到的字节。
- 等于 0：客户端已关闭；Header 不完整则退出且不连接上游。
- 等于 `-1`：系统调用失败，根据 `errno` 输出原因。

TCP 处理的是“地址 + 真实长度”，不能把整个数组都追加，也不能依赖 `\0`。

### 7. 输出请求

`std::cout.write(request.data(), request.size())` 按明确长度输出，不寻找 `\0`。

### 8. 连接并发送到上游

调用 `create_upstream_connection()`，再用 `send_all()` 原样转发请求，没有改写 Host 或其他 Header。

### 9. 转回响应

调用 `relay_upstream_response()`，完成“上游 → 临时缓冲区 → curl”的循环转发。

### 10. 释放 fd

关闭 `upstream_fd`、`client_fd`、`listen_fd`。此时尚未用 RAII，所以每条成功和错误路径都要手动 `close()`。

## 2.10 `perror()` 和 `std::cerr`

系统调用失败并设置 `errno` 时使用 `perror()`，例如 `socket()`、`connect()`、`send()`、`recv()` 失败。

程序依据自身规则判断错误时使用 `std::cerr`，例如 Header 超限、客户端提前关闭、`send()` 异常返回 0。

结论：

> 输出方式由错误来源决定。程序自己判断的错误不能随便读取可能已经过期的 `errno`。

## 2.11 Python 测试上游

Python 标准库临时启动 `127.0.0.1:19080`，返回：

```text
edgegate-upstream-ok\n
```

它只是联调工具，不是产品依赖。先验证 `curl → 上游`，再验证 `curl → EdgeGate → 上游`，失败时才能区分是上游问题还是代理问题。

## 2.12 阶段 2 验收

### 正常链路

- Ubuntu 严格编译零警告。
- EdgeGate 接收并向上游转发 79 字节请求。
- EdgeGate 向 curl 转发 207 字节完整响应。
- 正文精确为 21 字节 `edgegate-upstream-ok\n`，末字节是 `0a`。
- Python 上游记录 `GET / HTTP/1.1 200`。
- curl 退出码为 0，无 `BrokenPipeError`。

### 故障链路

- 19080 无监听时，`connect()` 返回 `Connection refused`，程序释放资源后退出。
- 客户端只发送 3 字节 `GET` 就关闭时，EdgeGate 判断 Header 不完整，不连接上游。
- 测试结束后 18080、19080 均无残留监听。

### Git

阶段 2 本地提交为 `e63e6c1`。

## 2.13 阶段 2 能力边界

已做到：

```text
一个客户端 → 找到 Header 结尾 → 固定上游 → 完整转发请求
→ 分批转发响应 → 上游关闭后结束
```

尚未做到：

- 真正解析 HTTP 请求和响应。
- `Content-Length` 请求体和畸形请求分类。
- Keep-Alive、chunked 响应。
- 多客户端并发、非阻塞 I/O、`epoll`。
- 路由、上游池、超时、背压、健康检查。
- RAII 自动管理 fd。

## 2.14 与阶段 3 的连接

阶段 2 的 `find("\r\n\r\n")` 只能回答“有没有看到 Header 结束标志”。

阶段 3 的增量解析器需要进一步回答：请求行和 Header 是否合法、Host 是否存在且唯一、请求体还缺多少字节、当前是需要更多数据/完整/错误，以及不支持的 HTTP 功能能否被明确拒绝。

因此阶段 3 不是推翻阶段 2，而是把“找四个字符”升级为真正理解 HTTP 消息结构的独立模块。

---

# 阶段 3：HTTP 请求与响应增量解析

## 3.1 阶段目标

阶段 2 只用 `find("\r\n\r\n")` 寻找 Header 结束标志。这个办法知道“Header 的字节似乎收齐了”，但不知道：

- 请求行、状态行和每个 Header 是否合法。
- `Content-Length` 后面还应接收多少正文。
- 当前是数据暂时不够，还是已经出现协议错误。
- 响应采用无正文、固定长度、chunked 还是关闭连接定界。

阶段 3 把这段临时判断升级成两个独立的增量解析器：

- `RequestParser`：解析客户端发来的 HTTP/1.1 请求。
- `ResponseParser`：解析上游返回的 HTTP/1.x 响应。

“增量”是指一次可以只交给解析器任意一小段字节。解析器保存已经收到的内容，等后续字节到来后从原来的进度继续，而不是要求一次 `recv()` 就得到完整消息。

## 3.2 整体输入、处理和输出

### 请求方向

```text
任意大小的 TCP 字节分片
→ consume() 累计到 buffer_
→ 解析请求行
→ 找到并逐行解析 Header
→ 组合校验 Host、Content-Length 等规则
→ 按 Content-Length 等待正文
→ 返回需要更多数据 / 完成 / 错误
```

### 响应方向

```text
任意大小的上游响应分片
→ consume() 累计到 buffer_
→ 解析状态行和 Header
→ 选择四种正文边界之一
→ 按相应规则判断正文结束位置
→ 返回需要更多数据 / 完成 / 错误
```

这两个解析器不负责调用 `recv()`，只负责解释调用者交进来的字节。网络读写和协议解析因此可以分别测试。

## 3.3 公共类型 `message_types.h`

### `ParseStatus`

它表示解析工作的三种结果：

- `kNeedMoreData`：目前收到的字节都没有证明消息错误，但还不足以组成完整消息。
- `kMessageComplete`：当前消息已经完整，可以读取解析结果。
- `kError`：消息违反当前项目支持的协议规则，继续追加字节也不能把它修好。

这个三态结果解决了阶段 2 无法区分“暂时没收完”和“已经非法”的问题。

### `HeaderField`

它用两个字符串保存一个 Header 的名称和值。例如：

```text
Host: example.com
```

会保存为 `name == "Host"`、`value == "example.com"`。解析器保留 Header 的原始顺序，同时在查询和规则判断时按 HTTP 要求进行大小写不敏感比较。

## 3.4 请求解析器 `RequestParser`

### 构造函数

构造时记录 Header 和 Body 容量上限，并计算整条消息允许占用的最大空间。默认 Header 最多 8192 字节，Body 最多 1 MiB。

容量限制不是优化，而是安全边界：慢客户端或恶意客户端不能让一个连接的字符串无限增长。

### `parse_request_line()`

输入是一条不包含结尾 `\r\n` 的请求行，例如：

```text
GET /users?id=1 HTTP/1.1
```

函数完成以下工作：

1. 找到两个空格，将请求行拆成方法、请求目标和版本。
2. 确认三段都非空，且没有多余空格破坏结构。
3. 校验方法只能由合法 token 字符组成。
4. 校验请求目标不能包含控制字符或空格。
5. 当前版本只接受 `HTTP/1.1`。
6. 全部通过后才把结果保存到成员变量。

输出是具体的 `ParseError`，成功返回 `kNone`。

### `parse_header_line()`

输入是单独一行 Header。它先找第一个冒号：冒号左边是名称，右边是值；值两端允许出现的空格会被去掉。

随后它分别检查 Header 名称和值，拒绝空名称、折叠行、非法控制字符等内容，最后把合法结果追加到 `headers_`。

只找第一个冒号很重要，因为 Header 的值本身还可以包含冒号，例如 `Host: 127.0.0.1:19080`。

### `parse_header_fields()`

这个函数在完整 Header 区域内循环寻找每一行的 `\r\n`，截出单行并交给 `parse_header_line()`。它负责“遍历”，单行函数负责“校验和保存”。

这样拆分后，错误可以准确定位为某一行格式问题，而不是把整块 Header 当成一个大字符串模糊处理。

### `validate_message_headers()`

单独一行合法，不代表多行放在一起就合理。这个函数负责跨 Header 的组合规则：

- HTTP/1.1 必须恰好有一个有效 `Host`。
- `Content-Length` 必须是只含十进制数字且不溢出的长度。
- 多个 `Content-Length` 只有值完全相同时才接受；不同值会产生边界歧义并被拒绝。
- 请求体不能超过配置上限。
- v1 明确拒绝 chunked 请求体和 `Expect: 100-continue`。

检查多个 `Content-Length` 时，`parsed_content_length` 保存“前面已经见过的值”，局部变量 `length` 保存“当前这一行解析出的值”。两者不同就说明同一请求给出了互相矛盾的正文长度。

### `consume()`

这是请求解析器的总入口。每次传入新分片后，它按当前进度推进：

1. 已经完成或出错时直接返回原状态，避免重复解析。
2. 先判断追加后是否会超过总容量。
3. 请求行尚未完成时寻找第一个 `\r\n` 并调用 `parse_request_line()`。
4. Header 尚未完成时寻找 `\r\n\r\n`，逐行解析并做组合校验。
5. 计算 `message_bytes_ = header_bytes_ + content_length_`。
6. 字节不足时返回 `kNeedMoreData`；正好收齐时返回完成。
7. 完整请求后仍有多余字节时，按当前范围把它识别为不支持的请求流水线。

因此，完整请求行已经收齐时，方法、目标和版本可以先被解析出来；但 Header 未结束和校验前，整条请求仍然是 `kNeedMoreData`。

### 查询函数

`method()`、`target()`、`version()`、`body()` 和 `headers()` 返回已经解析的字段；`header_value()` 用大小写不敏感方式查询第一个同名 Header。

`raw_message()` 返回当前完整请求实际占用的原始字节，后续代理状态机可以把它转发给上游。它不把流水线中的后续字节误算进当前消息。

## 3.5 响应解析器 `ResponseParser`

响应解析器比请求解析器复杂，因为 HTTP 响应有四种正文结束规则。构造函数还需要记住原请求方法；仅看响应状态行无法判断它是不是 HEAD 请求的响应。

### `parse_status_line()`

输入类似：

```text
HTTP/1.1 200 OK
```

函数拆出 HTTP 版本、三位状态码和可为空的原因短语，接受项目支持的 HTTP/1.0 与 HTTP/1.1 响应，并拒绝非法状态码和控制字符。

### `parse_header_line()` 与 `parse_header_fields()`

它们和请求侧采用相同的“逐行拆分、逐项校验”思路，但 `parse_header_line()` 可以把结果写入普通 `headers_` 或 chunked 结束后的 `trailers_`。

Trailer 是零长度块之后、最终空行之前的附加 Header。为防止它改变已经确定的消息边界，当前实现拒绝 Trailer 中的 `Content-Length`、`Transfer-Encoding` 和 `Host`。

### `select_body_mode()`

Header 全部解析后，这个函数先校验 `Content-Length` 与 `Transfer-Encoding` 是否矛盾，再按协议规则选择一种 `ResponseBodyMode`：

1. `kNoBody`：HEAD 响应，或状态码为 1xx、204、304；Header 结束就是消息结束。
2. `kContentLength`：读取 `Content-Length` 指定的准确字节数。
3. `kChunked`：逐块读取，直到零长度块和 Trailer 最终空行。
4. `kCloseDelimited`：没有其他长度信息，只能把上游关闭连接当作正文结束。

成员 `body_mode_` 构造时的默认值只是占位；只有 Header 解析并通过 `select_body_mode()` 后，才代表真实选择结果。

### `parse_body()`

它按已经选定的模式处理正文：

- 无正文模式立即完成。
- 固定长度模式等待 `header_bytes_ + content_length_` 个字节。
- chunked 模式转交给 `parse_chunked_body()`。
- 关闭定界模式仅累计和检查容量，不能仅凭当前字节判断完成。

`remaining_bytes()` 表示当前响应完成位置之后已经多收到的字节。响应侧保留它，是因为上游连接上可能先出现信息响应，或者同一批读取中已经带入后续响应数据；如何处理剩余字节由调用者决定。

### `parse_chunked_body()`

chunked 没有整段正文总长度，而是重复出现：

```text
十六进制块长度\r\n
该长度的正文数据\r\n
```

解析器使用三个内部状态推进：

- `kSizeLine`：等待一整行十六进制块长度。
- `kData`：已经知道长度，等待对应正文和紧随其后的 `\r\n`。
- `kTrailers`：已经读到零长度块，逐行解析零个或多个 Trailer，直到最终空行。

`chunk_position_` 记录下一次从 `buffer_` 的哪个位置继续；`current_chunk_size_` 记录当前块长度；`decoded_chunked_body_` 只拼接去掉块长度行和分隔符后的真实正文。

原始响应仍由 `raw_message()` 保留。例如两个块 `Wiki` 和 `pedia` 会在 `body()` 中得到 `Wikipedia`，而 `raw_message()` 仍保留可用于转发的 chunked 编码字节。

### `notify_eof()`

普通 `consume()` 只能看到字节，不能自行知道 TCP 连接是否关闭。网络层在 `recv()` 返回 0 时调用 `notify_eof()`：

- 关闭定界响应把 EOF 当作正常结束标志。
- Header 尚未完成，或固定长度/chunked 正文尚未完成时收到 EOF，返回 `kUnexpectedEof`。
- 响应已经完成时再通知 EOF，不改变原来的完成状态。

这把“没有更多字节暂时到达”和“以后永远不会再有字节”明确区分开。

## 3.6 测试文件在验证什么

### `http_request_parser_test.cpp`

请求测试覆盖：

- 一次输入完整请求、请求行提前解析和 Header 结束符跨分片。
- 带 `Content-Length` 正文、逐字节、全部二分位置和固定种子随机分片。
- Host 缺失/重复、错误请求行、错误版本和非法 Header。
- Header/Body 超限、非法或冲突 `Content-Length`。
- chunked 请求体、`Expect: 100-continue` 和流水线的明确拒绝。
- Header 名称大小写不敏感查询以及原始消息读取。

### `http_response_parser_test.cpp`

响应测试覆盖：

- 固定长度、无正文和关闭定界三类响应。
- chunked 一次输入、逐字节、全部二分位置和随机分片。
- chunk 扩展、Trailer、非法块长度、非法块结尾和非法 Trailer。
- Header/Body 超限、冲突长度信息、提前 EOF 和响应后的剩余字节。

每个测试上方的中文注释说明“输入场景”和“预期结果”，阅读时先看注释，再看构造的原始 HTTP 文本，最后看 `EXPECT_*` 如何证明预期。

## 3.7 用户复核中形成的关键判断

- 能准确区分阶段 2 的边界搜索与阶段 3 的协议解析。
- 理解 `parsed_content_length` 是历史值、`length` 是当前值，因此可以发现多个不同的 `Content-Length`。
- 理解响应必须依据原请求方法、状态码和 Header 选择四种正文边界。
- 理解 chunked 状态机中的长度行、数据、块结尾、零块、Trailer 和最终空行。
- 理解 `raw_message()` 保留协议原始字节，而 chunked 的 `body()` 返回解码后的真实正文。
- 已在请求和响应解析器源码中添加自己的中文笔记；AI 对其中容易造成误解的表述进行了复核，未把 AI 主导实现记成用户独立手写。

## 3.8 阶段 3 验收

阶段 3 最初独立完成时，普通 Debug 和 ASan/UBSan 测试已经通过。用户完成源码笔记后，2026-08-14 又把 Windows 权威源码单向同步到 Ubuntu，并从两个独立构建目录重新验证：

- 环境：Ubuntu 22.04.5、G++ 11.4.0、GoogleTest 1.11.0。
- 普通 Debug：严格编译没有出现 `-Wall -Wextra -Wpedantic` 警告，全量 CTest `84/84` 通过。
- ASan/UBSan：启用 AddressSanitizer、UndefinedBehaviorSanitizer 和泄漏检查，全量 CTest `84/84` 通过。
- 当前解析器测试共 46 个：请求解析 29 个、响应解析 17 个，全部通过。
- `git diff --check` 通过，AI 标记完整；本次源码差异仅为用户学习笔记、测试场景说明和注释排版，没有新增业务逻辑。

源码中的 `AI-CODE-BEGIN/END` 标记继续保留，用于区分 AI 直接实现的解析器与用户后续添加、修正的学习笔记。

## 3.9 阶段 3 能力边界

已经实现并验证的独立能力：

- 任意分片下解析受控 HTTP/1.1 请求。
- `Content-Length` 请求体。
- 四类响应正文边界。
- chunked 响应解码与 Trailer 校验。
- 容量限制和明确错误分类。

当前阶段仍不负责：

- Socket 的非阻塞读写和多个连接并发。
- 把解析器接入客户端与上游连接状态机。
- 路由、上游选择、超时、背压和健康检查。
- chunked 请求体、请求流水线、`Expect: 100-continue`、CONNECT、WebSocket、HTTP/2 和 TLS。

## 3.10 与阶段 4 的连接

阶段 3 解决“收到的字节是什么意思、消息何时完整”。阶段 4 要解决“同时管理许多 Socket 时，哪些 fd 现在可以读写，以及未处理完的字节保存在哪里”。

因此下一次源码阅读从阶段 4 的三个基础部件开始：

```text
UniqueFd：谁负责关闭 fd
ByteBuffer：未解析或未发送完的字节放在哪里
EventLoop：epoll 返回事件后怎样分派给连接对象
```

先掌握这三个基础部件，再阅读 EchoConnection 和 ReactorEchoServer，才能清楚看见解析器未来如何放进真正的非阻塞连接中。

# 阶段 4：非阻塞 Reactor

## 4.1 阶段目标

阶段 3 已经能解释一段 HTTP 字节的含义，但如果仍然对每个客户端执行阻塞 `recv()`，一个迟迟不发完数据的客户端就可能卡住整个服务。

阶段 4 要解决的是：

> 用一个事件循环同时管理多条非阻塞连接，只处理当前能够推进的 fd，并把没有读完、处理完或发送完的字节安全保存在应用层。

这一阶段使用 Echo 服务验收网络发动机。Echo 收到什么就回发什么，故意不加入 HTTP 和反向代理业务，便于单独验证 Reactor、缓冲、部分写、背压和资源释放。

## 4.2 整体输入、处理和输出

```text
客户端 connect()
    ↓
Linux 内核完成 TCP 握手，把连接放入监听 Socket 的已完成连接队列
    ↓
监听 fd 出现 EPOLLIN
    ↓
EventLoop 根据 fd 找到 ListenerHandler
    ↓
ListenerHandler::on_event() 循环 accept4()，直到 EAGAIN
    ↓
每个已连接 fd 包装成 UniqueFd 和 EchoConnection，再注册进同一个 EventLoop
    ↓
连接 fd 出现 EPOLLIN
    ↓
EchoConnection::read_from_peer() 循环 recv()
    ↓
input_ 保存已接收、尚未处理的字节
    ↓ Echo 业务原样复制
output_ 保存准备回发、尚未被 send() 接受的字节
    ↓
立即尝试 send()；若短写或 EAGAIN，保留剩余字节并订阅 EPOLLOUT
    ↓
连接 fd 再次可写后继续发送，成功多少就 consume() 多少
    ↓
对端关闭且 output_ 已清空，或发生致命错误
    ↓
EventLoop 取消注册并延迟销毁 EchoConnection
    ↓
UniqueFd 析构，自动 close(fd)
```

## 4.3 `UniqueFd`：确定谁负责关闭 fd

### 构造函数和析构函数

`UniqueFd(int fd)` 接管一个裸 fd。析构函数调用 `reset()`，因此对象离开作用域时自动关闭仍由它拥有的 fd。

它禁止复制，避免两个对象都认为自己应该关闭同一个 fd；允许移动，把唯一所有权从旧对象转给新对象。

### `get()`、`release()` 和 `reset()`

- `get()`：只查看 fd，不改变所有权。
- `release()`：交出 fd，当前对象变成无资源状态，调用者开始负责关闭返回值。
- `reset(new_fd)`：先关闭当前旧 fd，再接管新 fd；无参数时只关闭并清空。

在本阶段的释放链中：

```text
EventLoop 删除 EventHandler
→ EchoConnection 析构
→ socket_ 析构
→ UniqueFd 自动关闭连接 fd
```

## 4.4 `ByteBuffer`：保存应用还没有处理完的字节

Linux 内核 Socket 缓冲区不能代替应用缓冲区。`recv()` 一旦返回，字节已经被应用取出；内核不会替 HTTP 解析器记住“这是半条请求”。`send()` 没有接受的尾部字节也从未进入内核，只能由应用保存。

### `append()`

先用 `writable_capacity()` 判断加入后是否超过硬上限。容量不足时返回 `false`，并保持原数据不变。如果底层字符串仍保留大量已消费前缀，先 `compact()`，再把新字节追加到尾部。

### `consume()`

表示前 `count` 个可读字节已经处理，不立即为每次消费移动整个字符串。全部消费完时 `clear()`；已消费前缀足够大且至少占物理存储一半时才 `compact()`，这是减少频繁内存搬移的工程折中。

### `readable_view()` 和容量查询

`readable_view()` 返回“从 `read_offset_` 开始、长度为 `readable_size()`”的只读视图。`writable_capacity()` 按未消费的有效字节计算，因为已消费前缀以后可以回收。

## 4.5 `EventLoop`：等待并分派 fd 事件

### `EventHandler`

这是所有处理对象共同实现的接口：

- `fd()`：我管理哪个 fd。
- `interests()`：我希望 `epoll` 关注哪些事件。
- `on_event()`：这些事件真正发生后我怎样处理。

`ListenerHandler` 和 `EchoConnection` 都实现这个接口，所以 EventLoop 可以统一管理它们。

### `EventLoop()`

调用 `epoll_create1(EPOLL_CLOEXEC)` 创建内核 epoll 实例，并由 `UniqueFd epoll_fd_` 管理其生命周期。epoll fd 不是网络连接，只是 EdgeGate 操作这个内核 epoll 实例的句柄。

### `add()`

先通过 `epoll_ctl(EPOLL_CTL_ADD)` 把 fd 和关注事件注册进内核，再把 `fd -> unique_ptr<EventHandler>` 保存进 `handlers_`。如果保存对象失败，会撤销刚完成的内核注册，保证内核记录与应用对象表一致。

### `modify()`

连接状态变化后重新计算关注事件。例如 `output_` 出现待发送数据时加入 `EPOLLOUT`；发送完毕后取消它；背压暂停读取时暂时取消 `EPOLLIN`。

### `remove()` 和 `finish_pending_removals()`

回调正在执行时不能立即删除当前对象，否则相当于对象执行成员函数到一半删除自己。`remove()` 先从 epoll 取消注册，并把 fd 放入待删除集合；本轮全部回调返回后，`finish_pending_removals()` 才真正从 `handlers_` 删除对象。

### `run_once()` 和 `run()`

`run_once(timeout_ms)` 调用一次 `epoll_wait()`，遍历返回的就绪 fd，根据映射找到对象并调用 `on_event()`。`run()` 则循环调用它，直到 `stop()` 设置停止标志。

## 4.6 `EchoConnection`：推进一条已连接 Socket

### 构造与析构

构造函数接管客户端 fd、共享统计对象和缓冲区水位，并验证 `low < high <= maximum`。连接建立和关闭时分别更新活动连接统计；`socket_` 最终由 `UniqueFd` 自动关闭。

### `interests()`

关注事件不是固定值，而是根据状态生成：

- 还能读取且未被背压暂停：加入 `EPOLLIN`。
- `output_` 有待发送字节：加入 `EPOLLOUT`。
- 始终关注 `EPOLLRDHUP`，识别对端关闭发送方向。

### `on_event()`

它是单条连接的总调度函数：先记录错误事件；即使同时收到 `EPOLLIN | EPOLLRDHUP`，也先读取 FIN 之前可能到达的最后一批数据；随后尝试写出、刷新背压状态，并决定删除连接还是通过 `modify()` 更新关注事件。

### `read_from_peer()`

循环非阻塞 `recv()`：

- `> 0`：只处理本次真实收到的字节数；Echo 将其从 `input_` 原样复制到 `output_`。
- `== 0`：对端已关闭发送方向。
- `EINTR`：系统调用被信号打断，重新调用。
- `EAGAIN/EWOULDBLOCK`：本地内核接收缓冲区当前已读空，本轮正常结束。
- 其他错误：标记为不可恢复错误。

### `write_to_peer()`

循环发送 `output_` 的可读部分。`send()` 成功多少就 `consume()` 多少；短写后的尾部仍留在 `output_`。遇到 `EAGAIN` 时等待下一次 `EPOLLOUT`，而不是丢弃或忙等。`MSG_NOSIGNAL` 避免对端关闭时由 `SIGPIPE` 直接终止进程。

### `refresh_backpressure_state()`

当 `output_` 达到高水位时暂停继续读取，避免慢客户端让应用内存无限增长；当积压发送到低水位后恢复读取。它是应用层负反馈，更接近流量控制，不等同于 TCP 根据网络拥塞调整发送速率的拥塞控制算法。

## 4.7 `ReactorEchoServer`：组装可运行服务器

### `create_listener()`

主线仍是阶段 2 熟悉的 `socket -> setsockopt -> bind -> listen`，但监听 Socket 创建时同时加入 `SOCK_NONBLOCK | SOCK_CLOEXEC`。端口传 `0` 时由内核选择空闲端口，再用 `getsockname()` 和 `ntohs()` 取得实际端口，方便自动测试。

### `ListenerHandler::on_event()`

监听 fd 的 `EPOLLIN` 表示已完成连接队列中有连接可取，不表示业务正文到达。处理器循环调用 `accept4()`：成功时为刚取出的已连接 fd 创建 `EchoConnection`；`EINTR` 重试；`EAGAIN` 表示队列已经取空并正常返回。

### `ReactorEchoServer` 构造函数

C++ 会先按照头文件中的声明顺序构造成员：`loop_ -> stats_ -> port_`，随后才执行构造函数体。因此函数体内可以使用已经构造完成的 `loop_.add()` 注册 `ListenerHandler`；构造函数结束后，服务器对象才算完整建立。

## 4.8 本阶段形成的关键判断

- `epoll` 位于运行 EdgeGate 的 Linux 内核中，不在客户端，也不是另一个服务器。
- fd 就绪表示对应 I/O 操作当前可能取得进展，不表示客户端线程或 EdgeGate 线程被程序推进到运行态；线程调度由 Linux 调度器负责。
- 应用代码主动通过 `epoll_ctl()` 注册 fd；Socket 不会自己向 epoll 申请资源。
- `epoll` 解决“哪些 fd 有事”，Reactor 解决“找到对应对象后，代码怎样组织和分派”。
- `input_` 保存已经 `recv()` 但尚未完成应用处理的字节；`output_` 保存准备发送但尚未被 `send()` 接受的字节。
- 监听 fd 始终负责接连接，`accept4()` 另外返回已连接 fd；同一个客户端程序也可以建立多条 TCP 连接。
- 所谓“新连接”只是“刚完成并尚未被 `accept4()` 取出的 TCP 连接”，不是新类型的客户端。

## 4.9 测试与复核证据

阶段 4 自身共 16 项测试：

- `ByteBuffer` 5 项：追加/消费/复用、溢出不破坏原数据、越界消费、清空和零容量拒绝。
- `UniqueFd` 3 项：作用域自动关闭、移动转移唯一所有权和 `release()` 放弃自动关闭。
- `EventLoop` 3 项：fd 就绪分派与回调内安全删除、等待超时和重复 fd 拒绝。
- Reactor Echo 集成 5 项：顺序消息、24 客户端并发、慢连接隔离、512 KiB 背压完整传输和连续 50 次连接后的 fd 回收。

每个测试样例上方都已有中文场景说明。阶段实现最初于 2026-08-13 在 Ubuntu 完成普通与 Sanitizer `61/61` 验证。

用户完成源码阅读和笔记复核后，2026-08-15 又把 Windows 权威源码单向同步到 Ubuntu，并从两个新的独立目录重新验证：

- 环境：Ubuntu 22.04.5、Linux 6.8、G++ 11.4.0、CMake 3.22.1、GoogleTest 1.11.0。
- 普通 Debug：严格编译未出现 `-Wall -Wextra -Wpedantic` 警告，全量 CTest `84/84` 通过。
- ASan/UBSan：启用地址、未定义行为和泄漏检查，全量 CTest `84/84` 通过。
- Windows 与 Ubuntu 的 9 个阶段 4 源码文件 SHA-256 全部一致。
- `git diff --check` 通过，网络层 9 组 `AI-CODE-BEGIN/END` 标记首尾一致。
- 本次源码差异为用户学习笔记和注释修正，没有新增阶段 4 业务功能；AI 主导实现的边界继续保留。

## 4.10 能力边界和下一阶段

阶段 4 已经能够证明：

- 一个 LT `epoll` EventLoop 可以管理监听 fd 和多条连接 fd。
- 非阻塞读写能够正确处理 `EINTR`、`EAGAIN`、EOF、部分写和连接回收。
- 应用缓冲区硬上限、高低水位背压和 RAII 资源释放有效。
- 一个慢连接不会阻塞其他已就绪连接。

阶段 4 仍然不能证明：

- EchoConnection 具备 HTTP 解析或反向代理能力。
- 客户端连接与上游连接已经组成代理状态机。
- Keep-Alive、四类响应边界和明确代理错误码已经接入真实 Socket。
- `timerfd` 超时机制已经实现；它仍在后续可靠性阶段范围内，当前成果不能写成已完成。

阶段 5 将把 Echo 的单端链路替换为双端代理会话：

```text
客户端 fd ←→ ProxySession ←→ 上游 fd
```

此时阶段 3 的 HTTP 解析器负责判断消息边界，阶段 4 的 EventLoop、缓冲区和非阻塞读写负责推进两端 I/O。

---

# 阶段 5：非阻塞代理状态机

## 5.1 这一阶段解决什么

阶段 4 的 Echo 只有“客户端一端”：收到什么就回什么。阶段 5 增加“上游一端”，让一次请求真正完成：

```text
客户端 → EdgeGate 读请求 → EdgeGate 连接并发送给上游
       ← EdgeGate 写响应 ← EdgeGate 读取并解析上游响应
```

难点不是调用两次 Socket，而是客户端和上游随时可能只读/写一部分，所以必须保存“当前做到哪一步”。这个进度就是代理状态机。

## 5.2 主要对象

- `ProxyServer`：创建监听 Socket，接受客户端连接，为每条连接建立会话。
- `ProxyEndpoint`：让客户端 fd 和上游 fd 都能注册进同一个 `EventLoop`，并标明事件来自哪一端。
- `ProxySession`：一条客户端连接的总协调者。它保存请求解析器、响应解析器、两个方向的待发送缓冲区和当前状态。
- `ProxyState`：用枚举记录当前正在读请求、连上游、发请求、读响应、发响应还是关闭。

客户端 Endpoint 和上游 Endpoint 分别拥有各自的 fd，但共享同一个 `ProxySession`。因此任何一端发生事件，会话都能看到另一端当前处于什么状态。

## 5.3 一次请求怎样推进

1. `read_client()` 循环读取客户端字节并交给 `RequestParser`。
2. 请求完整后放入 `to_upstream_`，`connect_upstream()` 创建非阻塞上游连接。
3. `connect()` 返回 `EINPROGRESS` 时等待上游 fd 可写，再由 `finish_upstream_connect()` 读取 `SO_ERROR` 判断真正结果。
4. `write_upstream()` 处理短写和 `EAGAIN`，直到完整请求发完。
5. `read_upstream()` 把响应分片交给 `ResponseParser`；四种正文边界都确认完成后进入发送响应。
6. `write_client()` 处理部分写；本轮结束后根据客户端要求关闭，或重置解析器回到读下一条请求。

`on_event()` 是总调度入口。它不是按固定顺序把所有函数都调用一遍，而是结合“事件来自哪一端、fd 当前可读还是可写、状态机目前在哪一步”选择允许执行的动作。

## 5.4 已验证与边界

阶段 5 的 10 项真实 TCP 集成测试覆盖请求体、HEAD、四类响应、顺序 Keep-Alive、半关闭、16 并发、上游拒绝/提前关闭/畸形响应和 40 次会话 fd 回收。阶段完成时 Debug 与 ASan/UBSan 全量 `73/73`。

该阶段仍使用固定上游 `127.0.0.1:19080`，只返回 `400/413/502`；路由 `404/503` 留给阶段 6，超时 `504` 留给阶段 7。响应先完整缓存在受控容量内再发送，不代表已经完成流式跨端背压。

用户阅读了阶段 5 的对象关系、事件调度及部分读写函数，并在源码中留下中文笔记；由于时间安排改变，没有逐行读完全部实现，因此只能表述为部分理解，不能表述成用户独立实现或完全掌握。

---

# 阶段 6：YAML 路由和上游池

## 6.1 用最直白的话说明

阶段 5 像一个只知道固定地址的快递员：所有请求都送到 19080。阶段 6 给它增加“地址簿和分拣台”：

```text
请求 Host + 请求路径
→ 查 YAML 中的路由
→ 找到对应上游池
→ 从健康节点中轮流选一个
→ 整理 Header
→ 交给阶段 5 的状态机发送
```

因此阶段 6 没有替换阶段 5 的网络发动机，只决定“这次应该送到哪里、转发前后需要整理哪些说明信息”。

## 6.2 YAML 配置负责什么

`config/edgegate.yaml` 当前只写已经实现并会实际生效的内容：

- EdgeGate 监听的数字 IPv4 和端口。
- Header、请求体、响应体容量上限。
- 上游池名称以及池内每个节点的 ID、IPv4、端口。
- 路由 ID、Host、路径前缀和引用的上游池。

`load_edgegate_config()` 先完整读取和校验，再返回配置对象。字段拼错、类型错误、端口越界、域名地址、重复 ID、空上游池、未知池引用和非法路由都会让程序启动失败，并给出文件、行列和原因；不会带着半套配置继续运行。

本阶段只支持数字 IPv4。域名可能指向内网，也可能指向公网；是否访问公网取决于解析结果和系统路由。但普通 DNS 解析可能阻塞单 Reactor，所以域名和异步 DNS 缓存没有塞进阶段 6。

## 6.3 `RouteTable` 怎样选择

1. 先规范化 Host：忽略大小写、端口和末尾点，并拒绝非法格式。
2. 精确 Host 比 `*.example.com` 通配 Host 优先。
3. 多个通配同时匹配时，后缀更长的规则更具体，优先级更高。
4. Host 优先级确定后，选择最长的字面路径前缀；查询字符串不参加匹配。
5. 在命中路由的健康上游中按游标轮询；选完一个后，游标移到下一个。

没有路由是 `404 Not Found`；路由存在但没有健康节点是 `503 Service Unavailable`；连接已选节点失败才是 `502 Bad Gateway`。三个错误代表不同层次，不能混成一个状态码。

## 6.4 Header 调整在做什么

客户端与 EdgeGate 是连接 A，EdgeGate 与上游是连接 B。`Connection`、`Keep-Alive` 等 Header 只描述当前连接，不能从 A 原样复制到 B。

请求侧会：

- 保留业务 Header 和原始 Host。
- 删除 Hop-by-hop Header，以及 `Connection` 点名要求删除的 Header。
- 删除客户端自己填写的 `X-Forwarded-*`，防止伪造来源。
- 根据真实客户端连接生成 `X-Forwarded-For`，根据原请求生成 `X-Forwarded-Host` 和 `X-Forwarded-Proto: http`。
- 发给上游时使用 `Connection: close`，符合当前每轮请求建立一条上游连接的设计。

响应侧同样隔离两条连接。由于 EdgeGate 已经完整解析并缓存响应，可以把 chunked 或“上游关闭才结束”的正文重新生成成明确 `Content-Length`。这样上游连接关闭后，客户端与 EdgeGate 的 Keep-Alive 仍可以继续。

## 6.5 正式程序和三个后端

- `edgegate`：现在读取 YAML，创建共享路由表并启动非阻塞代理，已不再是阶段 2 的阻塞程序。
- `apps/edgegate_main.cpp`：继续保留阶段 2 阻塞源码作为学习对照，但不再编译成正式 `edgegate`。
- `edgegate_proxy_demo`：继续保留阶段 5 固定上游对照。
- `edgegate_test_backend <name> <port>`：可启动三个独立后端，每个正文返回自己的名字，用于肉眼观察轮询顺序。

真实链路的六次请求结果为：

```text
backend-a
backend-b
backend-c
backend-a
backend-b
backend-c
```

通配 Host 加 `/static` 路由也能命中后端；未配置路径得到 `404`。测试结束后 18080、19081、19082、19083 没有残留监听。

## 6.6 测试证据与一次误报

阶段 6 新增配置、Header 和代理集成测试，并保留所有旧阶段回归：

- Debug：`94/94`。
- ASan/UBSan：`94/94`，包括泄漏检测。
- Release：`94/94`。
- G++ 11.4、CMake 3.22，`-Wall -Wextra -Wpedantic` 未出现警告。

Header 测试曾错误报告“Host 出现两次”。根因是测试搜索普通字符串 `Host: ...`，同时命中了 `X-Forwarded-Host: ...`。改为搜索带前后 `\r\n` 的完整 Header 行后通过。这说明测试代码本身也可能制造误报，失败时必须先区分产品缺陷和断言缺陷。

## 6.7 当前明确没有完成什么

- `healthy` 目前只是路由表中的状态；还没有定时主动探测后端。
- 没有连接、读写和请求总超时，因此还不会返回真正的 `504`。
- 没有根据缓冲区高低水位联动暂停客户端与上游读取。
- 没有失败摘除、恢复上线和 GET/HEAD 安全重试。
- 没有热重载、管理 CLI、日志、指标和 Dashboard。

这些属于阶段 7 以后。阶段 6 由 AI 主导完成，用户决定了 Header 信任边界、只配置当前能力和 IPv4-only 范围；项目验证通过不等于用户已逐行掌握实现。

---

# 阶段 7：流式转发和可靠性

## 7.1 这一阶段解决什么

阶段 5/6 的响应流程是“把上游整条响应收完，再一次性交给客户端”。阶段 7 改成：

```text
上游收到一批正文
→ 放入固定上限的 to_client_ 缓冲
→ 客户端能写就立即发送
→ 缓冲达到高水位时暂停读取上游
→ 降到低水位后恢复读取上游
```

请求体方向同理使用 `to_upstream_`。因此总正文仍可允许 1 MiB/8 MiB，但任一时刻不需要把整条正文都放在代理缓冲区中。

## 7.2 主要模块怎样配合

- `ReliableProxyServer`：正式阶段 7 服务入口，创建 EventLoop、监听端点、共享运行时和 `timerfd`。
- `ReliableSession`：一条客户端连接的总状态机，协调客户端 fd、当轮上游 fd、两个有界缓冲、HTTP 边界、超时和重试。
- `rewrite_*_head_*()`：只重新生成请求头/响应头；正文不复制到大字符串，交给流式缓冲转发。
- `ChunkedStreamDecoder`：不保存完整正文，只逐段确认块长度、数据、CRLF、0 块和 Trailer 合法，并累计解码后总长度。
- `ReliableRuntime`：每 100 ms 接收一次 `timerfd` 事件，检查会话期限并推进非阻塞健康探测。
- `RouteTable` 新接口：重试时排除已尝试 ID；按地址端口同步多个路由副本的健康状态；提供去重物理节点列表。

## 7.3 超时、重试和健康

YAML 新增每方向 `256/192/64 KiB` 容量/高/低水位，以及客户端头、上游连接、上游响应头、I/O 空闲、请求总时长和 Keep-Alive 空闲超时。

GET/HEAD 只有在请求体为 0、客户端实际收到 0 个响应字节、且尚未重试过时，才能排除失败节点后换另一个健康节点。POST 不重试。已经发送半条响应后不能再发送另一条错误响应，只能关闭连接。

主动健康探测每 5 秒向 `/health` 发 GET，2xx 算成功；默认连续 3 次失败摘除，连续 2 次成功恢复。连接失败、超时和协议错误等真实请求故障也计入被动失败。全部不健康时路由返回 503；上游超时在尚未返回响应时返回 504。

## 7.4 四类响应如何流动

- 无正文：响应头结束即完成。
- `Content-Length`：保留长度，边读边转发，精确读够后完成。
- chunked：保留 `Transfer-Encoding: chunked` 和原始分块字节，同时增量校验，读到合法 0 块完成。
- 关闭定界：上游 EOF 才完成；响应头强制 `Connection: close`，正文发完后关闭客户端。

## 7.5 验证与修复

- Debug、ASan/UBSan、Release：全量 `110/110`，G++ 11.4，严格编译无警告。
- 4 MiB 大响应、32 KiB 测试缓冲和小客户端接收窗口实际触发背压暂停与恢复，正文逐字节一致。
- 512 KiB POST 通过 32 KiB 请求方向缓冲完整到达上游，证明上传正文没有整包缓存。
- GET 首节点拒绝后换第二节点成功且重试次数为 1；POST 同条件返回 502 且重试为 0。
- 上游发送部分 200 后提前关闭时，客户端得到部分响应后断开，未被拼接 502。
- chunked 与关闭定界顺序 Keep-Alive、响应头 100 ms 超时返回 504、主动健康摘除后 503和恢复后 200均通过。
- 第一处缺陷是 chunk size 无扩展时错误处理 `find(';') == npos`，由逐字节测试发现。
- 第二处缺陷是 chunked 完成并恢复 Keep-Alive 后，当前循环又读取一次已移除的旧上游 fd；`strace` 证明旧 EOF 被误判成 502，修复为上游 fd 移除后立即退出当前读循环。
- 正式进程联调六次结果为 `backend-a/b/c/a/b/c`，结束后临时进程已清理。

## 7.6 能力边界

阶段 7 没有实现上游连接复用、DNS、TLS、请求流水线、chunked 请求体、网页管理或热重载。健康探测和会话均在同一个非阻塞事件循环中推进，没有另开网络线程。该阶段由 AI 主导实现，用户确定了真流式路线及关键边界，但尚未逐段复核源码。

---

# 阶段 8：运行时管理、热重载和平滑退出

## 8.1 这一阶段解决什么

阶段 7 的 EdgeGate 已经能长期处理代理流量，但要查看状态或停止程序，仍只能看测试计数或直接结束进程。阶段 8 增加了一条只在本机使用的“管理旁路”：

```text
管理员在终端执行 edgegatectl status
→ edgegatectl 连接 Unix Socket
→ 发一行 {"command":"status"}
→ EdgeGate 的同一个 EventLoop 处理命令
→ 回一行 JSON
→ edgegatectl 整理成人能读的文字后退出
```

它不经过对外的 18080 HTTP 端口，也不转发到业务上游。管理连接与客户端代理连接是两类不同 fd，但都由同一个 epoll/EventLoop 分派。

## 8.2 为什么 CLI 执行一条命令就退出

本阶段在“常驻交互 Shell”和“单次命令”之间选择了单次命令。每次执行只完成一次连接、一次请求和一次响应：

```text
edgegatectl status
edgegatectl reload
edgegatectl drain
```

这样 Shell 脚本可以直接依靠退出码判断成功或失败；命令之间不存在旧连接、登录状态或半完成输入。需要持续刷新画面的场景由阶段 9 Dashboard 负责，而不是让 CLI 同时承担终端界面。

退出码约定为：成功 `0`，服务明确拒绝命令 `1`，命令行用法错误 `2`，Socket/JSON 通信错误 `3`。

## 8.3 主要函数和对象

- `make_management_listener()`：创建非阻塞 Unix Socket，拒绝覆盖不属于当前用户的路径，设为 `0600`，接收管理连接并注册给 EventLoop。
- `ManagementConnection::read_command()`：增量读取最多 4096 字节的一行 JSON；解析完成后调用统一命令处理函数。
- `ManagementConnection::write_response()`：处理短写和 `EAGAIN`，把一行 JSON 完整发送后关闭这条短连接。
- `edgegatectl main()`：解析命令和 `--socket/--json`，连接管理 Socket，发送命令，按退出码区分服务拒绝与通信失败，并把 JSON 转成人类表格。
- `ReliableRuntime::handle_management_command()`：统一分派 `status/routes/upstreams/stats/reload/drain/stop`，因此 CLI 与后续 Dashboard 可以共享同一份运行数据。
- `reload_configuration()`：先在局部变量中加载 YAML、构造 RouteTable 和 Logger；全部成功后才替换当前指针。任何异常都返回失败并保留旧配置。
- `request_drain()`：从 EventLoop 删除 TCP Listener，所以新客户端不能再连接；把现有 Session 标成“本轮响应后关闭”。
- `ReliableRuntime::tick()`：每 100 ms 检查排空是否完成或超时。超时后强制关闭仍卡住的会话；普通 drain 完成后保留管理面，stop 则停止 EventLoop。
- `block_termination_signals()` 与 `SignalControl::on_event()`：先屏蔽 SIGTERM/SIGINT，再用 `signalfd` 把信号转换成 epoll 可读事件，最终调用与 CLI stop 相同的排空逻辑。
- `RuntimeLogger`：为 access/error/management 各建一个轮转文件 Logger，共用 8192 条有界异步队列；磁盘跟不上时覆盖最旧日志，不阻塞网络 Reactor。

## 8.4 reload 的“原子切换”是什么意思

原子不是说 CPU 只执行一条指令，而是说其他代码只能看到“完整旧配置”或“完整新配置”，不会看到一半新路由配一半旧超时。

可热更新：路由、上游、超时、健康检查、排空期限和日志配置。必须重启：监听 IP/端口、管理 Socket 路径、解析容量、流缓冲容量和高低水位。后者与已经创建的 fd 或 Buffer 绑定，强行热换会造成对象实际容量和配置文字不一致。

当前请求保存自己开始时的 RouteTable 和超时快照，因此 reload 后不会在失败重试时突然换到另一套业务路由；下一条请求才读取新快照。

## 8.5 drain、stop 和信号的区别

- `drain`：停止接受新客户端，完成现有请求；之后 EdgeGate 仍运行，`edgegatectl status/stop` 仍可用。
- `stop`：先执行 drain，现有请求结束或达到期限后退出整个进程。
- `SIGTERM/SIGINT`：入口不同，但进入后与 stop 共用完全相同的状态机。

重复执行 drain/stop 不会创建第二套关闭流程。排空期限默认 30 秒；集成测试缩短为 500 ms，用一个永不返回响应的上游证明期限到达后会强制回收会话。

## 8.6 日志安全边界

访问日志记录 method、Host、路径、状态码、已发响应字节、延迟和上游地址。不记录请求/响应正文、Cookie 或 Authorization；URL 查询字符串也被去掉，因为查询参数中可能含令牌或密码。错误日志只记录事件类型和受控原因，管理日志记录命令名和成功与否。

阶段 8 开发配置的日志目录位于 `/tmp`；阶段 10 安装版已改为 `/var/log/edgegate`，并由 systemd 授予低权限服务用户写权限。

## 8.7 验证证据

- Debug、ASan/UBSan（含泄漏检查）、Release 三套独立构建：全量 `117/117`，G++ 11.4，严格编译零警告。
- 新增配置测试：管理/日志字段加载、相对 Socket 路径拒绝、非法日志级别拒绝。
- 新增运行时测试：Socket `0600`、四类查询、合法重载、新旧配置原子边界、不可热变更拒绝、重复 drain、排空超时和日志轮转。
- 真实进程脚本启动三个后端和正式 `edgegate`，合法 reload 后 `/api` 变为 404、`/changed` 成功；无效 reload 退出 1，旧 `/changed` 路由继续成功。
- drain 后新 TCP 连接失败，但管理 Socket 仍能查询；CLI stop 和真实 SIGTERM 都退出 0，Socket 文件随进程清理。
- `tests/stage8_cli_acceptance.sh` 最终输出 `STAGE8_CLI_ACCEPTANCE=PASS`。

## 8.8 当前能力边界

阶段 8 的 JSON 是命令响应，不是浏览器 HTTP API；还没有历史曲线、状态码分布、延迟分位数、最近错误内存摘要和网页。CLI 也不常驻刷新。以上由阶段 9完成。

本阶段由 AI 主导实现和验证。用户决定采用单次命令 CLI，并确认其职责是查看状态、reload、drain 和 stop；项目通过不等于用户已经逐行掌握 Unix Socket、JSON、spdlog 或 signalfd 源码。

---

# 阶段 9：聚合指标与只读 Dashboard

## 9.1 这一阶段解决什么

阶段 8 的 `edgegatectl` 适合“执行一次命令、查看一次结果”，但不适合让人持续观察服务。阶段 9 增加一个只在本机监听的网页仪表盘：

```text
客户端请求经过 EdgeGate
→ 请求真正完成时记录状态码、耗时、路由和上游
→ 固定内存保存聚合数字、最近错误和少量慢请求
→ 浏览器每秒请求 /api/dashboard
→ JavaScript 把 JSON 更新成卡片、状态条和表格
```

这里的前端不参与代理，不控制服务，也不直接连接 Unix Socket。它只负责把 C++ 后端已经整理好的只读数据展示出来。

## 9.2 为什么独立使用 18081

三个入口职责不同：

```text
127.0.0.1:18080    代理业务 HTTP 请求
/tmp/edgegate.sock edgegatectl 管理命令
127.0.0.1:18081    浏览器只读 Dashboard
```

如果把 Dashboard 放进 18080 的某个保留路径，它可能与用户配置的 `/api`、`/static` 等路由冲突，也可能跟随业务监听地址暴露出去。独立的 18081 只允许配置为 `127.0.0.1`，不会占用任何代理路由。

`dashboard.enabled/address/port` 决定是否创建监听 fd，修改后必须重启；刷新周期、错误数量、慢请求阈值和数量只是内存统计参数，可以在合法 reload 后更新。

## 9.3 ObservabilityStore 如何统计

- `record_request()`：只在一次响应的输出缓冲区真正发空后调用，累计请求数、响应字节、状态码、路由结果和总耗时。这样“上游已经返回”但客户端还没收完的请求不会被提前算作完成。
- `record_upstream_result()`：按物理 `address:port` 记录业务尝试、成功、失败和超时。主动健康探测不计入业务请求数字。
- `record_error()`：把受控错误摘要写入固定长度双端队列；满后删除最旧项。
- `snapshot()`：把当前统计复制成一份 JSON，不让前端自己推断内部状态。
- `reconfigure()`：更新有界记录参数；如果新上限更小，立即从队首删除多出的旧记录。

这些方法都在同一个 EventLoop 线程中调用，所以热路径不需要为每个请求加互斥锁。

## 9.4 延迟桶、P99 和慢请求的区别

“延迟”是从 EdgeGate 收到当前请求第一批字节，到完整响应写入客户端 Socket 为止的时间，包含请求接收、解析、路由、上游连接/处理和响应转发。

`ObservabilityStore` 不保存所有请求耗时，而是累计到 13 个固定区间：1、5、10、25、50、100、250、500、1000、2500、5000、10000 ms 和更慢区间。P50/P95/P99 根据累计数量落在哪个区间得到，因此是近似值。

P99 回答“99% 的请求大约不超过多久”，但不能说明哪个请求慢。慢请求摘要按 `slow_request_threshold_ms` 另行保留最近少量样本，包含方法、路径、路由、上游、状态、耗时和尝试次数。两者分别负责总体趋势和排查入口。

## 9.5 隐私和固定内存边界

慢请求路径在代理会话和指标存储两个边界都会删除 `?` 后的查询参数。记录中不包含请求体、响应体、Cookie、Authorization 或完整 Header。真实验收发送了带 `token=must-not-appear` 的请求，最终 JSON 中不存在该字符串。

最近错误默认最多 50 条，慢请求默认最多 20 条；配置加载还设置 500 和 200 的硬上限。状态码、路由和上游只保存聚合计数，不保存逐请求历史，因此内存不会随请求总数线性增长。当前指标只属于本次进程生命周期，重启后归零，不是持久时序数据库。

## 9.6 DashboardServer 的主要对象

- `make_dashboard_listener()`：创建非阻塞 IPv4 Listener，主程序使用配置端口，测试可用端口 0 让内核选择临时端口。
- `DashboardListener::on_event()`：循环 `accept4()` 到 `EAGAIN`，把每个浏览器连接注册进现有 EventLoop。
- `DashboardConnection::read_request()`：最多读取 8192 字节，只接受受控 HTTP/1.0/1.1 GET 或 HEAD。
- `route_request()`：只提供 `/`、`/index.html`、`/app.css`、`/app.js` 和 `/api/dashboard`；未知路径 404，POST 等写方法 405。
- `write_response()`：处理短写和 `EAGAIN`，发完即关闭，避免为 Dashboard 引入新的 Keep-Alive 状态机。
- `ReliableRuntime::dashboard_json()`：生成 `schema_version=1` 的单一快照，组合服务状态、原子运行计数、路由、健康状态和 ObservabilityStore 指标。

HTTP 响应包含禁止嗅探、禁止 iframe、无 Referrer 和只允许同源脚本/样式/请求的安全头。API 使用 `Cache-Control: no-store`，浏览器不会拿旧快照冒充当前状态。

## 9.7 原生前端做什么

HTML 只提供页面结构；CSS 提供深色响应式布局；JavaScript 每次只执行 GET `/api/dashboard`，然后用 `textContent` 更新：

- 服务状态、运行时间、活动/累计连接、请求和错误；
- P50/P95/P99/MAX；
- 1xx～5xx 状态码分布；
- 路由请求/错误统计；
- 上游健康、尝试、成功、失败和超时；
- 慢请求和最近错误。

页面没有 reload、drain、stop、配置编辑或登录逻辑。接口失败时显示“数据不可用”，空列表显示明确的空状态。所有页面资源直接编译进 `edgegate`，运行时不依赖 Node.js、CDN 或单独静态文件目录。

## 9.8 验证证据

- 配置测试覆盖 Dashboard 字段、非本机地址拒绝和 250 ms 最小刷新间隔。
- 指标测试覆盖固定桶分位数、状态码、路由/上游聚合、旧记录淘汰、热更新收缩和查询参数脱敏。
- HTTP 测试覆盖页面、JSON、安全头、GET/HEAD、未知路径 404 和写方法 405。
- 代理集成测试发送真实 200 与 404 请求，确认 Dashboard 得到 2 条请求、对应状态码、上游成功和最近错误；Dashboard 自己的访问不计入代理流量。
- Debug、ASan/UBSan（含泄漏检查）、Release 三套独立构建：全量 `125/125`，G++ 11.4，严格编译零警告。
- `tests/stage9_dashboard_acceptance.sh` 启动三个后端，其中一个固定延迟 150 ms；真实检查页面/CSS/JS、六个 200、一个 404、慢请求、路由/上游数字、查询参数脱敏和只读边界，最终输出 `STAGE9_DASHBOARD_ACCEPTANCE=PASS`。

## 9.9 当前能力边界和下一阶段

阶段 9 没有实现历史曲线、跨重启指标持久化、Prometheus 协议、用户登录、远程 Dashboard 或网页控制。P50/P95/P99 是固定桶近似值，不是保存全部样本后精确排序。页面资源和数据接口已经实测，但本阶段没有进行人工浏览器视觉点击验收。

后续的阶段 10 已完成 `cmake --install`、systemd 服务、正式配置/日志/运行目录、README、用户手册、架构说明、故障排查和许可证。阶段 9 由 AI 主导实现；用户决定了独立端口、聚合而非全量请求记录，以及保留少量慢请求摘要的产品边界，尚未逐行复核实现源码。

---

# 阶段 10：从开发程序到 Linux 产品服务

## 10.1 这一阶段解决什么

阶段 9 结束时，EdgeGate 已经具有代理、可靠性、管理和观测功能，但仍然需要在构建目录手动指定配置运行。阶段 10 把它变成 Ubuntu 可管理的服务：

```text
Windows 权威源码/Git
        ↓ 单向同步
Ubuntu 干净构建和 CTest
        ↓ cmake --install
/usr/local 程序、systemd 单元、示例和文档
        ↓ 管理脚本
/etc 配置 + /run Socket + /var/log 日志 + 低权限用户
        ↓ systemctl
可启动、停止、重启、reload 和开机自启的 EdgeGate
```

Windows 并不是“没有产品”：它保存产品的唯一权威源码和历史。Ubuntu 保存 Linux 构建副本和真实安装实例，因为 `epoll`、`timerfd`、`signalfd` 和 systemd 都是 Linux 能力。

## 10.2 CMake 安装规则

`GNUInstallDirs` 不让 CMakeLists 到处硬编码 `bin`、`lib`、`share`。默认前缀是 `/usr/local`，安装后的主要文件为：

| 安装结果 | 作用 |
| --- | --- |
| `/usr/local/bin/edgegate` | 正式代理服务 |
| `/usr/local/bin/edgegatectl` | 本机管理 CLI |
| `/usr/local/libexec/edgegate/edgegate_test_backend` | 演示/验收工具，不伪装成产品公开命令 |
| `/usr/local/share/edgegate/examples/edgegate.yaml` | 只读生产配置样例 |
| `/usr/local/lib/systemd/system/edgegate.service` | systemd 单元 |
| `/usr/local/share/doc/EdgeGate/` | README 和三份手册 |

`configure_file()` 在 CMake 配置阶段把实际安装路径写入 systemd 单元和管理脚本。`cmake --install` 只安装受版本管理的资产，不自动覆盖已有 `/etc` 配置，也不默认启动服务。

## 10.3 为什么不以 root 运行

`sysusers.d` 创建没有家目录、不能登录的 `edgegate` 用户。`tmpfiles.d` 和 systemd 创建只对该用户开放的目录：

- `/etc/edgegate/edgegate.yaml`：`root:edgegate 0640`，服务可读，不可改。
- `/run/edgegate`：`edgegate:edgegate 0750`，保存重启后可重建的 `0600` 管理 Socket。
- `/var/log/edgegate`：`edgegate:edgegate 0750`，仅服务可写日志。

如果代理中存在未知缺陷，进程也不默认拥有 root 权限。管理员运行 `edgegatectl` 时通过 sudo 明确提权，而不是把 Socket 放宽给所有本机用户。

## 10.4 systemd 单元做什么

- `ExecStart`：指定正式二进制和 `/etc` 配置。
- `ExecReload`：通过 `edgegatectl reload` 调用阶段 8 原子重载。
- `KillSignal=SIGTERM`：`systemctl stop` 进入已实现的 signalfd 排空状态机。
- `Restart=on-failure`：异常失败后重启，管理员正常 stop 不重启。
- `RuntimeDirectory`/`LogsDirectory`：启动时校正所有者和权限。
- `ProtectSystem=strict`、空 capability 集合等：文件系统默认只读，禁止获得新特权和修改内核设置。

最终实测 `User=edgegate`、`Group=edgegate`、`ActiveState=active`、`SubState=running`，主进程是 uid/gid 999。

## 10.5 管理安装器

`edgegate-service-install` 是需要 sudo 的管理员脚本：

1. 调用 `systemd-sysusers` 创建低权限用户。
2. 创建 `/etc/edgegate`，只在配置不存在时复制示例。
3. 调用 `systemd-tmpfiles` 创建运行和日志目录。
4. `systemctl daemon-reload`，使 systemd 看到新单元。
5. 只有显式传入 `--enable-now` 才开机自启并立即启动。

重复运行时保留现有配置，真实验收在前后各计算一次 SHA-256，确认两者完全一致。

## 10.6 文档交付

- `README.md`：陌生用户从 apt 依赖、构建、CTest、安装、三上游轮询到 Dashboard 的最短成功路径。
- `docs/user-guide.md`：正式目录、每组 YAML 配置和每条 CLI 命令。
- `docs/architecture.md`：一次请求从 epoll 就绪到响应返回的模块链路。
- `docs/troubleshooting.md`：按启动失败、CLI 无法连接、404、502/503/504、reload、Dashboard 和资源增长分类排查。
- `LICENSE`：用户确认的 2026 `lkdr` MIT License。

Windows 浏览器不需要改动 Dashboard 安全边界；使用 `ssh -L 18081:127.0.0.1:18081 ...` 将 Ubuntu 本机端口映射到 Windows 本机即可展示。

## 10.7 两层验收

`stage10_install_layout_test.sh` 使用 `DESTDIR` 把 `/usr/local` 暂时映射到 `mktemp` 目录，不需 root，不修改真实系统。它检查二进制、服务资产、文档、脚本语法和生产配置不含开发 `/tmp` 路径，并已加入 CTest。

`stage10_systemd_acceptance.sh` 在真实系统中检查：

- 专用用户和文件/Socket 权限；
- 三个上游的六次请求轮询；
- CLI 状态/路由/上游/统计和 Dashboard HTTP/JSON；
- systemd reload、restart 和就绪后 SIGTERM；
- 重复安装不覆盖正式配置。

最终六项输出全部 PASS，总验收为 `STAGE10_SYSTEMD_ACCEPTANCE=PASS`。

## 10.8 验收脚本的两次闭环修正

第一次没有 PASS 是因为脚本使用普通用户遍历 `0750 root:edgegate` 配置目录。这个“Permission denied”是最小权限正确，脚本却把它当成产品失败。修正为 sudo 检查，同时为 `set -e` 增加失败行和命令，不再静默退出。

第二次 SIGTERM 断言失败是因为脚本在 `systemctl restart` 返回 active 后立即 stop。对 `Type=simple` 来说，active 只说明进程已启动，不保证程序已创建管理 Socket 和 signalfd。手动对照测试证明就绪后的同一二进制退出码为 0，日志完整记录 SIGTERM 和排空完成。最终脚本要求 `edgegatectl status` 实际成功后才测试 stop，并在中途失败时恢复服务。

## 10.9 验证证据与当前边界

- Ubuntu 22.04.5、G++ 11.4、CMake 3.22。
- Debug、ASan/UBSan（`detect_leaks=1`）、Release 三套干净构建全量 `126/126`，严格编译零警告。
- 真实安装后 `edgegate.service` 为 enabled/active，Dashboard 返回 `HTTP/1.1 200 OK`。
- 代理和 Dashboard 仍只监听 `127.0.0.1:18080/18081`，展示不需要对局域网暴露管理页。

阶段 10 没有完成 100 并发/10 万请求、故障注入矩阵、长时间文件描述符/内存趋势和 12 小时长稳，这些属于阶段 11。它也没有发布 GitHub 或打 `v1.0.0` 标签，这属于阶段 12。

本阶段代码、脚本和文档由 AI 主导编写。用户决定采用 Ubuntu 真实产品展示、Windows 保留权威源码，亲自执行需要 sudo 的安装和验收，并通过“没有 PASS”和两次具体失败行推动验收闭环；不能把本阶段写成用户独立完成全部实现。

---

# 后续阶段维护规则

从阶段 3 开始，每当用户完成一个阶段的阅读和复核后，除了确认该阶段已有本地 Git 提交，还要在本文件追加对应总结。

每个阶段至少包含：

1. 阶段目标和要解决的问题。
2. 整体输入、处理和输出流程。
3. 以一个函数、类或完整功能代码块为单位的解释。
4. 新术语第一次出现时的直白说明。
5. 实际构建、测试和故障验证结果。
6. 已实现、已验证和尚未支持的能力边界。
7. 与下一阶段之间的连接。

阶段总结最终要让我能够解释：

> 这一阶段为什么要做、数据怎样流动、每个主要模块负责什么、有什么证据证明有效，以及它还不能解决什么。
